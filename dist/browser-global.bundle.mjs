// Generated by scripts/build.mjs. Do not edit by hand.
const __modules = Object.create(null);
const __cache = Object.create(null);
__modules["src/index.mjs"] = (__exports, __import) => {
  const __reexport1 = __import("src/shell.mjs");
  __exports["TerminalCore"] = __reexport1["TerminalCore"];
  const __reexport2 = __import("src/extension.mjs");
  __exports["composeExtensions"] = __reexport2["composeExtensions"];
  __exports["defineCommandPack"] = __reexport2["defineCommandPack"];
  __exports["defineExtension"] = __reexport2["defineExtension"];
  __exports["defineProfile"] = __reexport2["defineProfile"];
  __exports["filterRecords"] = __reexport2["filterRecords"];
  __exports["formatRecords"] = __reexport2["formatRecords"];
  __exports["getExtensionDiagnostics"] = __reexport2["getExtensionDiagnostics"];
  __exports["getRecordValue"] = __reexport2["getRecordValue"];
  __exports["mergeProfileOptions"] = __reexport2["mergeProfileOptions"];
  __exports["normalizeProperties"] = __reexport2["normalizeProperties"];
  __exports["projectRecords"] = __reexport2["projectRecords"];
  __exports["sortRecords"] = __reexport2["sortRecords"];
  __exports["validateExtension"] = __reexport2["validateExtension"];
  const __reexport3 = __import("src/vfs.mjs");
  __exports["MemoryFileSystem"] = __reexport3["MemoryFileSystem"];
  __exports["VfsError"] = __reexport3["VfsError"];
  __exports["createLinuxLikeFs"] = __reexport3["createLinuxLikeFs"];
  const __reexport4 = __import("src/diagnostics.mjs");
  __exports["onDiagnostic"] = __reexport4["onDiagnostic"];
  __exports["reportDiagnostic"] = __reexport4["reportDiagnostic"];
  __exports["withDiagnostic"] = __reexport4["withDiagnostic"];
  const __reexport5 = __import("src/plugins/basic-commands.mjs");
  __exports["basicCommandsPlugin"] = __reexport5["basicCommandsPlugin"];
  const __reexport6 = __import("src/plugins/system-commands.mjs");
  __exports["systemCommandsPlugin"] = __reexport6["systemCommandsPlugin"];
  const __reexport7 = __import("src/plugins/effect-events.mjs");
  __exports["effectEventsPlugin"] = __reexport7["effectEventsPlugin"];
  const __reexport8 = __import("src/plugins/windows-commands.mjs");
  __exports["windowsCommandsPlugin"] = __reexport8["windowsCommandsPlugin"];
  __exports["toWindowsPath"] = __reexport8["toWindowsPath"];
  __exports["fromWindowsPath"] = __reexport8["fromWindowsPath"];
  const __reexport9 = __import("src/plugins/feed-posts.mjs");
  __exports["feedPostsPlugin"] = __reexport9["feedPostsPlugin"];
  __exports["fetchFeedPosts"] = __reexport9["fetchFeedPosts"];
  __exports["fetchDiscoveredFeedPosts"] = __reexport9["fetchDiscoveredFeedPosts"];
  __exports["parseFeedPosts"] = __reexport9["parseFeedPosts"];
  __exports["discoverFeedUrl"] = __reexport9["discoverFeedUrl"];
  const __reexport10 = __import("src/plugins/hugo-adapter.mjs");
  __exports["hugoPostsPlugin"] = __reexport10["hugoPostsPlugin"];
  __exports["fetchHugoPosts"] = __reexport10["fetchHugoPosts"];
  const __reexport11 = __import("src/presets/blog-sandbox.mjs");
  __exports["blogSandboxPreset"] = __reexport11["blogSandboxPreset"];
  const __reexport12 = __import("src/adapters/dom.mjs");
  __exports["DEFAULT_TERMINAL_CSS"] = __reexport12["DEFAULT_TERMINAL_CSS"];
  __exports["createDomTerminalAdapter"] = __reexport12["createDomTerminalAdapter"];
  __exports["injectDefaultStyles"] = __reexport12["injectDefaultStyles"];
  const __reexport13 = __import("src/adapters/starter.mjs");
  __exports["mountStarterTerminal"] = __reexport13["mountStarterTerminal"];
  const __reexport14 = __import("src/adapters/static-site.mjs");
  __exports["mountStaticTerminal"] = __reexport14["mountStaticTerminal"];
  const __reexport15 = __import("src/adapters/feed.mjs");
  __exports["createFeedTerminal"] = __reexport15["createFeedTerminal"];
  __exports["mountFeedTerminal"] = __reexport15["mountFeedTerminal"];
  const __reexport16 = __import("src/adapters/hugo.mjs");
  __exports["createHugoTerminal"] = __reexport16["createHugoTerminal"];
  __exports["mountHugoTerminal"] = __reexport16["mountHugoTerminal"];
  const __reexport17 = __import("src/adapters/persistence.mjs");
  __exports["createSessionStorageAdapter"] = __reexport17["createSessionStorageAdapter"];
  __exports["createStorageAdapter"] = __reexport17["createStorageAdapter"];
  __exports["memoryPersistenceAdapter"] = __reexport17["memoryPersistenceAdapter"];
  const __reexport18 = __import("src/result.mjs");
  __exports["ok"] = __reexport18["ok"];
  __exports["fail"] = __reexport18["fail"];
  __exports["normalizeResult"] = __reexport18["normalizeResult"];
  const __reexport19 = __import("src/factory.mjs");
  __exports["createTerminal"] = __reexport19["createTerminal"];
  __exports["createWindowsTerminal"] = __reexport19["createWindowsTerminal"];
  const __reexport20 = __import("src/protocol/schema.mjs");
  __exports["TERMLET_PROTOCOL"] = __reexport20["TERMLET_PROTOCOL"];
  __exports["getActionSchema"] = __reexport20["getActionSchema"];
  __exports["getEventSchema"] = __reexport20["getEventSchema"];
  __exports["getSnapshotSchema"] = __reexport20["getSnapshotSchema"];
  __exports["isTerminalAction"] = __reexport20["isTerminalAction"];
  __exports["isTerminalEvent"] = __reexport20["isTerminalEvent"];
  const __reexport21 = __import("src/protocol/errors.mjs");
  __exports["ERR_CAPABILITY_DENIED"] = __reexport21["ERR_CAPABILITY_DENIED"];
  __exports["ERR_COMMAND_INTERRUPTED"] = __reexport21["ERR_COMMAND_INTERRUPTED"];
  __exports["ERR_COMMAND_NOT_FOUND"] = __reexport21["ERR_COMMAND_NOT_FOUND"];
  __exports["ERR_COMMAND_RUNNING"] = __reexport21["ERR_COMMAND_RUNNING"];
  __exports["ERR_COMMAND_TIMEOUT"] = __reexport21["ERR_COMMAND_TIMEOUT"];
  __exports["ERR_INTERNAL"] = __reexport21["ERR_INTERNAL"];
  __exports["ERR_INVALID_ACTION"] = __reexport21["ERR_INVALID_ACTION"];
  __exports["ERR_INVALID_STATE"] = __reexport21["ERR_INVALID_STATE"];
  __exports["ERR_MODE_UNSUPPORTED"] = __reexport21["ERR_MODE_UNSUPPORTED"];
  __exports["ERR_OUTPUT_LIMIT"] = __reexport21["ERR_OUTPUT_LIMIT"];
  __exports["ERR_PATH_NOT_FOUND"] = __reexport21["ERR_PATH_NOT_FOUND"];
  __exports["ERR_PERMISSION_DENIED"] = __reexport21["ERR_PERMISSION_DENIED"];
  __exports["ERR_SNAPSHOT_INVALID"] = __reexport21["ERR_SNAPSHOT_INVALID"];
  __exports["ERR_SNAPSHOT_UNSUPPORTED"] = __reexport21["ERR_SNAPSHOT_UNSUPPORTED"];
  const __reexport22 = __import("src/protocol/session.mjs");
  __exports["createTerminalSession"] = __reexport22["createTerminalSession"];
  const __reexport23 = __import("src/state/transcript.mjs");
  __exports["createTranscriptStore"] = __reexport23["createTranscriptStore"];
  const __reexport24 = __import("src/toolbox/input-controller.mjs");
  __exports["createInputController"] = __reexport24["createInputController"];
  const __reexport25 = __import("src/toolbox/completion.mjs");
  __exports["createCompletionEngine"] = __reexport25["createCompletionEngine"];
  const __reexport26 = __import("src/toolbox/output-stream.mjs");
  __exports["createOutputStreamController"] = __reexport26["createOutputStreamController"];
  const __reexport27 = __import("src/toolbox/mode-machine.mjs");
  __exports["createInteractionModeMachine"] = __reexport27["createInteractionModeMachine"];
  const __reexport28 = __import("src/toolbox/format.mjs");
  __exports["formatJson"] = __reexport28["formatJson"];
  __exports["formatTree"] = __reexport28["formatTree"];
  const __reexport29 = __import("src/toolbox/capability.mjs");
  __exports["createCapabilityBroker"] = __reexport29["createCapabilityBroker"];
  const __reexport30 = __import("src/toolbox/visual.mjs");
  __exports["createLayer"] = __reexport30["createLayer"];
  __exports["createPath"] = __reexport30["createPath"];
  __exports["createTimeline"] = __reexport30["createTimeline"];
  __exports["createVisualHost"] = __reexport30["createVisualHost"];
  __exports["getBounds"] = __reexport30["getBounds"];
  __exports["layoutTextPath"] = __reexport30["layoutTextPath"];
  __exports["tokenizeText"] = __reexport30["tokenizeText"];
  const __reexport31 = __import("src/testkit/contract.mjs");
  __exports["createAdapterContractTests"] = __reexport31["createAdapterContractTests"];
  __exports["createExtensionContractTests"] = __reexport31["createExtensionContractTests"];
  __exports["createSessionContractTests"] = __reexport31["createSessionContractTests"];
  
};
__modules["src/shell.mjs"] = (__exports, __import) => {
  const { fail, normalizeResult, ok, textByteLength } = __import("src/result.mjs");
  const { MemoryFileSystem, createLinuxLikeFs, VfsError } = __import("src/vfs.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  class TerminalCore {
    constructor(options = {}) {
      this.fs = options.fs || createLinuxLikeFs();
      if (!(this.fs instanceof MemoryFileSystem)) {
        throw new TypeError('TerminalCore expects a MemoryFileSystem instance');
      }
      this.user = options.user || 'guest';
      this.groups = options.groups || ['guest'];
      this.hostname = options.hostname || 'blog-server';
      this.cwd = options.cwd || `/home/${this.user}`;
      this.home = options.home || `/home/${this.user}`;
      this.env = {
        SHELL: '/bin/bash',
        TERM: 'xterm-256color',
        PATH: '/usr/local/bin:/usr/bin:/bin',
        LANG: 'en_US.UTF-8',
        HOME: this.home,
        USER: this.user,
        LOGNAME: this.user,
        PWD: this.cwd,
        OLDPWD: this.cwd,
        ...(options.env || {}),
      };
      this.aliases = { ...(options.aliases || {}) };
      this.commands = new Map();
      this.caseInsensitiveCommands = Boolean(options.caseInsensitiveCommands);
      this.backslashEscapes = options.backslashEscapes !== false;
      this.expandGlobs = options.expandGlobs !== false;
      this.maxLineLength = Math.max(256, Number(options.maxLineLength || 8000));
      this.maxCommandSubstitutionLength = Math.max(64, Number(options.maxCommandSubstitutionLength || 500));
      this.maxOutputBytes = Math.max(1024, Number(options.maxOutputBytes || 256 * 1024));
      this.commandTimeoutMs = Math.max(0, Number(options.commandTimeoutMs || 0));
      this.lastStatus = 0;
      this.history = [...(options.history || [])];
      this.maxHistory = options.maxHistory || 500;
      this.persistence = options.persistence || null;
      this.persistEnv = options.persistEnv || false;
      this.persistVfs = Boolean(options.persistVfs);
      this.profileName = options.profileName || 'default';
      this.formatPipelineData = typeof options.formatPipelineData === 'function'
        ? options.formatPipelineData
        : defaultPipelineFormatter;
      this.suppressNextPersist = false;
      this.pluginDisposers = [];
      (options.plugins || []).forEach(plugin => this.use(plugin));
      this.captureInitialVfsSnapshot();
      if (options.restore !== false && this.persistence?.load) {
        try {
          this.restore(this.persistence.load());
        } catch (error) {
          reportDiagnostic(error, { source: 'shell.restore.load' });
        }
      }
    }
  
    use(plugin, options = {}) {
      if (!plugin) return this;
      let dispose = null;
      if (typeof plugin === 'function') dispose = plugin(this, options);
      else if (typeof plugin.install === 'function') dispose = plugin.install(this, options);
      else throw new TypeError('plugin must be a function or { install() }');
      if (typeof dispose === 'function') this.pluginDisposers.push(dispose);
      return this;
    }
  
    register(name, handler, meta = {}) {
      this.commands.set(name, { name, handler, meta });
      return this;
    }
  
    unregister(name) {
      if (this.commands.delete(name)) return true;
      if (!this.caseInsensitiveCommands) return false;
      const normalized = String(name).toLowerCase();
      for (const key of this.commands.keys()) {
        if (key.toLowerCase() === normalized) return this.commands.delete(key);
      }
      return false;
    }
  
    hasCommand(name) {
      return Boolean(this.command(name));
    }
  
    command(name) {
      if (this.commands.has(name)) return this.commands.get(name);
      if (!this.caseInsensitiveCommands) return null;
      const normalized = String(name).toLowerCase();
      for (const [key, command] of this.commands) {
        if (key.toLowerCase() === normalized) return command;
      }
      return null;
    }
  
    alias(name) {
      if (Object.prototype.hasOwnProperty.call(this.aliases, name)) return this.aliases[name];
      if (!this.caseInsensitiveCommands) return null;
      const normalized = String(name).toLowerCase();
      const key = Object.keys(this.aliases).find(item => item.toLowerCase() === normalized);
      return key ? this.aliases[key] : null;
    }
  
    setAlias(name, value) {
      this.aliases[String(name)] = String(value);
      return this;
    }
  
    removeAlias(name) {
      if (Object.prototype.hasOwnProperty.call(this.aliases, name)) {
        delete this.aliases[name];
        return true;
      }
      if (!this.caseInsensitiveCommands) return false;
      const normalized = String(name).toLowerCase();
      const key = Object.keys(this.aliases).find(item => item.toLowerCase() === normalized);
      if (!key) return false;
      delete this.aliases[key];
      return true;
    }
  
    disposePlugins() {
      this.pluginDisposers.splice(0).reverse().forEach(dispose => {
        try {
          dispose(this);
        } catch (error) {
          reportDiagnostic(error, { source: 'shell.disposePlugins' });
        }
      });
      return this;
    }
  
    commandNames() {
      const names = [...this.commands.keys(), ...Object.keys(this.aliases)];
      if (!this.caseInsensitiveCommands) return [...new Set(names)].sort();
      const seen = new Set();
      return names.filter(name => {
        const key = name.toLowerCase();
        if (seen.has(key)) return false;
        seen.add(key);
        return true;
      }).sort((a, b) => a.localeCompare(b));
    }
  
    complete(line) {
      const input = String(line || '');
      const tokenMatch = input.match(/(?:^|\s)(\S*)$/);
      const token = tokenMatch ? tokenMatch[1] : '';
      const prefix = input.slice(0, input.length - token.length);
      const completingCommand = prefix.trim() === '';
      const values = completingCommand ? this.completeCommand(token) : this.completePath(token);
      return values.map(value => prefix + value);
    }
  
    completeCommand(token) {
      return this.commandNames().filter(name => (
        this.caseInsensitiveCommands
          ? name.toLowerCase().startsWith(String(token).toLowerCase())
          : name.startsWith(token)
      ));
    }
  
    completePath(token) {
      const raw = String(token || '');
      const slash = Math.max(raw.lastIndexOf('/'), raw.lastIndexOf('\\'));
      const dirToken = slash >= 0 ? raw.slice(0, slash + 1) : '';
      const nameToken = slash >= 0 ? raw.slice(slash + 1) : raw;
      const dirPath = dirToken ? dirToken.replace(/\\/g, '/') : '.';
      let names = [];
      try {
        const dir = this.fs.normalize(dirPath.replace(/\/$/, '') || '/', { cwd: this.cwd, home: this.home });
        names = this.fs.list(dir, { all: true, cwd: this.cwd, home: this.home }).map(name => {
          const path = dir === '/' ? `/${name}` : `${dir}/${name}`;
          const node = this.fs.stat(path);
          return dirToken + name + (node?.type === 'dir' ? '/' : '');
        });
      } catch (error) {
        reportDiagnostic(error, { source: 'shell.completePath' });
        return [];
      }
      return names.filter(name => {
        const base = slash >= 0 ? name.slice(dirToken.length) : name;
        return this.caseInsensitiveCommands
          ? base.toLowerCase().startsWith(nameToken.toLowerCase())
          : base.startsWith(nameToken);
      });
    }
  
    context() {
      return {
        terminal: this,
        fs: this.fs,
        user: this.user,
        groups: this.groups,
        hostname: this.hostname,
        cwd: this.cwd,
        home: this.home,
        env: this.envSnapshot(),
      };
    }
  
    envSnapshot() {
      return {
        ...this.env,
        USER: this.user,
        LOGNAME: this.user,
        HOME: this.home,
        PWD: this.cwd,
        OLDPWD: this.env.OLDPWD || this.cwd,
        '?': String(this.lastStatus),
      };
    }
  
    resolve(path) {
      return this.fs.normalize(path, { cwd: this.cwd, home: this.home });
    }
  
    async execute(line, options = {}) {
      const input = String(line || '').trim();
      if (!input) return ok('');
      if (input.length > this.maxLineLength) {
        const result = fail(`bash: command line too long (${input.length} > ${this.maxLineLength})\n`, 2);
        this.lastStatus = result.status;
        return result;
      }
      this.history.push(input);
      if (this.history.length > this.maxHistory) this.history.splice(0, this.history.length - this.maxHistory);
      let result;
      try {
        result = await this.runControl(input, options);
      } catch (error) {
        result = fail(`bash: ${error.message || String(error)}\n`, 2);
      }
      this.lastStatus = result.status;
      if (this.suppressNextPersist) this.suppressNextPersist = false;
      else this.persist();
      return result;
    }
  
    snapshot() {
      return {
        cwd: this.cwd,
        history: this.history.slice(-this.maxHistory),
        aliases: { ...this.aliases },
        env: this.selectPersistedEnv(),
        lastStatus: this.lastStatus,
        ...(this.persistVfs && this.fs.snapshot ? { vfs: this.fs.snapshot() } : {}),
      };
    }
  
    restore(state = {}) {
      if (!state || typeof state !== 'object') return this;
      if (this.persistVfs && state.vfs && this.fs.restoreSnapshot) {
        this.fs.restoreSnapshot(state.vfs);
      }
      if (typeof state.cwd === 'string') {
        const stat = this.fs.stat(state.cwd);
        if (stat?.type === 'dir' && this.fs.canExecute(state.cwd, { user: this.user, groups: this.groups })) {
          this.cwd = state.cwd;
          this.env.PWD = state.cwd;
        }
      }
      if (Array.isArray(state.history)) {
        this.history = state.history
          .filter(item => typeof item === 'string')
          .map(item => item.slice(0, 2000))
          .slice(-this.maxHistory);
        this.historyIndex = this.history.length;
      }
      if (state.aliases && typeof state.aliases === 'object') {
        Object.entries(state.aliases).forEach(([key, value]) => {
          if (/^[A-Za-z0-9_.-]{1,64}$/.test(key) && typeof value === 'string') {
            this.aliases[key] = value.slice(0, 1000);
          }
        });
      }
      if (state.env && typeof state.env === 'object') {
        Object.entries(state.env).forEach(([key, value]) => {
          if (/^[A-Za-z_][A-Za-z0-9_]{0,63}$/.test(key) && typeof value === 'string') {
            this.env[key] = value.slice(0, 4096);
          }
        });
      }
      this.lastStatus = Number.isInteger(state.lastStatus) ? state.lastStatus : this.lastStatus;
      return this;
    }
  
    captureInitialVfsSnapshot() {
      this.initialVfsSnapshot = this.fs.snapshot ? this.fs.snapshot() : null;
      return this;
    }
  
    persist() {
      if (!this.persistence?.save) return;
      try {
        this.persistence.save(this.snapshot());
      } catch (error) {
        reportDiagnostic(error, { source: 'shell.persist' });
      }
    }
  
    resetSessionState() {
      if (this.persistence?.reset) {
        try {
          this.persistence.reset();
        } catch (error) {
          reportDiagnostic(error, { source: 'shell.resetSessionState' });
        }
      }
      this.cwd = this.home;
      this.env.PWD = this.cwd;
      this.env.OLDPWD = this.cwd;
      this.history = [];
      this.aliases = {};
      this.lastStatus = 0;
      if (this.persistVfs && this.initialVfsSnapshot && this.fs.restoreSnapshot) {
        this.fs.restoreSnapshot(this.initialVfsSnapshot);
      }
      this.suppressNextPersist = true;
      return this;
    }
  
    selectPersistedEnv() {
      if (!this.persistEnv) return {};
      const excluded = new Set(['HOME', 'PWD', 'OLDPWD', 'USER', 'LOGNAME', 'SHELL']);
      const keys = Array.isArray(this.persistEnv) ? this.persistEnv : Object.keys(this.env);
      return keys.reduce((out, key) => {
        if (excluded.has(key)) return out;
        const value = this.env[key];
        if (typeof value === 'string') out[key] = value.slice(0, 4096);
        return out;
      }, {});
    }
  
    async runControl(line, options = {}) {
      const tokens = splitControlOperators(line, { backslashEscapes: this.backslashEscapes });
      let pending = ';';
      let last = ok('');
      let stdout = '';
      let stderr = '';
      let events = [];
      for (const token of tokens) {
        if (token.type === 'op') {
          pending = token.value;
          continue;
        }
        if (pending === '&&' && last.status !== 0) continue;
        if (pending === '||' && last.status === 0) continue;
        last = await this.runRedirect(token.value, options);
        stdout += last.stdout;
        stderr += last.stderr;
        events = events.concat(last.events);
        this.lastStatus = last.status;
      }
      return normalizeResult({ stdout, stderr, status: last.status, events });
    }
  
    async runRedirect(line, options = {}) {
      const redirect = extractRedirect(line, { backslashEscapes: this.backslashEscapes });
      const command = redirect ? redirect.command : line;
      const result = await this.runPipeline(command, options);
      if (!redirect) return result;
      try {
        this.fs.writeFile(redirect.target, result.stdout, {
          append: redirect.append,
          cwd: this.cwd,
          home: this.home,
          user: this.user,
          groups: this.groups,
        });
        return normalizeResult({ stdout: '', stderr: result.stderr, status: result.status, events: result.events });
      } catch (error) {
        return fail(`bash: ${error.message}\n`, 1);
      }
    }
  
    async runPipeline(line, options = {}) {
      const segments = splitTopLevel(line, '|', { backslashEscapes: this.backslashEscapes });
      let stdin = '';
      let input = null;
      let status = 0;
      let stderr = '';
      let events = [];
      for (const segment of segments) {
        const words = await this.parseWordsAsync(segment);
        if (words.length === 0) return fail('bash: syntax error near unexpected token `|`\n', 2);
        const result = await this.runCommand(words, stdin, { ...options, input });
        stdin = result.stdout;
        input = result.data;
        stderr += result.stderr;
        events = events.concat(result.events);
        status = result.status;
        this.lastStatus = status;
      }
      const stdout = stdin || (Array.isArray(input) ? this.formatPipelineData(input, { terminal: this }) : '');
      return normalizeResult({ stdout, stderr, status, events, data: input });
    }
  
    async runCommand(words, stdin = '', options = {}) {
      const localAssignments = {};
      const mutable = [...words];
      while (mutable.length && /^[A-Za-z_][A-Za-z0-9_]*=.*/.test(mutable[0])) {
        const item = mutable.shift();
        const idx = item.indexOf('=');
        localAssignments[item.slice(0, idx)] = item.slice(idx + 1);
      }
      if (mutable.length === 0) {
        Object.assign(this.env, localAssignments);
        return ok('');
      }
  
      let name = mutable.shift();
      const alias = this.alias(name);
      if (alias) {
        const aliasWords = await this.parseWordsAsync(alias);
        name = aliasWords.shift() || name;
        mutable.unshift(...aliasWords);
      }
  
      const args = this.expandGlobs
        ? mutable.flatMap(arg => this.fs.glob(arg, { cwd: this.cwd, home: this.home }))
        : mutable;
      const command = this.command(name);
      if (!command) return fail(`${name}: command not found\n`, 127);
      if (options.signal?.aborted) return fail(`${name}: interrupted\n`, 130);
      const envBackup = { ...this.env };
      Object.assign(this.env, localAssignments);
      try {
        const operation = Promise.resolve().then(() => command.handler({
          name,
          args,
          stdin,
          input: options.input || null,
          signal: options.signal || null,
          ...this.context(),
        }));
        operation.catch(error => reportDiagnostic(error, { source: 'shell.command.operation', code: 'COMMAND_REJECTED' }));
        const result = await this.raceCommandTimeout(name, operation, options.signal);
        return this.limitResult(normalizeResult(result));
      } catch (error) {
        if (error instanceof VfsError) return fail(`${name}: ${error.message}\n`, error.code === 'ENOENT' ? 1 : 1);
        return fail(`${name}: ${error.message || String(error)}\n`, 1);
      } finally {
        Object.keys(localAssignments).forEach(key => {
          if (Object.prototype.hasOwnProperty.call(envBackup, key)) this.env[key] = envBackup[key];
          else delete this.env[key];
        });
      }
    }
  
    async raceCommandTimeout(name, operation, signal = null) {
      if (!this.commandTimeoutMs && !signal) return operation;
      if (signal?.aborted) return fail(`${name}: interrupted\n`, 130);
      let timer = null;
      let cleanupAbort = null;
      try {
        const racers = [operation];
        if (this.commandTimeoutMs) {
          racers.push(new Promise(resolve => {
            timer = setTimeout(() => {
              resolve(fail(`${name}: command timed out after ${this.commandTimeoutMs}ms\n`, 124));
            }, this.commandTimeoutMs);
          }));
        }
        if (signal) {
          racers.push(new Promise(resolve => {
            const onAbort = () => resolve(fail(`${name}: interrupted\n`, 130));
            signal.addEventListener('abort', onAbort, { once: true });
            cleanupAbort = () => signal.removeEventListener('abort', onAbort);
          }));
        }
        return await Promise.race(racers);
      } finally {
        if (timer) clearTimeout(timer);
        if (cleanupAbort) cleanupAbort();
      }
    }
  
    limitResult(result) {
      const limited = { ...result };
      let truncated = false;
      if (textByteLength(limited.stdout) > this.maxOutputBytes) {
        limited.stdout = truncateBytes(limited.stdout, this.maxOutputBytes) + '\n[output truncated]\n';
        truncated = true;
      }
      if (textByteLength(limited.stderr) > this.maxOutputBytes) {
        limited.stderr = truncateBytes(limited.stderr, this.maxOutputBytes) + '\n[stderr truncated]\n';
        truncated = true;
      }
      if (truncated && limited.status === 0) limited.status = 0;
      return limited;
    }
  
    parseWords(input) {
      return this.parseWordParts(input).map(parts => (
        parts.map(part => part.expand ? this.expandShellFragment(part.text) : part.text).join('')
      ));
    }
  
    async parseWordsAsync(input) {
      const words = [];
      for (const parts of this.parseWordParts(input)) {
        const expanded = [];
        for (const part of parts) {
          expanded.push(part.expand ? await this.expandShellFragmentAsync(part.text) : part.text);
        }
        words.push(expanded.join(''));
      }
      return words;
    }
  
    parseWordParts(input) {
      const words = [];
      let parts = [];
      let buf = '';
      let quote = null;
      let bufExpand = true;
      let started = false;
      let escaped = false;
      let escapeQuote = null;
      let commandDepth = 0;
      const flushPart = () => {
        if (buf.length > 0) parts.push({ text: buf, expand: bufExpand });
        buf = '';
      };
      const pushWord = () => {
        flushPart();
        if (started) words.push(parts);
        parts = [];
        started = false;
        bufExpand = true;
      };
      const text = String(input);
      for (let i = 0; i < text.length; i++) {
        const ch = text[i];
        if (escaped) {
          started = true;
          if (escapeQuote === '"' && !['$', '`', '"', '\\', '\n'].includes(ch)) buf += '\\';
          buf += ch;
          escaped = false;
          escapeQuote = null;
          continue;
        }
        if (commandDepth > 0) {
          started = true;
          if (ch === '$' && text[i + 1] === '(') {
            buf += '$(';
            commandDepth++;
            i++;
            continue;
          }
          if (ch === '(') commandDepth++;
          else if (ch === ')') commandDepth--;
          buf += ch;
          continue;
        }
        if (this.backslashEscapes && ch === '\\' && quote !== "'") {
          started = true;
          escaped = true;
          escapeQuote = quote;
          continue;
        }
        if (quote) {
          if (ch === quote) {
            flushPart();
            quote = null;
            bufExpand = true;
            continue;
          }
          started = true;
          const canExpand = quote !== "'";
          if (bufExpand !== canExpand) {
            flushPart();
            bufExpand = canExpand;
          }
          buf += ch;
          continue;
        }
        if (ch === "'" || ch === '"') {
          started = true;
          flushPart();
          quote = ch;
          bufExpand = ch !== "'";
          continue;
        }
        if (ch === '$' && text[i + 1] === '(') {
          started = true;
          if (!bufExpand) {
            flushPart();
            bufExpand = true;
          }
          buf += '$(';
          commandDepth++;
          i++;
          continue;
        }
        if (/\s/.test(ch)) {
          pushWord();
          continue;
        }
        started = true;
        if (!bufExpand) {
          flushPart();
          bufExpand = true;
        }
        buf += ch;
      }
      if (escaped) buf += '\\';
      if (quote) throw new Error(`unexpected EOF while looking for matching ${quote}`);
      if (commandDepth > 0) throw new Error('unexpected EOF while looking for matching )');
      pushWord();
      return words;
    }
  
    expandShellFragment(text) {
      return this.expandVariables(this.expandCommandSubstitutions(text));
    }
  
    async expandShellFragmentAsync(text) {
      return this.expandVariables(await this.expandCommandSubstitutionsAsync(text));
    }
  
    expandVariables(text) {
      const env = this.envSnapshot();
      return String(text).replace(/\$\{([A-Za-z_][A-Za-z0-9_]*)\}|\$([A-Za-z_][A-Za-z0-9_]*|\?)/g, (_, braced, plain) => {
        const key = braced || plain;
        return Object.prototype.hasOwnProperty.call(env, key) ? env[key] : '';
      });
    }
  
    expandCommandSubstitutions(text) {
      return String(text);
    }
  
    async expandCommandSubstitutionsAsync(text) {
      let output = '';
      let i = 0;
      const input = String(text);
      while (i < input.length) {
        if (input[i] === '$' && input[i + 1] === '(') {
          let depth = 1;
          let j = i + 2;
          while (j < input.length && depth > 0) {
            if (input[j] === '(') depth++;
            else if (input[j] === ')') depth--;
            j++;
          }
          if (depth === 0) {
            const inner = input.slice(i + 2, j - 1).trim();
            if (inner && inner.length <= this.maxCommandSubstitutionLength) {
              const result = await this.runPipeline(inner);
              output += result.stdout.trim().replace(/\s*\n\s*/g, ' ');
            }
            i = j;
            continue;
          }
        }
        output += input[i++];
      }
      return output;
    }
  }
  
  function splitTopLevel(input, delimiter, options = {}) {
    const backslashEscapes = options.backslashEscapes !== false;
    const chunks = [];
    let current = '';
    let quote = null;
    let escaped = false;
    let commandDepth = 0;
    const text = String(input);
    for (let i = 0; i < text.length; i++) {
      const ch = text[i];
      if (escaped) {
        current += ch;
        escaped = false;
        continue;
      }
      if (backslashEscapes && ch === '\\' && quote !== "'") {
        current += ch;
        escaped = true;
        continue;
      }
      if (quote) {
        current += ch;
        if (ch === quote) quote = null;
        continue;
      }
      if (ch === "'" || ch === '"') {
        quote = ch;
        current += ch;
        continue;
      }
      if (ch === '$' && text[i + 1] === '(') {
        current += '$(';
        commandDepth++;
        i++;
        continue;
      }
      if (commandDepth > 0) {
        if (ch === '(') commandDepth++;
        else if (ch === ')') commandDepth--;
        current += ch;
        continue;
      }
      if (ch === delimiter) {
        chunks.push(current.trim());
        current = '';
        continue;
      }
      current += ch;
    }
    chunks.push(current.trim());
    return chunks.filter(Boolean);
  }
  
  function splitControlOperators(input, options = {}) {
    const backslashEscapes = options.backslashEscapes !== false;
    const tokens = [];
    let current = '';
    let quote = null;
    let escaped = false;
    let commandDepth = 0;
    const pushCommand = () => {
      const value = current.trim();
      if (value) tokens.push({ type: 'cmd', value });
      current = '';
    };
    for (let i = 0; i < input.length; i++) {
      const ch = input[i];
      if (escaped) {
        current += ch;
        escaped = false;
        continue;
      }
      if (backslashEscapes && ch === '\\' && quote !== "'") {
        current += ch;
        escaped = true;
        continue;
      }
      if (quote) {
        current += ch;
        if (ch === quote) quote = null;
        continue;
      }
      if (ch === "'" || ch === '"') {
        quote = ch;
        current += ch;
        continue;
      }
      if (ch === '$' && input[i + 1] === '(') {
        current += '$(';
        commandDepth++;
        i++;
        continue;
      }
      if (commandDepth > 0) {
        if (ch === '(') commandDepth++;
        else if (ch === ')') commandDepth--;
        current += ch;
        continue;
      }
      const two = input.slice(i, i + 2);
      if (two === '&&' || two === '||') {
        pushCommand();
        tokens.push({ type: 'op', value: two });
        i++;
        continue;
      }
      if (ch === ';') {
        pushCommand();
        tokens.push({ type: 'op', value: ';' });
        continue;
      }
      current += ch;
    }
    pushCommand();
    return tokens;
  }
  
  function extractRedirect(commandLine, options = {}) {
    const backslashEscapes = options.backslashEscapes !== false;
    let quote = null;
    let escaped = false;
    let commandDepth = 0;
    for (let i = 0; i < commandLine.length; i++) {
      const ch = commandLine[i];
      if (escaped) {
        escaped = false;
        continue;
      }
      if (backslashEscapes && ch === '\\' && quote !== "'") {
        escaped = true;
        continue;
      }
      if (quote) {
        if (ch === quote) quote = null;
        continue;
      }
      if (ch === "'" || ch === '"') {
        quote = ch;
        continue;
      }
      if (ch === '$' && commandLine[i + 1] === '(') {
        commandDepth++;
        i++;
        continue;
      }
      if (commandDepth > 0) {
        if (ch === '(') commandDepth++;
        else if (ch === ')') commandDepth--;
        continue;
      }
      if (ch === '>') {
        const append = commandLine[i + 1] === '>';
        const opLength = append ? 2 : 1;
        return {
          command: commandLine.slice(0, i).trim(),
          target: commandLine.slice(i + opLength).trim(),
          append,
        };
      }
    }
    return null;
  }
  
  function truncateBytes(value, maxBytes) {
    let text = String(value ?? '');
    if (textByteLength(text) <= maxBytes) return text;
    let low = 0;
    let high = text.length;
    while (low < high) {
      const mid = Math.ceil((low + high) / 2);
      if (textByteLength(text.slice(0, mid)) <= maxBytes) low = mid;
      else high = mid - 1;
    }
    return text.slice(0, low).replace(/\s+$/, '');
  }
  
  function defaultPipelineFormatter(data) {
    if (!Array.isArray(data) || data.length === 0) return '';
    if (data.every(item => typeof item !== 'object' || item === null)) {
      return data.map(item => String(item)).join('\n') + '\n';
    }
    return data.map(item => JSON.stringify(item)).join('\n') + '\n';
  }
  
  __exports["splitTopLevel"] = splitTopLevel;
  __exports["splitControlOperators"] = splitControlOperators;
  __exports["extractRedirect"] = extractRedirect;
  __exports["TerminalCore"] = TerminalCore;
  
};
__modules["src/result.mjs"] = (__exports, __import) => {
  function ok(stdout = '', extra = {}) {
    return normalizeResult({ status: 0, stdout, ...extra });
  }
  
  function fail(stderr = '', status = 1, extra = {}) {
    return normalizeResult({ status, stderr, ...extra });
  }
  
  function normalizeResult(result = {}) {
    const data = Array.isArray(result.data) ? result.data : null;
    return {
      stdout: result.stdout == null ? '' : String(result.stdout),
      stderr: result.stderr == null ? '' : String(result.stderr),
      status: Number.isInteger(result.status) ? result.status : 0,
      events: Array.isArray(result.events) ? result.events : [],
      data,
    };
  }
  
  const textEncoder = new TextEncoder();
  
  function textByteLength(value) {
    return textEncoder.encode(String(value ?? '')).length;
  }
  
  __exports["ok"] = ok;
  __exports["fail"] = fail;
  __exports["normalizeResult"] = normalizeResult;
  __exports["textByteLength"] = textByteLength;
  
};
__modules["src/vfs.mjs"] = (__exports, __import) => {
  const { textByteLength } = __import("src/result.mjs");
  const DEFAULT_DATE = 'Jan  1 00:00';
  
  class MemoryFileSystem {
    constructor({ now = () => new Date(), clockText = DEFAULT_DATE } = {}) {
      this.nodes = new Map();
      this.now = now;
      this.clockText = clockText;
      this.addDir('/', { owner: 'root', group: 'root', perm: 'drwxr-xr-x' });
    }
  
    normalize(target = '.', { cwd = '/', home = '/home/guest' } = {}) {
      let input = String(target || '.');
      if (input === '~') input = home;
      else if (input.startsWith('~/')) input = home + input.slice(1);
      else if (!input.startsWith('/')) input = cwd === '/' ? `/${input}` : `${cwd}/${input}`;
  
      const parts = [];
      input.split('/').forEach(part => {
        if (!part || part === '.') return;
        if (part === '..') parts.pop();
        else parts.push(part);
      });
      return '/' + parts.join('/');
    }
  
    dirname(path) {
      const normalized = this.normalize(path);
      if (normalized === '/') return '/';
      return normalized.split('/').slice(0, -1).join('/') || '/';
    }
  
    basename(path) {
      const normalized = this.normalize(path);
      if (normalized === '/') return '/';
      return normalized.split('/').pop();
    }
  
    has(path) {
      return this.nodes.has(this.normalize(path));
    }
  
    get(path) {
      return this.nodes.get(this.normalize(path));
    }
  
    stat(path) {
      const normalized = this.normalize(path);
      const node = this.nodes.get(normalized);
      return node ? { path: normalized, ...node } : null;
    }
  
    addDir(path, meta = {}) {
      const normalized = this.normalize(path);
      this.nodes.set(normalized, {
        type: 'dir',
        owner: meta.owner || meta.user || 'root',
        user: meta.user || meta.owner || 'root',
        group: meta.group || meta.owner || meta.user || 'root',
        perm: meta.perm || 'drwxr-xr-x',
        date: meta.date || this.clockText,
        size: meta.size || 4096,
        meta: meta.meta || {},
      });
      return normalized;
    }
  
    ensureDir(path, meta = {}) {
      let current = '';
      this.normalize(path).split('/').filter(Boolean).forEach(part => {
        current += `/${part}`;
        if (!this.nodes.has(current)) this.addDir(current, meta);
      });
    }
  
    makeDir(path, { cwd = '/', home = '/home/guest', user = 'guest', groups = [], parents = false, meta = {} } = {}) {
      const normalized = this.normalize(path, { cwd, home });
      if (this.nodes.has(normalized)) throw new VfsError(`${path}: File exists`, 'EEXIST');
      const parent = this.dirname(normalized);
      const parentNode = this.nodes.get(parent);
      if (!parentNode || parentNode.type !== 'dir') {
        if (!parents) throw new VfsError(`${path}: No such file or directory`, 'ENOENT');
        this.makeDir(parent, { cwd: '/', home, user, groups, parents: true, meta });
      }
      if (!this.canWrite(parent, { user, groups })) throw new VfsError(`${path}: Permission denied`, 'EACCES');
      return this.addDir(normalized, {
        owner: user,
        user,
        group: groups[0] || user,
        perm: 'drwxr-xr-x',
        ...meta,
      });
    }
  
    addFile(path, content = '', meta = {}) {
      const normalized = this.normalize(path);
      const parent = this.dirname(normalized);
      this.ensureDir(parent);
      const text = String(content ?? '');
      this.nodes.set(normalized, {
        type: meta.type || 'file',
        owner: meta.owner || meta.user || 'root',
        user: meta.user || meta.owner || 'root',
        group: meta.group || meta.owner || meta.user || 'root',
        perm: meta.perm || '-rw-r--r--',
        date: meta.date || this.clockText,
        size: meta.size ?? textByteLength(text),
        content: text,
        link: meta.link,
        title: meta.title,
        meta: meta.meta || {},
      });
      return normalized;
    }
  
    addExecutable(path, handler, meta = {}) {
      const normalized = this.addFile(path, meta.content || '', {
        ...meta,
        type: 'exec',
        perm: meta.perm || '-rwxr-xr-x',
      });
      this.nodes.get(normalized).handler = handler;
      return normalized;
    }
  
    list(path, { all = false, cwd = '/', home = '/home/guest' } = {}) {
      const dir = this.normalize(path, { cwd, home });
      const node = this.nodes.get(dir);
      if (!node) throw new VfsError(`${path}: No such file or directory`, 'ENOENT');
      if (node.type !== 'dir') return [this.basename(dir)];
      const prefix = dir === '/' ? '/' : `${dir}/`;
      const names = [];
      for (const key of this.nodes.keys()) {
        if (key === dir || !key.startsWith(prefix)) continue;
        const relative = key.slice(prefix.length);
        if (!relative.includes('/') && (all || !relative.startsWith('.'))) names.push(relative);
      }
      return names.sort();
    }
  
    readFile(path, context = {}) {
      const normalized = this.normalize(path, context);
      const node = this.nodes.get(normalized);
      if (!node) throw new VfsError(`${path}: No such file or directory`, 'ENOENT');
      if (node.type === 'dir') throw new VfsError(`${path}: Is a directory`, 'EISDIR');
      if (!this.canRead(normalized, context)) throw new VfsError(`${path}: Permission denied`, 'EACCES');
      return String(node.content || '');
    }
  
    writeFile(path, content, { append = false, cwd = '/', home = '/home/guest', user = 'guest', groups = [] } = {}) {
      const normalized = this.normalize(path, { cwd, home });
      if (normalized === '/dev/null') return;
      const parent = this.dirname(normalized);
      const parentNode = this.nodes.get(parent);
      const existing = this.nodes.get(normalized);
      if (!parentNode || parentNode.type !== 'dir') throw new VfsError(`${normalized}: No such file or directory`, 'ENOENT');
      if (existing && existing.type === 'dir') throw new VfsError(`${normalized}: Is a directory`, 'EISDIR');
      if (!this.canWrite(parent, { user, groups }) || (existing && !this.canWrite(normalized, { user, groups }))) {
        throw new VfsError(`${normalized}: Permission denied`, 'EACCES');
      }
      const text = append && existing ? String(existing.content || '') + String(content ?? '') : String(content ?? '');
      this.nodes.set(normalized, {
        ...(existing || {}),
        type: 'file',
        owner: existing?.owner || user,
        user: existing?.user || user,
        group: existing?.group || user,
        perm: existing?.perm || '-rw-r--r--',
        date: this.clockText,
        size: textByteLength(text),
        content: text,
        meta: existing?.meta || {},
      });
    }
  
    remove(path, context = {}) {
      const normalized = this.normalize(path, context);
      const node = this.nodes.get(normalized);
      if (!node) {
        if (context.force) return;
        throw new VfsError(`${path}: No such file or directory`, 'ENOENT');
      }
      if (normalized === '/') throw new VfsError('refusing to remove /', 'EACCES');
      if (!this.canWrite(this.dirname(normalized), context)) throw new VfsError(`${path}: Permission denied`, 'EACCES');
      const children = node.type === 'dir' ? [...this.nodes.keys()].filter(key => key.startsWith(`${normalized}/`)) : [];
      if (children.length > 0 && !context.recursive) {
        throw new VfsError(`${path}: Directory not empty`, 'ENOTEMPTY');
      }
      children.sort((a, b) => b.length - a.length).forEach(key => this.nodes.delete(key));
      this.nodes.delete(normalized);
    }
  
    copy(source, target, context = {}) {
      const src = this.normalize(source, context);
      let dest = this.normalize(target, context);
      const node = this.nodes.get(src);
      if (!node) throw new VfsError(`${source}: No such file or directory`, 'ENOENT');
      if (!this.canRead(src, context)) throw new VfsError(`${source}: Permission denied`, 'EACCES');
      const destNode = this.nodes.get(dest);
      if (destNode?.type === 'dir') dest = dest === '/' ? `/${this.basename(src)}` : `${dest}/${this.basename(src)}`;
      if (node.type === 'dir') {
        if (!context.recursive) throw new VfsError(`${source}: omitting directory`, 'EISDIR');
        this.makeDir(dest, { ...context, parents: true, meta: copyMeta(node) });
        const prefix = `${src}/`;
        [...this.nodes.keys()].filter(key => key.startsWith(prefix)).sort().forEach(key => {
          const child = this.nodes.get(key);
          const childDest = `${dest}/${key.slice(prefix.length)}`;
          if (child.type === 'dir') this.makeDir(childDest, { ...context, parents: true, meta: copyMeta(child) });
          else this.addFile(childDest, child.content || '', copyMeta(child));
        });
        return dest;
      }
      this.writeFile(dest, node.content || '', context);
      const written = this.nodes.get(dest);
      Object.assign(written, copyMeta(node), {
        owner: context.user || written.owner,
        user: context.user || written.user,
        group: context.groups?.[0] || context.user || written.group,
      });
      return dest;
    }
  
    move(source, target, context = {}) {
      const dest = this.copy(source, target, { ...context, recursive: true });
      this.remove(source, { ...context, recursive: true });
      return dest;
    }
  
    chmod(path, mode, context = {}) {
      const normalized = this.normalize(path, context);
      const node = this.nodes.get(normalized);
      if (!node) throw new VfsError(`${path}: No such file or directory`, 'ENOENT');
      if ((context.user || 'guest') !== 'root' && (node.owner || node.user) !== context.user) {
        throw new VfsError(`${path}: Operation not permitted`, 'EPERM');
      }
      node.perm = normalizeMode(mode, node.type === 'dir' ? 'd' : (node.perm || '-')[0]);
    }
  
    chown(path, owner, group, context = {}) {
      if ((context.user || 'guest') !== 'root') throw new VfsError(`${path}: Operation not permitted`, 'EPERM');
      const normalized = this.normalize(path, context);
      const node = this.nodes.get(normalized);
      if (!node) throw new VfsError(`${path}: No such file or directory`, 'ENOENT');
      if (owner) {
        node.owner = owner;
        node.user = owner;
      }
      if (group) node.group = group;
    }
  
    canRead(path, { user = 'guest', groups = [] } = {}) {
      return this.checkBit(path, user, groups, 'r');
    }
  
    canWrite(path, { user = 'guest', groups = [] } = {}) {
      return this.checkBit(path, user, groups, 'w');
    }
  
    canExecute(path, { user = 'guest', groups = [] } = {}) {
      return this.checkBit(path, user, groups, 'x');
    }
  
    checkBit(path, user, groups, bit) {
      const node = this.nodes.get(this.normalize(path));
      if (!node) return false;
      if (user === 'root') return true;
      const perm = node.perm || '----------';
      const owner = node.owner || node.user || 'root';
      const group = node.group || owner;
      const offset = owner === user ? 1 : (groups.includes(group) ? 4 : 7);
      const value = perm[offset + { r: 0, w: 1, x: 2 }[bit]];
      if (bit === 'x') return value === 'x' || value === 's' || value === 't';
      return value === bit;
    }
  
    glob(pattern, context = {}) {
      if (!/[?*]/.test(pattern) || pattern.startsWith('-')) return [pattern];
      const slash = pattern.lastIndexOf('/');
      const dirToken = slash >= 0 ? pattern.slice(0, slash + 1) : '';
      const namePattern = slash >= 0 ? pattern.slice(slash + 1) : pattern;
      const dir = dirToken ? this.normalize(dirToken.replace(/\/$/, '') || '/', context) : (context.cwd || '/');
      const regex = new RegExp('^' + escapeRegExp(namePattern).replace(/\\\*/g, '.*').replace(/\\\?/g, '.') + '$');
      const matches = this.list(dir, { all: true, ...context }).filter(name => regex.test(name));
      return matches.length ? matches.map(name => dirToken + name) : [pattern];
    }
  
    snapshot() {
      return {
        nodes: [...this.nodes.entries()]
          .filter(([, node]) => typeof node.handler !== 'function')
          .map(([path, node]) => [path, serializeNode(node)]),
      };
    }
  
    restoreSnapshot(state = {}) {
      if (!state || !Array.isArray(state.nodes)) return this;
      const restored = new Map();
      for (const item of state.nodes) {
        if (!Array.isArray(item) || item.length !== 2) continue;
        const [path, node] = item;
        if (typeof path !== 'string' || !path.startsWith('/') || !node || typeof node !== 'object') continue;
        restored.set(this.normalize(path), sanitizeNode(node));
      }
      if (!restored.has('/')) {
        restored.set('/', {
          type: 'dir',
          owner: 'root',
          user: 'root',
          group: 'root',
          perm: 'drwxr-xr-x',
          date: this.clockText,
          size: 4096,
          meta: {},
        });
      }
      this.nodes = restored;
      return this;
    }
  }
  
  class VfsError extends Error {
    constructor(message, code = 'EIO') {
      super(message);
      this.name = 'VfsError';
      this.code = code;
    }
  }
  
  function createLinuxLikeFs(options = {}) {
    const fs = new MemoryFileSystem(options);
    fs.addDir('/home', { owner: 'root', group: 'root' });
    fs.addDir('/home/guest', { owner: 'guest', group: 'guest' });
    fs.addFile('/home/guest/README.txt', 'Welcome to the virtual terminal.\n', { owner: 'guest', group: 'guest' });
    fs.addDir('/tmp', { owner: 'root', group: 'root', perm: 'drwxrwxrwt' });
    fs.addDir('/root', { owner: 'root', group: 'root', perm: 'drwx------' });
    fs.addDir('/bin');
    fs.addDir('/usr/bin');
    fs.addDir('/etc');
    fs.addDir('/var');
    fs.addDir('/var/log');
    fs.addDir('/proc', { perm: 'dr-xr-xr-x' });
    fs.addDir('/dev');
    fs.addFile('/etc/hostname', 'blog-server\n');
    fs.addFile('/etc/os-release', 'NAME="BlogTerminalOS"\nVERSION_ID="1.0"\nPRETTY_NAME="Blog Terminal Sandbox"\n');
    fs.addFile('/etc/passwd', 'root:x:0:0:root:/root:/bin/bash\nguest:x:1000:1000:Guest:/home/guest:/bin/bash\n');
    fs.addFile('/etc/shadow', 'root:*:19000:0:99999:7:::\n', { perm: '-r--------', owner: 'root', group: 'shadow' });
    fs.addFile('/proc/version', 'Linux version 6.8.0-blog-terminal (browser@localhost) #1 SMP PREEMPT_DYNAMIC\n');
    fs.addFile('/proc/meminfo', 'MemTotal:        8192000 kB\nMemFree:         4219000 kB\nMemAvailable:   6124000 kB\n');
    fs.addFile('/dev/null', '', { perm: 'crw-rw-rw-' });
    fs.addFile('/dev/zero', '\0'.repeat(1024), { perm: 'crw-rw-rw-' });
    fs.addFile('/dev/random', '', { perm: 'crw-rw-rw-' });
    return fs;
  }
  
  function escapeRegExp(value) {
    return String(value).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  }
  
  function copyMeta(node) {
    return {
      type: node.type === 'exec' ? 'exec' : 'file',
      owner: node.owner || node.user || 'root',
      user: node.user || node.owner || 'root',
      group: node.group || node.owner || node.user || 'root',
      perm: node.perm,
      date: node.date,
      size: node.size,
      link: node.link,
      title: node.title,
      meta: { ...(node.meta || {}) },
    };
  }
  
  function serializeNode(node) {
    const {
      handler: _handler,
      ...rest
    } = node;
    return {
      ...rest,
      meta: { ...(rest.meta || {}) },
    };
  }
  
  function sanitizeNode(node) {
    const type = typeof node.type === 'string' ? node.type : 'file';
    const content = node.content == null ? undefined : String(node.content);
    return {
      type,
      owner: typeof node.owner === 'string' ? node.owner : (typeof node.user === 'string' ? node.user : 'root'),
      user: typeof node.user === 'string' ? node.user : (typeof node.owner === 'string' ? node.owner : 'root'),
      group: typeof node.group === 'string' ? node.group : 'root',
      perm: typeof node.perm === 'string' ? node.perm : (type === 'dir' ? 'drwxr-xr-x' : '-rw-r--r--'),
      date: typeof node.date === 'string' ? node.date : DEFAULT_DATE,
      size: Number.isFinite(node.size) ? node.size : textByteLength(content || ''),
      ...(content == null ? {} : { content }),
      ...(typeof node.link === 'string' ? { link: node.link } : {}),
      ...(typeof node.title === 'string' ? { title: node.title } : {}),
      meta: node.meta && typeof node.meta === 'object' && !Array.isArray(node.meta) ? { ...node.meta } : {},
    };
  }
  
  function normalizeMode(mode, typeChar = '-') {
    const value = String(mode || '').trim();
    if (/^[0-7]{3,4}$/.test(value)) {
      const digits = value.slice(-3).split('').map(Number);
      const bits = digits.map(digit => (
        `${digit & 4 ? 'r' : '-'}${digit & 2 ? 'w' : '-'}${digit & 1 ? 'x' : '-'}`
      )).join('');
      return `${typeChar}${bits}`;
    }
    if (/^[bcdlps-][rwxstST-]{9}$/.test(value)) return value;
    throw new VfsError(`${mode}: invalid mode`, 'EINVAL');
  }
  
  __exports["createLinuxLikeFs"] = createLinuxLikeFs;
  __exports["MemoryFileSystem"] = MemoryFileSystem;
  __exports["VfsError"] = VfsError;
  
};
__modules["src/diagnostics.mjs"] = (__exports, __import) => {
  const diagnosticListeners = new Set();
  
  function onDiagnostic(listener) {
    if (typeof listener !== 'function') return () => {};
    diagnosticListeners.add(listener);
    return () => diagnosticListeners.delete(listener);
  }
  
  function reportDiagnostic(error, context = {}) {
    if (!diagnosticListeners.size) return;
    const event = normalizeDiagnostic(error, context);
    diagnosticListeners.forEach(listener => {
      try {
        listener(event);
      } catch (error) {
        // Diagnostics must never change terminal behavior.
        void error;
      }
    });
  }
  
  function withDiagnostic(fn, context = {}) {
    try {
      return fn();
    } catch (error) {
      reportDiagnostic(error, context);
      return undefined;
    }
  }
  
  function normalizeDiagnostic(error, context) {
    return {
      type: 'diagnostic',
      level: context.level || 'debug',
      source: context.source || 'termlet',
      code: context.code || 'TERMLET_DIAGNOSTIC',
      message: error?.message || String(error),
      error,
    };
  }
  
  __exports["onDiagnostic"] = onDiagnostic;
  __exports["reportDiagnostic"] = reportDiagnostic;
  __exports["withDiagnostic"] = withDiagnostic;
  
};
__modules["src/extension.mjs"] = (__exports, __import) => {
  function defineCommandPack(nameOrOptions, install, meta = {}) {
    const options = typeof nameOrOptions === 'object'
      ? nameOrOptions
      : { name: nameOrOptions, install, meta };
    if (typeof options.install !== 'function') {
      throw new TypeError('command pack requires an install(terminal, options) function');
    }
    return {
      name: String(options.name || 'command-pack'),
      meta: { ...(options.meta || {}) },
      install: options.install,
    };
  }
  
  function defineExtension(options = {}) {
    if (!options || typeof options !== 'object' || Array.isArray(options)) {
      throw new TypeError('extension definition must be an object');
    }
    return {
      name: String(options.name || '').trim(),
      requires: Array.isArray(options.requires) ? options.requires.map(String) : [],
      capabilities: normalizeCapabilityDeclarations(options.capabilities),
      contributions: normalizeContributionMap(options.contributions),
      activate: typeof options.activate === 'function' ? options.activate : null,
      meta: { ...(options.meta || {}) },
    };
  }
  
  function validateExtension(extension) {
    const diagnostics = [];
    if (!extension || typeof extension !== 'object') {
      diagnostics.push({ type: 'extension.invalid', message: 'extension must be an object' });
    } else {
      if (!/^[A-Za-z0-9_.-]{1,120}$/.test(String(extension.name || ''))) {
        diagnostics.push({ type: 'extension.invalid', message: 'extension name is required' });
      }
      if (extension.contributions != null && (typeof extension.contributions !== 'object' || Array.isArray(extension.contributions))) {
        diagnostics.push({ type: 'contribution.invalid', owner: extension.name || '', message: 'contributions must be an object' });
      }
    }
    return { ok: diagnostics.length === 0, diagnostics };
  }
  
  function composeExtensions(extensions = [], options = {}) {
    const capabilities = options.capabilities || {};
    const diagnostics = [];
    const seenNames = new Set();
    const activeExtensions = [];
    const skipped = new Set();
  
    for (const raw of extensions.flat(Infinity).filter(Boolean)) {
      const extension = isExtensionShape(raw) ? raw : defineExtension(raw);
      const validation = validateExtension(extension);
      if (!validation.ok) {
        diagnostics.push(...validation.diagnostics);
        continue;
      }
      if (seenNames.has(extension.name)) {
        diagnostics.push({ type: 'extension.duplicate', name: extension.name });
        continue;
      }
      seenNames.add(extension.name);
      const capabilityCheck = checkCapabilities(extension, capabilities);
      diagnostics.push(...capabilityCheck.diagnostics);
      if (!capabilityCheck.ok) {
        skipped.add(extension.name);
        continue;
      }
      activeExtensions.push(extension);
    }
  
    const contributions = composeContributions(activeExtensions, diagnostics);
    const disposers = [];
  
    return {
      activeExtensions: activeExtensions.map(extension => extension.name),
      skippedExtensions: [...skipped],
      contributions,
      diagnostics,
      activate(context = {}) {
        for (const extension of activeExtensions) {
          if (!extension.activate) continue;
          try {
            const activated = extension.activate({
              ...context,
              extension,
              contributions,
              diagnostics,
            });
            if (typeof activated === 'function') disposers.push({ name: extension.name, dispose: activated });
            else if (activated && typeof activated.dispose === 'function') {
              disposers.push({ name: extension.name, dispose: activated.dispose });
            }
          } catch (error) {
            diagnostics.push({
              type: 'extension.activateFailed',
              name: extension.name,
              message: String(error?.message || error),
            });
          }
        }
      },
      dispose() {
        while (disposers.length) {
          const item = disposers.pop();
          try {
            item.dispose();
          } catch (error) {
            diagnostics.push({
              type: 'extension.disposeFailed',
              name: item.name,
              message: String(error?.message || error),
            });
          }
        }
      },
    };
  }
  
  function getExtensionDiagnostics(graph) {
    return Array.isArray(graph?.diagnostics) ? graph.diagnostics.map(item => ({ ...item })) : [];
  }
  
  function defineProfile(nameOrOptions, options = {}) {
    const config = typeof nameOrOptions === 'object'
      ? nameOrOptions
      : { ...options, name: nameOrOptions };
    return {
      name: String(config.name || 'custom'),
      core: { ...(config.core || {}) },
      env: { ...(config.env || {}) },
      aliases: { ...(config.aliases || {}) },
      commandPacks: [...(config.commandPacks || [])],
      plugins: [...(config.plugins || [])],
      formatPipelineData: config.formatPipelineData,
      meta: { ...(config.meta || {}) },
    };
  }
  
  function mergeProfileOptions(options = {}) {
    if (!options.profile) return { ...options };
    const profile = typeof options.profile === 'function' ? options.profile(options) : options.profile;
    const normalized = defineProfile(profile || {});
    const {
      commandPacks = [],
      env = {},
      aliases = {},
      plugins = [],
      profile: _profile,
      ...rest
    } = options;
    return {
      ...normalized.core,
      ...rest,
      profileName: rest.profileName || normalized.name,
      env: {
        ...normalized.env,
        ...env,
      },
      aliases: {
        ...normalized.aliases,
        ...aliases,
      },
      commandPacks: [
        ...normalized.commandPacks,
        ...commandPacks,
      ],
      plugins: [
        ...normalized.plugins,
        ...plugins,
      ],
      formatPipelineData: rest.formatPipelineData || normalized.formatPipelineData || normalized.core.formatPipelineData,
    };
  }
  
  function formatRecords(records, columns = null) {
    if (!Array.isArray(records) || records.length === 0) return '';
    const rows = records.filter(item => item && typeof item === 'object');
    if (rows.length === 0) return records.map(item => String(item)).join('\n') + '\n';
    const selected = columns?.length ? columns : collectColumns(rows);
    const widths = selected.map(column => Math.max(
      column.length,
      ...rows.map(row => String(getRecordValue(row, column) ?? '').length),
    ));
    const line = values => values.map((value, index) => String(value ?? '').padEnd(widths[index])).join('  ').replace(/\s+$/, '');
    return [
      line(selected),
      line(widths.map(width => '-'.repeat(width))),
      ...rows.map(row => line(selected.map(column => getRecordValue(row, column) ?? ''))),
      '',
    ].join('\n');
  }
  
  function projectRecords(records, properties) {
    const selected = normalizeProperties(properties);
    if (!selected.length) return records;
    return records.map(record => selected.reduce((out, property) => {
      out[property] = getRecordValue(record, property);
      return out;
    }, {}));
  }
  
  function sortRecords(records, property, direction = 'asc') {
    const multiplier = String(direction).toLowerCase() === 'desc' ? -1 : 1;
    return [...records].sort((left, right) => {
      const a = getRecordValue(left, property);
      const b = getRecordValue(right, property);
      if (a === b) return 0;
      if (a == null) return 1;
      if (b == null) return -1;
      return String(a).localeCompare(String(b), undefined, { numeric: true }) * multiplier;
    });
  }
  
  function filterRecords(records, property, operator, expected) {
    const op = String(operator || 'eq').replace(/^-/, '').toLowerCase();
    return records.filter(record => compareRecordValue(getRecordValue(record, property), op, expected));
  }
  
  function getRecordValue(record, property) {
    if (!record || typeof record !== 'object') return undefined;
    const key = Object.keys(record).find(item => item.toLowerCase() === String(property || '').toLowerCase());
    return key ? record[key] : undefined;
  }
  
  function normalizeProperties(values) {
    return values
      .flatMap(value => String(value || '').split(','))
      .map(value => value.trim())
      .filter(Boolean);
  }
  
  function collectColumns(rows) {
    const seen = new Set();
    rows.forEach(row => {
      Object.keys(row).forEach(key => {
        if (!seen.has(key)) seen.add(key);
      });
    });
    return [...seen].slice(0, 8);
  }
  
  function compareRecordValue(actual, operator, expected) {
    const left = actual == null ? '' : String(actual);
    const right = String(expected ?? '');
    if (operator === 'eq') return left.toLowerCase() === right.toLowerCase();
    if (operator === 'ne') return left.toLowerCase() !== right.toLowerCase();
    if (operator === 'like') return wildcardToRegExp(right).test(left);
    if (operator === 'notlike') return !wildcardToRegExp(right).test(left);
    const leftNumber = Number(left);
    const rightNumber = Number(right);
    if (Number.isFinite(leftNumber) && Number.isFinite(rightNumber)) {
      if (operator === 'gt') return leftNumber > rightNumber;
      if (operator === 'ge') return leftNumber >= rightNumber;
      if (operator === 'lt') return leftNumber < rightNumber;
      if (operator === 'le') return leftNumber <= rightNumber;
    }
    return false;
  }
  
  function wildcardToRegExp(pattern) {
    const escaped = String(pattern)
      .replace(/[.+^${}()|[\]\\]/g, '\\$&')
      .replace(/\*/g, '.*')
      .replace(/\?/g, '.');
    return new RegExp(`^${escaped}$`, 'i');
  }
  
  function isExtensionShape(value) {
    return Boolean(value && typeof value === 'object' && typeof value.name === 'string'
      && value.contributions && typeof value.contributions === 'object');
  }
  
  function normalizeCapabilityDeclarations(capabilities = {}) {
    if (!capabilities || typeof capabilities !== 'object' || Array.isArray(capabilities)) return {};
    return Object.fromEntries(Object.entries(capabilities).map(([name, config]) => {
      if (config && typeof config === 'object') {
        return [sanitizeContributionName(name), {
          required: Boolean(config.required),
          reason: config.reason == null ? '' : String(config.reason).slice(0, 500),
        }];
      }
      return [sanitizeContributionName(name), { required: Boolean(config), reason: '' }];
    }).filter(([name]) => name));
  }
  
  function normalizeContributionMap(contributions = {}) {
    if (!contributions || typeof contributions !== 'object' || Array.isArray(contributions)) return {};
    return Object.fromEntries(Object.entries(contributions).map(([type, values]) => [
      sanitizeContributionType(type),
      Array.isArray(values) ? values.filter(Boolean) : [values].filter(Boolean),
    ]).filter(([type]) => type));
  }
  
  function checkCapabilities(extension, availableCapabilities) {
    const diagnostics = [];
    let ok = true;
    for (const [name, declaration] of Object.entries(extension.capabilities || {})) {
      const allowed = Boolean(availableCapabilities[name]);
      if (!allowed && declaration.required) {
        ok = false;
        diagnostics.push({
          type: 'extension.capabilityDenied',
          name: extension.name,
          capability: name,
          required: true,
          reason: declaration.reason || '',
        });
      } else if (!allowed) {
        diagnostics.push({
          type: 'extension.partialActivation',
          name: extension.name,
          capability: name,
          required: false,
        });
      }
    }
    return { ok, diagnostics };
  }
  
  function composeContributions(extensions, diagnostics) {
    const out = {};
    for (const extension of extensions) {
      for (const [type, values] of Object.entries(extension.contributions || {})) {
        if (type === 'termlet.command') {
          out[type] = composeCommandContributions(out[type] || [], extension, values, diagnostics);
        } else {
          out[type] = [...(out[type] || []), ...values.map(value => normalizeContributionItem(value, extension.name))];
        }
      }
    }
    Object.keys(out).forEach(type => {
      if (type !== 'termlet.command') out[type] = sortContributionItems(out[type]);
    });
    return out;
  }
  
  function composeCommandContributions(existing, extension, values, diagnostics) {
    const commands = [...existing];
    for (const value of values) {
      const command = normalizeContributionItem(value, extension.name);
      if (!command.name) {
        diagnostics.push({ type: 'contribution.invalid', owner: extension.name, message: 'command name is required' });
        continue;
      }
      const index = commands.findIndex(item => item.name.toLowerCase() === command.name.toLowerCase());
      if (index >= 0 && !command.override) {
        diagnostics.push({
          type: 'extension.conflict',
          contribution: 'termlet.command',
          name: command.name,
          owners: [commands[index].owner, extension.name],
        });
        continue;
      }
      if (index >= 0) commands.splice(index, 1, command);
      else commands.push(command);
    }
    return sortContributionItems(commands);
  }
  
  function normalizeContributionItem(value, owner) {
    if (typeof value === 'function') {
      return { name: value.name || 'anonymous', owner, priority: 0, value };
    }
    const item = value && typeof value === 'object' ? value : {};
    return {
      ...item,
      name: sanitizeContributionName(item.name || item.id || ''),
      owner,
      priority: clampPriority(item.priority),
      override: Boolean(item.override),
    };
  }
  
  function sortContributionItems(values) {
    return [...values].sort((left, right) => (
      right.priority - left.priority
      || String(left.name || '').localeCompare(String(right.name || ''))
      || String(left.owner || '').localeCompare(String(right.owner || ''))
    ));
  }
  
  function clampPriority(value) {
    const number = Number(value || 0);
    if (!Number.isFinite(number)) return 0;
    return Math.max(-1000, Math.min(1000, Math.trunc(number)));
  }
  
  function sanitizeContributionType(value) {
    return String(value || '').replace(/[^A-Za-z0-9_.:-]/g, '').slice(0, 120);
  }
  
  function sanitizeContributionName(value) {
    return String(value || '').replace(/[^A-Za-z0-9_.:-]/g, '').slice(0, 120);
  }
  
  __exports["defineCommandPack"] = defineCommandPack;
  __exports["defineExtension"] = defineExtension;
  __exports["validateExtension"] = validateExtension;
  __exports["composeExtensions"] = composeExtensions;
  __exports["getExtensionDiagnostics"] = getExtensionDiagnostics;
  __exports["defineProfile"] = defineProfile;
  __exports["mergeProfileOptions"] = mergeProfileOptions;
  __exports["formatRecords"] = formatRecords;
  __exports["projectRecords"] = projectRecords;
  __exports["sortRecords"] = sortRecords;
  __exports["filterRecords"] = filterRecords;
  __exports["getRecordValue"] = getRecordValue;
  __exports["normalizeProperties"] = normalizeProperties;
  
};
__modules["src/plugins/basic-commands.mjs"] = (__exports, __import) => {
  const { fail, ok, textByteLength } = __import("src/result.mjs");
  function basicCommandsPlugin(terminal, options = {}) {
    const manuals = {
      ls: 'ls - list directory contents\nusage: ls [-al] [file]\n',
      grep: 'grep - print lines matching a pattern\nusage: grep [-in] pattern [file]\n',
      terminal: 'Blog Terminal Sandbox: pure frontend Linux-like terminal core.\n',
      ...(options.manuals || {}),
    };
  
    terminal
      .register('echo', ({ args }) => ok((args[0] === '-n' ? args.slice(1) : args).join(' ') + (args[0] === '-n' ? '' : '\n')))
      .register('printf', ({ args }) => ok(args.join(' ').replace(/\\n/g, '\n')))
      .register('pwd', ({ terminal }) => ok(`${terminal.cwd}\n`))
      .register('whoami', ({ user }) => ok(`${user}\n`))
      .register('hostname', ({ hostname }) => ok(`${hostname}\n`))
      .register('date', () => ok(`${new Date().toString()}\n`))
      .register('true', () => ok(''))
      .register('false', () => fail('', 1))
      .register('cd', ({ args, terminal, fs, home }) => {
        const target = args[0] === '-' ? terminal.env.OLDPWD : (args[0] || home);
        const path = fs.normalize(target, { cwd: terminal.cwd, home });
        const stat = fs.stat(path);
        if (!stat) return fail(`cd: ${args[0] || target}: No such file or directory\n`);
        if (stat.type !== 'dir') return fail(`cd: ${args[0] || target}: Not a directory\n`);
        terminal.env.OLDPWD = terminal.cwd;
        terminal.cwd = path;
        terminal.env.PWD = path;
        return ok(args[0] === '-' ? `${path}\n` : '');
      })
      .register('export', ({ args, terminal }) => {
        if (args.length === 0) {
          return ok(Object.keys(terminal.envSnapshot()).sort().map(key => `declare -x ${key}="${terminal.envSnapshot()[key]}"`).join('\n') + '\n');
        }
        args.forEach(arg => {
          const match = arg.match(/^([A-Za-z_][A-Za-z0-9_]*)(=(.*))?$/);
          if (match) terminal.env[match[1]] = match[3] ?? '';
        });
        return ok('');
      })
      .register('unset', ({ args, terminal }) => {
        args.forEach(key => delete terminal.env[key]);
        return ok('');
      })
      .register('env', ({ terminal }) => ok(Object.keys(terminal.envSnapshot()).sort().map(key => `${key}=${terminal.envSnapshot()[key]}`).join('\n') + '\n'))
      .register('printenv', ({ args, terminal }) => {
        const env = terminal.envSnapshot();
        if (args[0]) return ok(`${env[args[0]] || ''}\n`);
        return ok(Object.keys(env).sort().map(key => `${key}=${env[key]}`).join('\n') + '\n');
      })
      .register('alias', ({ args, terminal }) => {
        if (args.length === 0) return ok(Object.keys(terminal.aliases).sort().map(key => `alias ${key}='${terminal.aliases[key]}'`).join('\n') + '\n');
        args.forEach(arg => {
          const match = arg.match(/^([A-Za-z0-9_-]+)=(.+)$/);
          if (match) terminal.setAlias(match[1], match[2].replace(/^['"]|['"]$/g, ''));
        });
        return ok('');
      })
      .register('type', ({ args, terminal }) => ok(args.map(name => {
        const alias = terminal.alias(name);
        if (alias) return `${name} is aliased to '${alias}'`;
        if (terminal.command(name)) return `${name} is a shell command`;
        return `bash: type: ${name}: not found`;
      }).join('\n') + '\n'))
      .register('command', ({ args, terminal }) => {
        if (args[0] !== '-v' || !args[1]) return ok('');
        return ok(terminal.command(args[1]) || terminal.alias(args[1]) ? `${args[1]}\n` : '');
      })
      .register('which', ({ args, terminal }) => ok(args.filter(name => terminal.command(name)).map(name => `/bin/${name}`).join('\n') + (args.length ? '\n' : '')))
      .register('compgen', ({ args, terminal }) => ok(args[0] === '-c' ? terminal.commandNames().join('\n') + '\n' : ''))
      .register('man', ({ args }) => {
        const topic = (args[0] || '').toLowerCase();
        if (!topic) return ok('What manual page do you want?\n');
        return ok(manuals[topic] || `No manual entry for ${args[0]}\n`);
      })
      .register('ls', lsCommand)
      .register('ll', ctx => lsCommand({ ...ctx, args: ctx.args.some(a => a.includes('l')) ? ctx.args : ['-l', ...ctx.args] }))
      .register('cat', ({ args, stdin, fs, terminal, home, user, groups }) => ok(readArgsOrStdin(args, stdin, fs, terminal, home, user, groups, 'cat')))
      .register('head', ({ args, stdin, fs, terminal, home, user, groups }) => {
        const { count, rest } = parseCount(args, 10);
        return ok(readArgsOrStdin(rest, stdin, fs, terminal, home, user, groups, 'head').split('\n').slice(0, count).join('\n') + '\n');
      })
      .register('tail', ({ args, stdin, fs, terminal, home, user, groups }) => {
        const { count, rest } = parseCount(args, 10);
        const lines = readArgsOrStdin(rest, stdin, fs, terminal, home, user, groups, 'tail').replace(/\n$/, '').split('\n');
        return ok(lines.slice(Math.max(0, lines.length - count)).join('\n') + '\n');
      })
      .register('grep', grepCommand)
      .register('wc', ({ args, stdin, fs, terminal, home, user, groups }) => {
        const text = readArgsOrStdin(args, stdin, fs, terminal, home, user, groups, 'wc');
        const normalized = text.replace(/\n$/, '');
        const lines = normalized ? normalized.split('\n').length : 0;
        const words = normalized.trim() ? normalized.trim().split(/\s+/).length : 0;
        return ok(`${String(lines).padStart(7)} ${String(words).padStart(7)} ${String(textByteLength(text)).padStart(7)}\n`);
      })
      .register('sort', ({ args, stdin, fs, terminal, home, user, groups }) => {
        let lines = readArgsOrStdin(args.filter(a => a !== '-r'), stdin, fs, terminal, home, user, groups, 'sort').split('\n').filter(Boolean).sort();
        if (args.includes('-r')) lines = lines.reverse();
        return ok(lines.join('\n') + '\n');
      })
      .register('uniq', ({ args, stdin, fs, terminal, home, user, groups }) => {
        const seen = [];
        readArgsOrStdin(args, stdin, fs, terminal, home, user, groups, 'uniq').split('\n').forEach(line => {
          if (line && seen[seen.length - 1] !== line) seen.push(line);
        });
        return ok(seen.join('\n') + '\n');
      })
      .register('find', ({ args, fs, terminal, home }) => {
        let searchPath = terminal.cwd;
        let pattern = '*';
        if (args.length >= 2 && args[0] === '-name') pattern = args[1];
        else if (args.length >= 3 && args[1] === '-name') {
          searchPath = fs.normalize(args[0], { cwd: terminal.cwd, home });
          pattern = args[2];
        }
        const regex = new RegExp('^' + escapeRegExp(pattern).replace(/\\\*/g, '.*').replace(/\\\?/g, '.') + '$');
        const prefix = searchPath === '/' ? '/' : `${searchPath}/`;
        return ok([...fs.nodes.keys()].filter(path => (path === searchPath || path.startsWith(prefix)) && regex.test(path.split('/').pop())).sort().join('\n') + '\n');
      })
      .register('touch', ({ args, fs, terminal, home, user, groups }) => {
        args.forEach(arg => {
          try {
            if (fs.has(fs.normalize(arg, { cwd: terminal.cwd, home }))) return;
            fs.writeFile(arg, '', { cwd: terminal.cwd, home, user, groups });
          } catch (error) {
            throw error;
          }
        });
        return ok('');
      })
      .register('mkdir', ({ args, fs, terminal, home, user, groups }) => {
        const parents = args.includes('-p');
        args.filter(arg => !arg.startsWith('-')).forEach(arg => {
          const normalized = fs.normalize(arg, { cwd: terminal.cwd, home });
          if (fs.has(normalized)) {
            if (!parents) throw new Error(`${arg}: File exists`);
            return;
          }
          fs.makeDir(arg, { cwd: terminal.cwd, home, user, groups, parents });
        });
        return ok('');
      })
      .register('rmdir', ({ args, fs, terminal, home, user, groups }) => {
        args.forEach(arg => fs.remove(arg, { cwd: terminal.cwd, home, user, groups }));
        return ok('');
      })
      .register('rm', ({ args, fs, terminal, home, user, groups }) => {
        const flags = args.filter(arg => arg.startsWith('-')).join('');
        const recursive = /[rR]/.test(flags);
        const force = flags.includes('f');
        if (args.includes('/') || args.includes('--no-preserve-root')) return fail('rm: refusing to remove root directory in browser sandbox\n');
        args.filter(arg => !arg.startsWith('-')).forEach(arg => {
          if (fs.normalize(arg, { cwd: terminal.cwd, home }) === '/') throw new Error('refusing to remove /');
          fs.remove(arg, { cwd: terminal.cwd, home, user, groups, recursive, force });
        });
        return ok('');
      })
      .register('cp', ({ args, fs, terminal, home, user, groups }) => {
        const recursive = args.some(arg => /^-[^-]*[rRa]/.test(arg));
        const files = args.filter(arg => !arg.startsWith('-'));
        if (files.length < 2) return fail('cp: missing file operand\n');
        const target = files.at(-1);
        files.slice(0, -1).forEach(source => fs.copy(source, target, { cwd: terminal.cwd, home, user, groups, recursive }));
        return ok('');
      })
      .register('mv', ({ args, fs, terminal, home, user, groups }) => {
        const files = args.filter(arg => !arg.startsWith('-'));
        if (files.length < 2) return fail('mv: missing file operand\n');
        const target = files.at(-1);
        files.slice(0, -1).forEach(source => fs.move(source, target, { cwd: terminal.cwd, home, user, groups }));
        return ok('');
      })
      .register('chmod', ({ args, fs, terminal, home, user, groups }) => {
        if (args.length < 2) return fail('chmod: missing operand\n');
        args.slice(1).forEach(path => fs.chmod(path, args[0], { cwd: terminal.cwd, home, user, groups }));
        return ok('');
      })
      .register('chown', ({ args, fs, terminal, home, user, groups }) => {
        if (args.length < 2) return fail('chown: missing operand\n');
        const [owner, group] = args[0].split(':');
        args.slice(1).forEach(path => fs.chown(path, owner, group, { cwd: terminal.cwd, home, user, groups }));
        return ok('');
      })
      .register('tee', ({ args, stdin, fs, terminal, home, user, groups }) => {
        const append = args.includes('-a');
        args.filter(arg => !arg.startsWith('-')).forEach(arg => fs.writeFile(arg, stdin, { append, cwd: terminal.cwd, home, user, groups }));
        return ok(stdin);
      })
      .register('basename', ({ args }) => ok(`${(args[0] || '').replace(/\/+$/, '').split('/').pop()}\n`))
      .register('dirname', ({ args }) => {
        const value = (args[0] || '.').replace(/\/+$/, '');
        const idx = value.lastIndexOf('/');
        return ok(`${idx <= 0 ? (value.startsWith('/') ? '/' : '.') : value.slice(0, idx)}\n`);
      })
      .register('realpath', ({ args, fs, terminal, home }) => ok(args.map(arg => fs.normalize(arg, { cwd: terminal.cwd, home })).join('\n') + (args.length ? '\n' : '')))
      .register('test', ({ args, fs, terminal, home }) => ok('', evaluateTest(args, fs, terminal, home) ? {} : { status: 1 }))
      .register('[', ({ args, fs, terminal, home }) => ok('', evaluateTest(args.filter(arg => arg !== ']'), fs, terminal, home) ? {} : { status: 1 }));
  }
  
  function lsCommand({ args, fs, terminal, home, user, groups }) {
    const flags = args.filter(arg => arg.startsWith('-')).join('');
    const all = flags.includes('a');
    const long = flags.includes('l');
    const pathArgs = args.filter(arg => !arg.startsWith('-'));
    const targets = pathArgs.length ? pathArgs : [terminal.cwd];
    const chunks = [];
    const errors = [];
    let status = 0;
    targets.forEach((arg, index) => {
      const target = fs.normalize(arg, { cwd: terminal.cwd, home });
      const stat = fs.stat(target);
      if (!stat) {
        errors.push(`ls: cannot access '${arg || target}': No such file or directory`);
        status = 1;
        return;
      }
      if (!fs.canRead(target, { user, groups })) {
        errors.push(`ls: cannot open directory '${arg || target}': Permission denied`);
        status = 1;
        return;
      }
      const multiple = targets.length > 1;
      if (multiple && stat.type === 'dir') chunks.push(`${arg}:`);
      if (stat.type !== 'dir') {
        chunks.push(fs.basename(target));
      } else {
        const items = fs.list(target, { all, cwd: terminal.cwd, home });
        items.forEach(name => {
          const path = target === '/' ? `/${name}` : `${target}/${name}`;
          const node = fs.stat(path);
          const display = name + (node?.type === 'dir' ? '/' : (node?.type === 'exec' ? '*' : ''));
          chunks.push(long
            ? `${node.perm || '----------'} ${(node.owner || node.user || 'root').padEnd(8)} ${(node.group || 'root').padEnd(8)} ${String(node.size || 0).padStart(8)} ${node.date || ''} ${display}`
            : display);
        });
      }
      if (multiple && index < targets.length - 1) chunks.push('');
    });
    const text = chunks.join('\n').replace(/\n+$/, '');
    const stderr = errors.join('\n');
    return ok(text + (text ? '\n' : ''), {
      status,
      stderr: stderr + (stderr ? '\n' : ''),
    });
  }
  
  function grepCommand({ args, stdin, fs, terminal, home, user, groups }) {
    const opts = args.filter(arg => arg.startsWith('-'));
    const plain = args.filter(arg => !arg.startsWith('-'));
    const pattern = plain.shift();
    if (!pattern) return fail('grep: missing pattern\n');
    const text = plain.length ? readArgsOrStdin(plain, '', fs, terminal, home, user, groups, 'grep') : stdin;
    const needle = opts.includes('-i') ? pattern.toLowerCase() : pattern;
    const lines = text.split('\n').map((line, index) => ({ line, index })).filter(({ line }) => (opts.includes('-i') ? line.toLowerCase() : line).includes(needle));
    return ok(lines.map(({ line, index }) => opts.includes('-n') ? `${index + 1}:${line}` : line).join('\n') + (lines.length ? '\n' : ''), { status: lines.length ? 0 : 1 });
  }
  
  function readArgsOrStdin(args, stdin, fs, terminal, home, user, groups, commandName) {
    const files = args.filter(arg => !arg.startsWith('-'));
    if (!files.length) return stdin || '';
    return files.map(arg => {
      try {
        return fs.readFile(arg, { cwd: terminal.cwd, home, user, groups });
      } catch (error) {
        throw new Error(`${arg}: ${error.message.replace(/^.*?: /, '')}`);
      }
    }).join('\n');
  }
  
  function parseCount(args, fallback) {
    const rest = [];
    let count = fallback;
    for (let i = 0; i < args.length; i++) {
      if (args[i] === '-n') count = parseInt(args[++i], 10) || fallback;
      else if (args[i].startsWith('-n')) count = parseInt(args[i].slice(2), 10) || fallback;
      else rest.push(args[i]);
    }
    return { count, rest };
  }
  
  function evaluateTest(args, fs, terminal, home) {
    if (!args.length) return false;
    if (args[0] === '-f') return fs.stat(fs.normalize(args[1] || '', { cwd: terminal.cwd, home }))?.type === 'file';
    if (args[0] === '-d') return fs.stat(fs.normalize(args[1] || '', { cwd: terminal.cwd, home }))?.type === 'dir';
    if (args.length >= 3 && ['=', '=='].includes(args[1])) return args[0] === args[2];
    if (args.length >= 3 && args[1] === '!=') return args[0] !== args[2];
    return Boolean(args[0]);
  }
  
  function escapeRegExp(value) {
    return String(value).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  }
  
  __exports["basicCommandsPlugin"] = basicCommandsPlugin;
  
};
__modules["src/plugins/system-commands.mjs"] = (__exports, __import) => {
  const { fail, ok, textByteLength } = __import("src/result.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  const DEFAULT_SERVICES = [
    ['nginx.service', 'loaded active running', 'Static blog frontend'],
    ['ssh.service', 'loaded inactive dead', 'OpenSSH server'],
    ['cron.service', 'loaded active running', 'Regular background jobs'],
  ];
  
  function systemCommandsPlugin(terminal, options = {}) {
    const bootTime = options.bootTime || new Date(Date.now() - 58 * 60 * 1000);
    const kernel = options.kernel || '6.8.0-blog-terminal';
    const distro = options.distro || 'BlogTerminalOS';
    const services = options.services || DEFAULT_SERVICES;
  
    terminal
      .register('help', ({ terminal }) => ok(renderHelp(terminal)))
      .register('clear', () => ok('', { events: [{ type: 'clear' }] }))
      .register('reset', () => ok('', { events: [{ type: 'clear' }] }))
      .register('exit', () => ok('logout\n', { events: [{ type: 'exit' }] }))
      .register('session', sessionCommand)
      .register('history', ({ terminal }) => ok(terminal.history.map((entry, index) => `${String(index + 1).padStart(5)}  ${entry}`).join('\n') + '\n'))
      .register('id', ({ user, groups }) => ok(`uid=${uidFor(user)}(${user}) gid=${uidFor(user)}(${user}) groups=${groups.map(group => `${uidFor(group)}(${group})`).join(',')}\n`))
      .register('groups', ({ user, groups }) => ok(`${user} : ${groups.join(' ')}\n`))
      .register('uname', ({ args }) => ok(renderUname(args, kernel)))
      .register('uptime', () => ok(renderUptime(bootTime)))
      .register('who', ({ user }) => ok(`${user.padEnd(8)} pts/0        ${formatLoginTime(bootTime)} (127.0.0.1)\n`))
      .register('w', ({ user }) => ok(` ${new Date().toTimeString().slice(0, 5)} up ${formatDuration(Date.now() - bootTime.getTime())},  1 user,  load average: 0.08, 0.04, 0.01\nUSER     TTY      FROM             LOGIN@   IDLE   JCPU   PCPU WHAT\n${user.padEnd(8)} pts/0    127.0.0.1        ${formatLoginTime(bootTime).slice(7)}    0.00s  0.02s  0.00s blog-terminal\n`))
      .register('df', () => ok('Filesystem      Size  Used Avail Use% Mounted on\nbrowserfs        64M   12M   52M  19% /\ntmpfs            32M     0   32M   0% /tmp\nblog-content     16M  4.2M   12M  27% /home/guest/blog\n'))
      .register('free', () => ok('               total        used        free      shared  buff/cache   available\nMem:         8192000     1673220     4219000       52240     2299780     6124000\nSwap:              0           0           0\n'))
      .register('ps', () => ok('    PID TTY          TIME CMD\n      1 ?        00:00:01 init\n    327 pts/0    00:00:00 bash\n    404 pts/0    00:00:00 blog-terminal\n'))
      .register('top', () => ok('top - browser sandbox: use an adapter effect for an interactive view\nTasks:   3 total,   1 running,   2 sleeping,   0 stopped,   0 zombie\n%Cpu(s):  7.0 us,  2.0 sy, 91.0 id\n'))
      .register('lscpu', () => ok('Architecture:            x86_64\nCPU op-mode(s):        32-bit, 64-bit\nModel name:             Browser Virtual CPU\nCPU(s):                 4\nVirtualization:         frontend-sandbox\n'))
      .register('lsblk', () => ok('NAME   MAJ:MIN RM  SIZE RO TYPE MOUNTPOINTS\nvda    252:0    0   64M  0 disk /\nvda1   252:1    0   64M  0 part /\n'))
      .register('mount', () => ok('browserfs on / type browserfs (rw,nosuid,nodev,noexec,relatime)\ntmpfs on /tmp type tmpfs (rw,nosuid,nodev)\n'))
      .register('ip', ({ args }) => ok(renderIp(args)))
      .register('ifconfig', () => ok(renderIp(['addr'])))
      .register('ss', ({ args }) => ok(renderSockets(args)))
      .register('netstat', ({ args }) => ok(renderSockets(args)))
      .register('ping', ({ args }) => ok(`PING ${args.at(-1) || 'localhost'} (127.0.0.1) 56(84) bytes of data.\n64 bytes from 127.0.0.1: icmp_seq=1 ttl=64 time=0.042 ms\n64 bytes from 127.0.0.1: icmp_seq=2 ttl=64 time=0.039 ms\n\n--- ${args.at(-1) || 'localhost'} ping statistics ---\n2 packets transmitted, 2 received, 0% packet loss\n`))
      .register('curl', ({ args }) => fail(`curl: network access is disabled in this frontend sandbox (${args.at(-1) || 'no URL'})\n`, 6))
      .register('wget', ({ args }) => fail(`wget: network access is disabled in this frontend sandbox (${args.at(-1) || 'no URL'})\n`, 4))
      .register('dig', ({ args }) => ok(`; <<>> DiG 9.18 <<>> ${args.at(-1) || 'localhost'}\n;; ANSWER SECTION:\n${args.at(-1) || 'localhost'}. 60 IN A 127.0.0.1\n`))
      .register('nslookup', ({ args }) => ok(`Server:\t\t127.0.0.1\nAddress:\t127.0.0.1#53\n\nName:\t${args[0] || 'localhost'}\nAddress: 127.0.0.1\n`))
      .register('systemctl', ({ args }) => ok(renderSystemctl(args, services)))
      .register('service', ({ args }) => ok(renderSystemctl(args.length > 1 ? [args[1], args[0]] : args, services)))
      .register('journalctl', ({ args, fs, terminal, home, user, groups }) => ok(renderJournal(args, fs, terminal, home, user, groups)))
      .register('sudo', ({ args }) => renderSudo(args))
      .register('su', ({ args }) => fail(`Password: \nsu: Authentication failure${args[0] ? ` for ${args[0]}` : ''}\n`, 1))
      .register('passwd', () => fail('passwd: Authentication token manipulation error\npasswd: password unchanged\n', 10))
      .register('ssh', ({ args }) => fail(`ssh: connect to host ${args.at(-1) || 'localhost'} port 22: Network is unreachable\n`, 255))
      .register('scp', () => fail('scp: network access is disabled in this frontend sandbox\n', 255))
      .register('apt', packageManager)
      .register('apt-get', packageManager)
      .register('dnf', packageManager)
      .register('yum', packageManager)
      .register('pacman', packageManager)
      .register('apk', packageManager)
      .register('brew', packageManager)
      .register('git', gitCommand)
      .register('python', runtimeCommand('Python 3.12.3'))
      .register('python3', runtimeCommand('Python 3.12.3'))
      .register('node', runtimeCommand('v22.12.0'))
      .register('npm', packageRuntimeCommand('npm 11.0.0'))
      .register('vim', editorCommand('vim'))
      .register('vi', editorCommand('vi'))
      .register('nano', editorCommand('nano'))
      .register('less', pagerCommand)
      .register('more', pagerCommand)
      .register('tree', treeCommand)
      .register('file', fileCommand)
      .register('stat', statCommand)
      .register('du', duCommand)
      .register('sha256sum', digestCommand('sha256'))
      .register('md5sum', digestCommand('md5'))
      .register('base64', base64Command)
      .register('xxd', xxdCommand)
      .register('od', odCommand)
      .register('strings', stringsCommand)
      .register('sed', sedCommand)
      .register('awk', awkCommand)
      .register('cut', cutCommand)
      .register('tr', trCommand)
      .register('rev', ({ args, stdin }) => ok(((args.length ? args.join(' ') : stdin).split('').reverse().join('')) + '\n'))
      .register('yes', ({ args }) => ok(Array(64).fill(args.join(' ') || 'y').join('\n') + '\n'));
  }
  
  function renderHelp(terminal) {
    const names = terminal.commandNames();
    return [
      'Blog Terminal Sandbox',
      '',
      'Core syntax: quotes, $VAR, $(cmd), pipes, &&, ||, ;, >, >>, globs.',
      'Virtual FS: POSIX-like paths, owners, groups, permissions, safe rm guard.',
      '',
      wrapColumns(names, 18, 4),
      '',
    ].join('\n');
  }
  
  function renderUname(args, kernel) {
    if (args.includes('-a')) return `Linux blog-server ${kernel} #1 SMP PREEMPT_DYNAMIC x86_64 GNU/Linux\n`;
    if (args.includes('-r')) return `${kernel}\n`;
    if (args.includes('-m')) return 'x86_64\n';
    return 'Linux\n';
  }
  
  function renderUptime(bootTime) {
    return ` ${new Date().toTimeString().slice(0, 8)} up ${formatDuration(Date.now() - bootTime.getTime())},  1 user,  load average: 0.08, 0.04, 0.01\n`;
  }
  
  function renderIp(args) {
    if (args.includes('route')) return 'default via 127.0.0.1 dev lo proto static\n127.0.0.0/8 dev lo proto kernel scope link src 127.0.0.1\n';
    return '1: lo: <LOOPBACK,UP,LOWER_UP> mtu 65536 qdisc noqueue state UNKNOWN group default\n    inet 127.0.0.1/8 scope host lo\n2: eth0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 state UP group default\n    inet 10.0.2.15/24 brd 10.0.2.255 scope global eth0\n';
  }
  
  function renderSockets() {
    return 'Netid State  Recv-Q Send-Q Local Address:Port  Peer Address:Port Process\nudp   UNCONN 0      0      127.0.0.53:53      0.0.0.0:*       users:(("systemd-resolve",pid=211))\ntcp   LISTEN 0      128    0.0.0.0:80         0.0.0.0:*       users:(("nginx",pid=901))\ntcp   LISTEN 0      128    127.0.0.1:22       0.0.0.0:*       users:(("sshd",pid=777))\n';
  }
  
  function renderSystemctl(args, services) {
    const action = args[0] || 'list-units';
    const unit = args[1] || args[0];
    if (action === 'status' && unit) {
      const svc = services.find(([name]) => name === unit || name.startsWith(`${unit}.`));
      if (!svc) return `Unit ${unit}.service could not be found.\n`;
      return `* ${svc[0]} - ${svc[2]}\n     Loaded: ${svc[1].split(' ')[0]} (/lib/systemd/system/${svc[0]}; enabled)\n     Active: ${svc[1].split(' ').slice(1).join(' ')} since today; simulated\n`;
    }
    if (['start', 'stop', 'restart', 'enable', 'disable'].includes(action)) return `Failed to ${action} ${unit || 'unit'}: Interactive authentication required.\n`;
    return 'UNIT           LOAD   ACTIVE SUB     DESCRIPTION\n' + services.map(row => `${row[0].padEnd(14)} ${row[1].padEnd(20)} ${row[2]}`).join('\n') + '\n';
  }
  
  function sessionCommand({ args, terminal }) {
    const action = args[0] || 'status';
    if (action === 'status') {
      return ok(`cwd=${terminal.cwd}\nhistory=${terminal.history.length}\npersistence=${terminal.persistence ? 'enabled' : 'disabled'}\nlast_status=${terminal.lastStatus}\n`);
    }
    if (action === 'save') {
      terminal.persist();
      return ok('session: state saved\n');
    }
    if (action === 'reset') {
      terminal.resetSessionState();
      return ok('session: state reset\n', { events: [{ type: 'clear' }, { type: 'session-reset' }] });
    }
    return fail('session: usage: session [status|save|reset]\n', 2);
  }
  
  function renderJournal(args, fs, terminal, home, user, groups) {
    const lines = [
      'May 08 10:01:00 blog-server systemd[1]: Started Blog Terminal Sandbox.',
      'May 08 10:01:01 blog-server nginx[901]: static content ready',
      'May 08 10:01:02 blog-server sshd[777]: listening on 127.0.0.1 port 22',
    ];
    try {
      const syslog = fs.readFile('/var/log/syslog', { cwd: terminal.cwd, home, user, groups }).trim();
      if (syslog) lines.push(...syslog.split('\n'));
    } catch (error) {
      reportDiagnostic(error, { source: 'plugins.system.journalctl' });
    }
    const countIndex = args.findIndex(arg => arg === '-n');
    const count = countIndex >= 0 ? parseInt(args[countIndex + 1], 10) || 10 : lines.length;
    return lines.slice(-count).join('\n') + '\n';
  }
  
  function renderSudo(args) {
    if (args.length === 0 || args.includes('-v')) return fail('sudo: a password is required\n', 1);
    if (args.includes('-l')) {
      return ok('Matching Defaults entries for guest on blog-server:\n    env_reset, mail_badpass, secure_path=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\n\nUser guest may run the following commands on blog-server:\n    (root) NOPASSWD: /usr/bin/true\n');
    }
    if (args[0] === 'rm' && (args.includes('/') || args.includes('--no-preserve-root'))) {
      return fail('sudo rm: refusing to remove root directory in browser sandbox\n', 1);
    }
    return fail(`sudo: ${args[0]}: command requires privileges that are not granted in this sandbox\n`, 1);
  }
  
  function packageManager({ name, args }) {
    if (args.includes('--version') || args.includes('-v')) return ok(`${name} 2.7.14 (simulated)\n`);
    return fail(`${name}: package installation is disabled in this read-only frontend sandbox\n`, 100);
  }
  
  function gitCommand({ args, terminal }) {
    const sub = args[0] || 'status';
    if (sub === '--version' || sub === 'version') return ok('git version 2.45.0\n');
    if (sub === 'status') return ok(`On branch main\nYour branch is up to date with 'origin/main'.\n\nnothing to commit, working tree clean\n`);
    if (sub === 'branch') return ok('* main\n');
    if (sub === 'log') return ok('commit 9f3a1db terminal: improve browser sandbox\nAuthor: guest <guest@blog-server>\n\n    Simulated repository history\n');
    if (sub === 'remote') return ok('origin\thttps://github.com/example/blog.git (fetch)\norigin\thttps://github.com/example/blog.git (push)\n');
    if (sub === 'clone' || sub === 'pull' || sub === 'push' || sub === 'fetch') return fail(`git: network operation '${sub}' is disabled in this sandbox\n`, 128);
    return ok(`git: '${sub}' is recognized by the shell, but not modeled here (${terminal.cwd})\n`);
  }
  
  function runtimeCommand(version) {
    return ({ args, name }) => {
      if (args.includes('--version') || args.includes('-V') || args.includes('-v')) return ok(`${version}\n`);
      if (args.includes('-c') || args.includes('-e') || args.includes('--eval')) {
        return fail(`${name}: executing arbitrary code is disabled in this frontend sandbox\n`, 126);
      }
      return fail(`${name}: executing arbitrary code is disabled in this frontend sandbox\n`, 126);
    };
  }
  
  function packageRuntimeCommand(version) {
    return ({ args, name }) => {
      if (args.includes('--version') || args.includes('-v')) return ok(`${version}\n`);
      return fail(`${name}: package scripts are disabled in this frontend sandbox\n`, 126);
    };
  }
  
  function editorCommand(editor) {
    return ({ args }) => {
      const file = args[0] || null;
      return ok(`${editor}: opened ${file || '[No Name]'} in the frontend editor preview\n`, {
        events: [{ type: 'editor', editor, file }],
      });
    };
  }
  
  function pagerCommand(ctx) {
    return ok(readText(ctx.args, ctx.stdin, ctx));
  }
  
  function treeCommand({ args, fs, terminal, home }) {
    const root = fs.normalize(args.find(arg => !arg.startsWith('-')) || terminal.cwd, { cwd: terminal.cwd, home });
    const stat = fs.stat(root);
    if (!stat) return fail(`tree: ${root}: No such file or directory\n`);
    const lines = [root];
    const walk = (dir, prefix = '') => {
      const items = fs.list(dir, { all: args.includes('-a'), cwd: terminal.cwd, home });
      items.forEach((name, index) => {
        const path = dir === '/' ? `/${name}` : `${dir}/${name}`;
        const node = fs.stat(path);
        const last = index === items.length - 1;
        lines.push(`${prefix}${last ? '`-- ' : '|-- '}${name}${node?.type === 'dir' ? '/' : ''}`);
        if (node?.type === 'dir' && lines.length < 300) walk(path, prefix + (last ? '    ' : '|   '));
      });
    };
    if (stat.type === 'dir') walk(root);
    return ok(lines.join('\n') + '\n');
  }
  
  function fileCommand({ args, fs, terminal, home }) {
    if (!args.length) return fail('file: missing operand\n');
    return ok(args.map(arg => {
      const path = fs.normalize(arg, { cwd: terminal.cwd, home });
      const node = fs.stat(path);
      if (!node) return `${arg}: cannot open (${arg}: No such file or directory)`;
      if (node.type === 'dir') return `${arg}: directory`;
      if (node.type === 'exec') return `${arg}: ELF 64-bit LSB executable, x86-64, simulated`;
      const text = String(node.content || '');
      if (/^#!.*\b(sh|bash|node|python)/.test(text)) return `${arg}: script text executable`;
      if (/[\x00-\x08\x0E-\x1F]/.test(text)) return `${arg}: data`;
      if (/^\s*</.test(text)) return `${arg}: HTML document, UTF-8 Unicode text`;
      return `${arg}: UTF-8 Unicode text`;
    }).join('\n') + '\n');
  }
  
  function statCommand({ args, fs, terminal, home }) {
    if (!args.length) return fail('stat: missing operand\n');
    return ok(args.map(arg => {
      const path = fs.normalize(arg, { cwd: terminal.cwd, home });
      const node = fs.stat(path);
      if (!node) return `stat: cannot statx '${arg}': No such file or directory`;
      return `  File: ${arg}\n  Size: ${node.size || 0}\tBlocks: 1\tIO Block: 4096\t${node.type === 'dir' ? 'directory' : 'regular file'}\nAccess: (${modeOctal(node.perm || '-rw-r--r--')}/${node.perm || '-rw-r--r--'})  Uid: (${node.owner || node.user || 'root'})   Gid: (${node.group || 'root'})`;
    }).join('\n') + '\n');
  }
  
  function duCommand({ args, fs, terminal, home }) {
    const targets = args.filter(arg => !arg.startsWith('-'));
    const human = args.includes('-h');
    const paths = targets.length ? targets : [terminal.cwd];
    return ok(paths.map(arg => {
      const path = fs.normalize(arg, { cwd: terminal.cwd, home });
      const prefix = path === '/' ? '/' : `${path}/`;
      let size = 0;
      for (const [key, node] of fs.nodes) if (key === path || key.startsWith(prefix)) size += node.size || 0;
      return `${human ? humanSize(size) : Math.ceil(size / 1024)}\t${arg}`;
    }).join('\n') + '\n');
  }
  
  function digestCommand(kind) {
    return async ctx => {
      const files = ctx.args.filter(arg => !arg.startsWith('-'));
      if (!files.length) return ok(`${await digest(kind, ctx.stdin)}  -\n`);
      const rows = [];
      for (const file of files) rows.push(`${await digest(kind, ctx.fs.readFile(file, ctx))}  ${file}`);
      return ok(rows.join('\n') + '\n');
    };
  }
  
  function base64Command(ctx) {
    const decode = ctx.args.includes('-d') || ctx.args.includes('--decode');
    const text = readText(ctx.args.filter(arg => !arg.startsWith('-')), ctx.stdin, ctx);
    return ok((decode ? bytesToText(base64ToBytes(text.trim())) : bytesToBase64(textToBytes(text))) + '\n');
  }
  
  function xxdCommand(ctx) {
    const bytes = textToBytes(readText(ctx.args.filter(arg => !arg.startsWith('-')), ctx.stdin, ctx));
    const rows = [];
    for (let offset = 0; offset < bytes.length; offset += 16) {
      const chunk = bytes.slice(offset, offset + 16);
      const hex = [...chunk].map(byte => byte.toString(16).padStart(2, '0')).join(' ').padEnd(47);
      const ascii = [...chunk].map(byte => byte >= 32 && byte <= 126 ? String.fromCharCode(byte) : '.').join('');
      rows.push(`${offset.toString(16).padStart(8, '0')}: ${hex}  ${ascii}`);
    }
    return ok(rows.join('\n') + (rows.length ? '\n' : ''));
  }
  
  function odCommand(ctx) {
    const bytes = textToBytes(readText(ctx.args.filter(arg => !arg.startsWith('-')), ctx.stdin, ctx));
    const rows = [];
    for (let offset = 0; offset < bytes.length; offset += 16) {
      const chunk = bytes.slice(offset, offset + 16);
      rows.push(`${offset.toString(8).padStart(7, '0')} ${[...chunk].map(byte => byte.toString(8).padStart(3, '0')).join(' ')}`);
    }
    return ok(rows.join('\n') + (rows.length ? '\n' : ''));
  }
  
  function stringsCommand(ctx) {
    const text = readText(ctx.args.filter(arg => !arg.startsWith('-')), ctx.stdin, ctx);
    return ok((text.match(/[ -~]{4,}/g) || []).join('\n') + '\n');
  }
  
  function sedCommand(ctx) {
    const script = ctx.args.find(arg => !arg.startsWith('-')) || '';
    const rest = ctx.args.slice(ctx.args.indexOf(script) + 1);
    const text = readText(rest, ctx.stdin, ctx);
    const match = script.match(/^s(.)(.*?)\1(.*?)\1(g?)$/);
    if (!match) return fail('sed: only s/pattern/replacement/[g] is modeled\n');
    const regex = new RegExp(match[2], match[4] ? 'g' : '');
    return ok(text.replace(regex, match[3]));
  }
  
  function awkCommand(ctx) {
    const program = ctx.args.find(arg => !arg.startsWith('-')) || '';
    const rest = ctx.args.slice(ctx.args.indexOf(program) + 1);
    const text = readText(rest, ctx.stdin, ctx);
    const match = program.match(/^\{\s*print\s+\$(\d+)\s*\}$/);
    if (!match) return fail('awk: only {print $N} is modeled\n');
    const field = Number(match[1]) - 1;
    return ok(text.split('\n').filter(Boolean).map(line => line.trim().split(/\s+/)[field] || '').join('\n') + '\n');
  }
  
  function cutCommand(ctx) {
    const parsed = { delimiter: '\t', field: null, chars: null, files: [] };
    for (let i = 0; i < ctx.args.length; i++) {
      const arg = ctx.args[i];
      if (arg === '-d') parsed.delimiter = ctx.args[++i] || '\t';
      else if (arg.startsWith('-d')) parsed.delimiter = arg.slice(2) || '\t';
      else if (arg === '-f') parsed.field = ctx.args[++i] || null;
      else if (arg.startsWith('-f')) parsed.field = arg.slice(2);
      else if (arg === '-c') parsed.chars = ctx.args[++i] || null;
      else if (arg.startsWith('-c')) parsed.chars = arg.slice(2);
      else if (!arg.startsWith('-')) parsed.files.push(arg);
    }
    const text = readText(parsed.files, ctx.stdin, ctx);
    if (parsed.field) {
      const field = Number(parsed.field) - 1;
      return ok(text.split('\n').filter(Boolean).map(line => line.split(parsed.delimiter)[field] || '').join('\n') + '\n');
    }
    if (parsed.chars) {
      const [start, end] = parseRange(parsed.chars);
      return ok(text.split('\n').map(line => line.slice(start, end)).join('\n'));
    }
    return fail('cut: you must specify a list of bytes, characters, or fields\n');
  }
  
  function trCommand({ args, stdin }) {
    if (args[0] === '-d') {
      const remove = new Set([...expandTrSet(args[1] || '')]);
      return ok([...stdin].filter(ch => !remove.has(ch)).join(''));
    }
    const from = expandTrSet(args[0] || '');
    const to = expandTrSet(args[1] || '');
    const map = new Map([...from].map((ch, index) => [ch, to[index] ?? to.at(-1) ?? '']));
    return ok([...stdin].map(ch => map.has(ch) ? map.get(ch) : ch).join(''));
  }
  
  function expandTrSet(value) {
    const chars = [];
    const text = String(value);
    for (let i = 0; i < text.length; i++) {
      if (i + 2 < text.length && text[i + 1] === '-') {
        const start = text.charCodeAt(i);
        const end = text.charCodeAt(i + 2);
        const step = start <= end ? 1 : -1;
        for (let code = start; step > 0 ? code <= end : code >= end; code += step) chars.push(String.fromCharCode(code));
        i += 2;
      } else {
        chars.push(text[i]);
      }
    }
    return chars.join('');
  }
  
  function readText(args, stdin, { fs, terminal, home, user, groups }) {
    const files = args.filter(arg => !arg.startsWith('-'));
    if (!files.length) return stdin || '';
    return files.map(file => fs.readFile(file, { cwd: terminal.cwd, home, user, groups })).join('\n');
  }
  
  async function digest(kind, text) {
    if (kind === 'sha256' && globalThis.crypto?.subtle) {
      const buffer = await globalThis.crypto.subtle.digest('SHA-256', textToBytes(text));
      return bytesToHex(new Uint8Array(buffer));
    }
    return simpleHash(`${kind}:${text}`, kind === 'md5' ? 32 : 64);
  }
  
  function bytesToHex(bytes) {
    return [...bytes].map(byte => byte.toString(16).padStart(2, '0')).join('');
  }
  
  function simpleHash(text, length) {
    let h1 = 0x811c9dc5;
    let h2 = 0x9e3779b9;
    for (let i = 0; i < text.length; i++) {
      h1 = Math.imul(h1 ^ text.charCodeAt(i), 0x01000193) >>> 0;
      h2 = Math.imul(h2 + text.charCodeAt(i), 0x85ebca6b) >>> 0;
    }
    return (h1.toString(16).padStart(8, '0') + h2.toString(16).padStart(8, '0')).repeat(4).slice(0, length);
  }
  
  const textEncoder = new TextEncoder();
  
  function textToBytes(text) {
    return textEncoder.encode(String(text ?? ''));
  }
  
  function bytesToText(bytes) {
    return new TextDecoder().decode(bytes);
  }
  
  const BASE64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
  
  function bytesToBase64(bytes) {
    let out = '';
    for (let i = 0; i < bytes.length; i += 3) {
      const a = bytes[i];
      const b = bytes[i + 1] ?? 0;
      const c = bytes[i + 2] ?? 0;
      const n = (a << 16) | (b << 8) | c;
      out += BASE64[(n >> 18) & 63] + BASE64[(n >> 12) & 63] + (i + 1 < bytes.length ? BASE64[(n >> 6) & 63] : '=') + (i + 2 < bytes.length ? BASE64[n & 63] : '=');
    }
    return out;
  }
  
  function base64ToBytes(value) {
    const clean = String(value).replace(/[^A-Za-z0-9+/=]/g, '');
    const bytes = [];
    for (let i = 0; i < clean.length; i += 4) {
      const n = (BASE64.indexOf(clean[i]) << 18) | (BASE64.indexOf(clean[i + 1]) << 12) | ((BASE64.indexOf(clean[i + 2]) & 63) << 6) | (BASE64.indexOf(clean[i + 3]) & 63);
      bytes.push((n >> 16) & 255);
      if (clean[i + 2] !== '=') bytes.push((n >> 8) & 255);
      if (clean[i + 3] !== '=') bytes.push(n & 255);
    }
    return new Uint8Array(bytes);
  }
  
  function wrapColumns(items, width, columns) {
    const rows = [];
    for (let i = 0; i < items.length; i += columns) rows.push(items.slice(i, i + columns).map(item => item.padEnd(width)).join(''));
    return rows.join('\n');
  }
  
  function uidFor(name) {
    if (name === 'root') return 0;
    if (name === 'www-data') return 33;
    if (name === 'adm') return 4;
    return 1000;
  }
  
  function formatLoginTime(date) {
    return `${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')} ${date.toTimeString().slice(0, 5)}`;
  }
  
  function formatDuration(ms) {
    const minutes = Math.max(1, Math.floor(ms / 60000));
    const hours = Math.floor(minutes / 60);
    const days = Math.floor(hours / 24);
    if (days) return `${days} day${days > 1 ? 's' : ''}, ${hours % 24}:${String(minutes % 60).padStart(2, '0')}`;
    if (hours) return `${hours}:${String(minutes % 60).padStart(2, '0')}`;
    return `${minutes} min`;
  }
  
  function humanSize(bytes) {
    if (bytes < 1024) return `${bytes}B`;
    if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)}K`;
    return `${(bytes / 1024 / 1024).toFixed(1)}M`;
  }
  
  function modeOctal(perm) {
    const triplets = [perm.slice(1, 4), perm.slice(4, 7), perm.slice(7, 10)];
    return triplets.map(chunk => (chunk[0] === 'r' ? 4 : 0) + (chunk[1] === 'w' ? 2 : 0) + (/[xst]/.test(chunk[2]) ? 1 : 0)).join('');
  }
  
  function parseRange(value) {
    const [start, end] = String(value || '').split('-').map(Number);
    return [Math.max(0, (start || 1) - 1), Number.isFinite(end) ? end : undefined];
  }
  
  __exports["systemCommandsPlugin"] = systemCommandsPlugin;
  
};
__modules["src/plugins/effect-events.mjs"] = (__exports, __import) => {
  const { ok } = __import("src/result.mjs");
  function effectEventsPlugin(terminal, options = {}) {
    const effects = {
      clear: { type: 'clear' },
      htop: { type: 'effect', name: 'htop' },
      vim: { type: 'editor', editor: 'vim' },
      vi: { type: 'editor', editor: 'vi' },
      nano: { type: 'editor', editor: 'nano' },
      cmatrix: { type: 'effect', name: 'cmatrix' },
      matrix: { type: 'effect', name: 'cmatrix' },
      sl: { type: 'effect', name: 'sl' },
      starwars: { type: 'effect', name: 'starwars' },
      hollywood: { type: 'effect', name: 'hollywood' },
      invaders: { type: 'game', name: 'invaders' },
      ...options.effects,
    };
  
    Object.entries(effects).forEach(([name, event]) => {
      terminal.register(name, ({ args }) => {
        const payload = { ...event, args };
        if (event.type === 'editor') payload.file = args[0] || null;
        return ok('', { events: [payload] });
      });
    });
  }
  
  __exports["effectEventsPlugin"] = effectEventsPlugin;
  
};
__modules["src/plugins/windows-commands.mjs"] = (__exports, __import) => {
  const { filterRecords, formatRecords, normalizeProperties, projectRecords, sortRecords } = __import("src/extension.mjs");
  const { fail, ok } = __import("src/result.mjs");
  function windowsCommandsPlugin(terminal, options = {}) {
    const shell = options.shell || 'powershell';
    const defaultDrive = options.drive || 'C:';
    terminal.env.OS = 'Windows_NT';
    terminal.env.SHELL = shell === 'cmd' ? 'cmd.exe' : 'pwsh.exe';
    terminal.windowsDrive = defaultDrive;
  
    registerCommonCommands(terminal);
    if (shell === 'cmd') registerCmdCommands(terminal);
    else registerPowerShellCommands(terminal);
  }
  
  function registerCommonCommands(terminal) {
    terminal
      .register('cls', () => ok('', { events: [{ type: 'clear' }] }))
      .register('cd', locationCommand)
      .register('cd\\', ({ terminal }) => {
        terminal.cwd = '/';
        terminal.env.PWD = terminal.cwd;
        return ok('');
      });
  }
  
  function registerPowerShellCommands(terminal) {
    terminal
      .register('Clear-Host', () => ok('', { events: [{ type: 'clear' }] }))
      .register('Get-Location', ({ terminal }) => ok(`${toWindowsPath(terminal.cwd, windowsDrive(terminal))}\n`))
      .register('Set-Location', locationCommand)
      .register('dir', directoryCommand)
      .register('Get-ChildItem', directoryCommand)
      .register('Get-Item', getItemCommand)
      .register('Get-Content', contentCommand)
      .register('Set-Content', setContentCommand)
      .register('Add-Content', addContentCommand)
      .register('Test-Path', testPathCommand)
      .register('Where-Object', whereObjectCommand)
      .register('Select-Object', selectObjectCommand)
      .register('Sort-Object', sortObjectCommand)
      .register('Format-Table', formatTableCommand)
      .register('Write-Output', ({ args }) => ok(args.join(' ') + '\n'))
      .register('Copy-Item', copyCommand)
      .register('Move-Item', moveCommand)
      .register('Remove-Item', removeCommand)
      .register('Rename-Item', renameCommand)
      .register('New-Item', newItemCommand)
      .register('Get-Help', getHelpCommand)
      .register('Get-Command', ({ terminal }) => ok(terminal.commandNames().join('\n') + '\n'))
      .register('powershell', () => ok('PowerShell 7.4.0 (simulated frontend shell)\n'))
      .register('pwsh', () => ok('PowerShell 7.4.0 (simulated frontend shell)\n'));
  }
  
  function registerCmdCommands(terminal) {
    terminal
      .register('chdir', locationCommand)
      .register('dir', directoryCommand)
      .register('type', contentCommand)
      .register('copy', copyCommand)
      .register('move', moveCommand)
      .register('del', removeCommand)
      .register('erase', removeCommand)
      .register('rd', removeCommand)
      .register('rmdir', removeCommand)
      .register('ren', renameCommand)
      .register('md', ctx => newItemCommand({ ...ctx, args: ['-ItemType', 'Directory', ...ctx.args] }))
      .register('mkdir', ctx => newItemCommand({ ...ctx, args: ['-ItemType', 'Directory', ...ctx.args] }))
      .register('ver', () => ok('Microsoft Windows [Version 10.0.22631.0000] (simulated)\n'))
      .register('cmd', () => ok('Microsoft Windows Command Prompt (simulated frontend shell)\n'));
  }
  
  function locationCommand({ args, terminal, fs, home }) {
    const target = fromWindowsPath(args[0] || home);
    const path = fs.normalize(target, { cwd: terminal.cwd, home });
    const stat = fs.stat(path);
    if (!stat) return fail(`Set-Location: Cannot find path '${args[0] || target}' because it does not exist.\n`, 1);
    if (stat.type !== 'dir') return fail(`Set-Location: Path '${args[0] || target}' is not a container.\n`, 1);
    terminal.env.OLDPWD = terminal.cwd;
    terminal.cwd = path;
    terminal.env.PWD = path;
    return ok('');
  }
  
  function getItemCommand(ctx) {
    const targetArg = firstPathArg(ctx.args) || ctx.terminal.cwd;
    const target = ctx.fs.normalize(fromWindowsPath(targetArg), { cwd: ctx.terminal.cwd, home: ctx.home });
    const stat = ctx.fs.stat(target);
    if (!stat) return fail(`Get-Item: Cannot find path '${targetArg}' because it does not exist.\n`, 1);
    const record = itemRecord(stat, ctx.terminal);
    return ok(`\n    Directory: ${toWindowsPath(ctx.fs.dirname(target), windowsDrive(ctx.terminal))}\n\nMode       Length Name\n----       ------ ----\n${itemRow(stat)}\n`, { data: [record] });
  }
  
  function testPathCommand(ctx) {
    const targetArg = firstPathArg(ctx.args);
    if (!targetArg) return ok('False\n');
    const target = ctx.fs.normalize(fromWindowsPath(targetArg), { cwd: ctx.terminal.cwd, home: ctx.home });
    return ok(ctx.fs.stat(target) ? 'True\n' : 'False\n');
  }
  
  function directoryCommand(ctx) {
    const targetArg = firstPathArg(ctx.args) || ctx.terminal.cwd;
    const target = ctx.fs.normalize(fromWindowsPath(targetArg), { cwd: ctx.terminal.cwd, home: ctx.home });
    const stat = ctx.fs.stat(target);
    if (!stat) return fail(`dir: cannot find ${targetArg}\n`, 1);
    if (!ctx.fs.canRead(target, ctx)) return fail(`dir: access denied: ${targetArg}\n`, 1);
    if (stat.type !== 'dir') return ok(`${toWindowsPath(target, windowsDrive(ctx.terminal))}\n`, { data: [itemRecord(stat, ctx.terminal)] });
    const nodes = ctx.fs.list(target, { all: hasFlag(ctx.args, '-Force'), cwd: ctx.terminal.cwd, home: ctx.home }).map(name => {
      const path = target === '/' ? `/${name}` : `${target}/${name}`;
      const node = ctx.fs.stat(path);
      return { node, name };
    });
    const rows = nodes.map(item => itemRow(item.node, item.name));
    const data = nodes.map(item => itemRecord(item.node, ctx.terminal, item.name));
    return ok(`\n    Directory: ${toWindowsPath(target, windowsDrive(ctx.terminal))}\n\nMode       Length Name\n----       ------ ----\n${rows.join('\n')}${rows.length ? '\n' : ''}`, { data });
  }
  
  function contentCommand(ctx) {
    const files = ctx.args.filter(arg => !arg.startsWith('-'));
    if (!files.length) return ok(ctx.stdin || '');
    return ok(files.map(file => ctx.fs.readFile(fromWindowsPath(file), {
      cwd: ctx.terminal.cwd,
      home: ctx.home,
      user: ctx.user,
      groups: ctx.groups,
    })).join('\n'));
  }
  
  function setContentCommand(ctx) {
    const path = firstPathArg(ctx.args);
    if (!path) return fail('Set-Content: missing path\n', 1);
    ctx.fs.writeFile(fromWindowsPath(path), contentValue(ctx.args, path), ctx);
    return ok('');
  }
  
  function addContentCommand(ctx) {
    const path = firstPathArg(ctx.args);
    if (!path) return fail('Add-Content: missing path\n', 1);
    ctx.fs.writeFile(fromWindowsPath(path), contentValue(ctx.args, path), { ...ctx, append: true });
    return ok('');
  }
  
  function whereObjectCommand(ctx) {
    const input = Array.isArray(ctx.input) ? ctx.input : [];
    if (input.length === 0) return ok('', { data: [] });
    const expression = parseWhereExpression(ctx.args);
    if (!expression) return fail('Where-Object: expected <Property> <-Operator> <Value>\n', 1);
    const data = filterRecords(input, expression.property, expression.operator, expression.value);
    return ok('', { data });
  }
  
  function selectObjectCommand(ctx) {
    const input = Array.isArray(ctx.input) ? ctx.input : [];
    const properties = normalizeProperties(propertiesAfter(ctx.args, '-Property') || ctx.args);
    const data = properties.length ? projectRecords(input, properties) : input;
    return ok('', { data });
  }
  
  function sortObjectCommand(ctx) {
    const input = Array.isArray(ctx.input) ? ctx.input : [];
    const property = (propertiesAfter(ctx.args, '-Property') || ctx.args).find(arg => !arg.startsWith('-')) || 'Name';
    const direction = hasFlag(ctx.args, '-Descending') ? 'desc' : 'asc';
    return ok('', { data: sortRecords(input, property, direction) });
  }
  
  function formatTableCommand(ctx) {
    const input = Array.isArray(ctx.input) ? ctx.input : [];
    const properties = normalizeProperties(propertiesAfter(ctx.args, '-Property') || ctx.args);
    return ok(formatRecords(input, properties));
  }
  
  function copyCommand(ctx) {
    const files = ctx.args.filter(arg => !arg.startsWith('-'));
    if (files.length < 2) return fail('Copy-Item: missing source or destination\n', 1);
    const target = fromWindowsPath(files.at(-1));
    files.slice(0, -1).forEach(source => ctx.fs.copy(fromWindowsPath(source), target, ctx));
    return ok('');
  }
  
  function moveCommand(ctx) {
    const files = ctx.args.filter(arg => !arg.startsWith('-'));
    if (files.length < 2) return fail('Move-Item: missing source or destination\n', 1);
    const target = fromWindowsPath(files.at(-1));
    files.slice(0, -1).forEach(source => ctx.fs.move(fromWindowsPath(source), target, ctx));
    return ok('');
  }
  
  function removeCommand(ctx) {
    const recursive = hasFlag(ctx.args, '-Recurse') || hasFlag(ctx.args, '/s');
    const force = hasFlag(ctx.args, '-Force') || hasFlag(ctx.args, '/f');
    const targets = ctx.args.filter(arg => !arg.startsWith('-') && !arg.startsWith('/'));
    if (targets.some(target => ['/', '\\', 'C:\\', 'C:/'].includes(target))) {
      return fail('Remove-Item: refusing to remove root directory in browser sandbox\n', 1);
    }
    targets.forEach(target => ctx.fs.remove(fromWindowsPath(target), { ...ctx, recursive, force }));
    return ok('');
  }
  
  function renameCommand(ctx) {
    const files = ctx.args.filter(arg => !arg.startsWith('-'));
    if (files.length !== 2) return fail('Rename-Item: expected source and new name\n', 1);
    ctx.fs.move(fromWindowsPath(files[0]), fromWindowsPath(files[1]), ctx);
    return ok('');
  }
  
  function newItemCommand(ctx) {
    const path = firstPathArg(ctx.args);
    if (!path) return fail('New-Item: missing path\n', 1);
    const itemType = valueAfter(ctx.args, '-ItemType') || valueAfter(ctx.args, '-Type') || 'File';
    if (/directory/i.test(itemType)) ctx.fs.makeDir(fromWindowsPath(path), { ...ctx, parents: hasFlag(ctx.args, '-Force') });
    else ctx.fs.writeFile(fromWindowsPath(path), contentValue(ctx.args, path, { positional: false }), ctx);
    return ok('');
  }
  
  function getHelpCommand({ args }) {
    const topic = args[0] || 'Termlet';
    const help = {
      Termlet: 'PowerShell profile commands: Get-Location, Get-ChildItem, Get-Item, Get-Content, Set-Content, Add-Content, Test-Path, New-Item, Copy-Item, Move-Item, Remove-Item, Rename-Item.\n',
      'Get-Item': 'Get-Item [-Path] <path>\n',
      'Set-Content': 'Set-Content [-Path] <path> [-Value] <text>\n',
      'Test-Path': 'Test-Path [-Path] <path>\n',
      'Where-Object': 'Where-Object <Property> <-EQ|-NE|-Like|-GT|-GE|-LT|-LE> <Value>\n',
      'Select-Object': 'Select-Object [-Property] <Name[,Length,...]>\n',
      'Sort-Object': 'Sort-Object [-Property] <Name> [-Descending]\n',
      'Format-Table': 'Format-Table [-Property] <Name[,Length,...]>\n',
    };
    return ok(help[topic] || `Get-Help: no help found for ${topic}\n`);
  }
  
  function itemRow(node, name = null) {
    const mode = node?.type === 'dir' ? 'd----' : '-a---';
    const size = String(node?.size || 0).padStart(10);
    return `${mode} ${size} ${name || node?.path?.split('/').pop() || ''}`;
  }
  
  function itemRecord(node, terminal, name = null) {
    const itemName = name || node?.path?.split('/').pop() || '';
    return {
      Mode: node?.type === 'dir' ? 'd----' : '-a---',
      Length: node?.size || 0,
      Name: itemName,
      FullName: toWindowsPath(node?.path || '/', windowsDrive(terminal)),
      Type: node?.type || 'unknown',
    };
  }
  
  function parseWhereExpression(args) {
    const expression = [...args];
    if (expression[0]?.toLowerCase() === '-property') expression.splice(0, 1);
    if (expression.length < 3) return null;
    return {
      property: expression[0],
      operator: expression[1],
      value: expression.slice(2).join(' '),
    };
  }
  
  function propertiesAfter(args, flag) {
    const index = args.findIndex(arg => arg.toLowerCase() === flag.toLowerCase());
    if (index < 0) return null;
    return args.slice(index + 1).filter(arg => !arg.startsWith('-'));
  }
  
  function firstPathArg(args) {
    const valueFlags = new Set(['-itemtype', '-literalpath', '-path', '-type', '-value']);
    const pathIndex = args.findIndex(arg => ['-path', '-literalpath'].includes(arg.toLowerCase()));
    if (pathIndex >= 0) return args[pathIndex + 1];
    for (let i = 0; i < args.length; i += 1) {
      const arg = args[i];
      const lower = arg.toLowerCase();
      if (valueFlags.has(lower)) {
        i += 1;
        continue;
      }
      if (!arg.startsWith('-')) return arg;
    }
    return null;
  }
  
  function valueAfter(args, flag) {
    const index = args.findIndex(arg => arg.toLowerCase() === flag.toLowerCase());
    return index >= 0 ? args[index + 1] : null;
  }
  
  function contentValue(args, path, options = {}) {
    const valueIndex = args.findIndex(arg => arg.toLowerCase() === '-value');
    const positional = options.positional !== false;
    const values = valueIndex >= 0
      ? args.slice(valueIndex + 1)
      : (positional ? args.slice(Math.max(0, args.indexOf(path)) + 1).filter(arg => !arg.startsWith('-')) : []);
    const text = values.join(' ');
    return text ? `${text}\n` : '';
  }
  
  function hasFlag(args, flag) {
    const expected = flag.toLowerCase();
    return args.some(arg => arg.toLowerCase() === expected);
  }
  
  function windowsDrive(terminal) {
    return terminal.windowsDrive || 'C:';
  }
  
  function toWindowsPath(path, drive = 'C:') {
    const normalized = String(path || '/').replace(/^\/+/, '').replace(/\//g, '\\');
    return normalized ? `${drive}\\${normalized}` : `${drive}\\`;
  }
  
  function fromWindowsPath(path) {
    const value = String(path || '').trim();
    if (!value) return value;
    return value
      .replace(/^[A-Za-z]:[\\/]/, '/')
      .replace(/^[A-Za-z]:$/, '/')
      .replace(/\\/g, '/');
  }
  
  __exports["windowsCommandsPlugin"] = windowsCommandsPlugin;
  __exports["toWindowsPath"] = toWindowsPath;
  __exports["fromWindowsPath"] = fromWindowsPath;
  
};
__modules["src/plugins/feed-posts.mjs"] = (__exports, __import) => {
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  function feedPostsPlugin(posts = [], options = {}) {
    const root = options.root || '/home/guest/blog';
    const source = options.source || 'feed';
    return terminal => {
      terminal.fs.ensureDir(root, { owner: terminal.user, group: terminal.user });
      posts.forEach((post, index) => {
        const rawTitle = post.title || `post-${index + 1}`;
        const fileName = slugTitle(rawTitle, index) + '.md';
        terminal.fs.addFile(`${root}/${fileName}`, post.content || `# ${rawTitle}\n\n${post.link || ''}\n`, {
          owner: options.owner || terminal.user,
          group: options.group || terminal.user,
          perm: '-r--r--r--',
          type: 'post',
          link: post.link,
          title: rawTitle,
          meta: { source, ...post },
        });
      });
    };
  }
  
  async function fetchFeedPosts(feedUrl = '/index.xml', fetchImpl = globalThis.fetch) {
    if (!fetchImpl) throw new Error('fetch is not available');
    const response = await fetchImpl(feedUrl);
    if (!response.ok) throw new Error(`failed to fetch ${feedUrl}: ${response.status}`);
    const text = await response.text();
    return parseFeedPosts(text);
  }
  
  async function fetchDiscoveredFeedPosts(options = {}) {
    const fetchImpl = options.fetch || options.fetchImpl || globalThis.fetch;
    const feedUrl = options.feedUrl || discoverFeedUrl(options.document, options.baseUrl) || '/index.xml';
    return fetchFeedPosts(feedUrl, fetchImpl);
  }
  
  function parseFeedPosts(text) {
    const xmlText = String(text || '');
    if (typeof DOMParser === 'function') {
      try {
        const parsed = parseFeedPostsDom(xmlText);
        if (parsed.length) return parsed;
      } catch (error) {
        reportDiagnostic(error, { source: 'plugins.feed.parseDom' });
      }
    }
    return parseFeedPostsText(xmlText);
  }
  
  function discoverFeedUrl(doc = globalThis.document, baseUrl = globalThis.location?.href) {
    if (!doc?.querySelectorAll) return null;
    const links = [...doc.querySelectorAll('link[rel~="alternate"][href]')];
    const feed = links.find(link => /rss|atom|xml/i.test(link.getAttribute('type') || link.getAttribute('href') || ''));
    const href = feed?.getAttribute('href');
    if (!href) return null;
    if (!baseUrl) return href;
    try {
      return new URL(href, baseUrl).toString();
    } catch (error) {
      reportDiagnostic(error, { source: 'plugins.feed.discoverUrl' });
      return href;
    }
  }
  
  function parseFeedPostsDom(text) {
    const parser = new DOMParser();
    const xml = parser.parseFromString(text, 'text/xml');
    const rssItems = [...xml.querySelectorAll('item')];
    if (rssItems.length) {
      return rssItems.map(item => ({
        title: textOf(item, 'title') || 'untitled',
        link: textOf(item, 'link'),
        date: textOf(item, 'pubDate', 'date'),
        content: textOf(item, 'encoded', 'content', 'description'),
      }));
    }
    return [...xml.querySelectorAll('entry')].map(entry => ({
      title: textOf(entry, 'title') || 'untitled',
      link: atomLink(entry),
      date: textOf(entry, 'updated') || textOf(entry, 'published'),
      content: textOf(entry, 'content') || textOf(entry, 'summary'),
    }));
  }
  
  function parseFeedPostsText(text) {
    const rssItems = collectBlocks(text, 'item');
    if (rssItems.length) {
      return rssItems.map(item => ({
        title: tagText(item, 'title') || 'untitled',
        link: tagText(item, 'link'),
        date: tagText(item, 'pubDate', 'date'),
        content: tagText(item, 'encoded', 'content', 'description'),
      }));
    }
    return collectBlocks(text, 'entry').map(entry => ({
      title: tagText(entry, 'title') || 'untitled',
      link: atomLinkText(entry),
      date: tagText(entry, 'updated', 'published'),
      content: tagText(entry, 'content', 'summary'),
    }));
  }
  
  function slugTitle(title, index) {
    const slug = String(title)
      .replace(/\s+/g, '_')
      .replace(/[^\w\u4e00-\u9fa5_\-.\uff01-\uff5e\u3000-\u303f\uff1a\uff0c\uff08\uff09]/g, '')
      .slice(0, 48);
    return slug || `post_${index + 1}`;
  }
  
  function textOf(node, ...names) {
    const wanted = new Set(names.flat().map(name => localTagName(name).toLowerCase()));
    const found = [...node.querySelectorAll('*')].find(child => wanted.has(localTagName(child.localName || child.tagName).toLowerCase()));
    return found?.textContent?.trim() || '';
  }
  
  function atomLink(entry) {
    const alternate = [...entry.querySelectorAll('link')].find(link => !link.getAttribute('rel') || link.getAttribute('rel') === 'alternate');
    return alternate?.getAttribute('href') || alternate?.textContent?.trim() || '';
  }
  
  function collectBlocks(text, tag) {
    return [...String(text || '').matchAll(new RegExp(`<(?:[\\w.-]+:)?${escapeRegExp(tag)}\\b[^>]*>([\\s\\S]*?)<\\/(?:[\\w.-]+:)?${escapeRegExp(tag)}>`, 'gi'))]
      .map(match => match[1]);
  }
  
  function tagText(block, ...names) {
    for (const name of names) {
      const local = escapeRegExp(localTagName(name));
      const match = String(block || '').match(new RegExp(`<(?:[\\w.-]+:)?${local}\\b[^>]*>([\\s\\S]*?)<\\/(?:[\\w.-]+:)?${local}>`, 'i'));
      if (match) return decodeXml(match[1]).trim();
    }
    return '';
  }
  
  function atomLinkText(block) {
    const links = [...String(block || '').matchAll(/<link\b([^>]*)\/>|<link\b([^>]*)>([\s\S]*?)<\/link>/gi)];
    const parsed = links.map(match => ({
      attrs: match[1] || match[2] || '',
      text: decodeXml(match[3] || '').trim(),
    }));
    const alternate = parsed.find(link => !/\brel\s*=\s*["'][^"']+["']/i.test(link.attrs) || /\brel\s*=\s*["']alternate["']/i.test(link.attrs));
    const href = alternate?.attrs.match(/\bhref\s*=\s*["']([^"']+)["']/i)?.[1];
    return decodeXml(href || alternate?.text || '').trim();
  }
  
  function decodeXml(value) {
    return String(value || '')
      .replace(/<!\[CDATA\[([\s\S]*?)\]\]>/g, '$1')
      .replace(/&lt;/g, '<')
      .replace(/&gt;/g, '>')
      .replace(/&quot;/g, '"')
      .replace(/&apos;/g, "'")
      .replace(/&amp;/g, '&')
      .replace(/&#(\d+);/g, (_, code) => String.fromCodePoint(Number(code)))
      .replace(/&#x([0-9a-f]+);/gi, (_, code) => String.fromCodePoint(Number.parseInt(code, 16)));
  }
  
  function localTagName(name) {
    return String(name || '').split(':').pop();
  }
  
  function escapeRegExp(value) {
    return String(value).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  }
  
  __exports["fetchFeedPosts"] = fetchFeedPosts;
  __exports["fetchDiscoveredFeedPosts"] = fetchDiscoveredFeedPosts;
  __exports["feedPostsPlugin"] = feedPostsPlugin;
  __exports["parseFeedPosts"] = parseFeedPosts;
  __exports["discoverFeedUrl"] = discoverFeedUrl;
  
};
__modules["src/plugins/hugo-adapter.mjs"] = (__exports, __import) => {
  const { feedPostsPlugin, fetchFeedPosts } = __import("src/plugins/feed-posts.mjs");
  const __reexport1 = __import("src/plugins/feed-posts.mjs");
  __exports["feedPostsPlugin"] = __reexport1["feedPostsPlugin"];
  __exports["fetchFeedPosts"] = __reexport1["fetchFeedPosts"];
  __exports["fetchDiscoveredFeedPosts"] = __reexport1["fetchDiscoveredFeedPosts"];
  __exports["parseFeedPosts"] = __reexport1["parseFeedPosts"];
  __exports["discoverFeedUrl"] = __reexport1["discoverFeedUrl"];
  function hugoPostsPlugin(posts = [], options = {}) {
    return feedPostsPlugin(posts, { source: 'hugo', ...options });
  }
  
  async function fetchHugoPosts(feedUrl = '/index.xml', fetchImpl = globalThis.fetch) {
    return fetchFeedPosts(feedUrl, fetchImpl);
  }
  
  __exports["fetchHugoPosts"] = fetchHugoPosts;
  __exports["hugoPostsPlugin"] = hugoPostsPlugin;
  
};
__modules["src/presets/blog-sandbox.mjs"] = (__exports, __import) => {
  function blogSandboxPreset(options = {}) {
    return terminal => {
      const fs = terminal.fs;
      const user = options.user || terminal.user;
      const group = options.group || user;
      const blogRoot = options.blogRoot || `/home/${user}/blog`;
  
      terminal.hostname = options.hostname || terminal.hostname;
      if (!terminal.alias('ll')) terminal.setAlias('ll', 'ls -al');
      if (!terminal.alias('la')) terminal.setAlias('la', 'ls -a');
      if (!terminal.alias('grep')) terminal.setAlias('grep', 'grep --color=auto');
  
      fs.ensureDir(blogRoot, { owner: user, group });
      fs.addFile(`/home/${user}/.bashrc`, 'alias ll="ls -al"\nalias la="ls -a"\nexport EDITOR=vim\n', { owner: user, group, perm: '-rw-r--r--' });
      fs.addFile(`/home/${user}/.profile`, '[[ -f ~/.bashrc ]] && . ~/.bashrc\n', { owner: user, group, perm: '-rw-r--r--' });
      fs.addFile(`/home/${user}/.bash_history`, 'ls -la\ncat /etc/os-release\njournalctl -n 5\nsudo -l\n', { owner: user, group, perm: '-rw-------' });
      fs.addFile(`${blogRoot}/README.md`, '# Blog workspace\n\nThis directory is generated by the blog-sandbox preset.\n', { owner: user, group });
      fs.addFile(`${blogRoot}/hint.txt`, 'This is a sample hidden file for demos. Replace it with your own content.\n', { owner: user, group, perm: '-rw-r--r--' });
  
      fs.addFile('/etc/motd', 'Welcome to the Blog Terminal Sandbox.\nAll commands run in an isolated frontend VFS.\n');
      fs.addFile('/etc/hosts', '127.0.0.1 localhost\n127.0.1.1 blog-server\n10.0.2.15 blog-server.internal\n');
      fs.addFile('/var/log/syslog', 'May 08 10:01:01 blog-server systemd[1]: Started static blog service.\nMay 08 10:01:02 blog-server nginx[901]: GET / 200\n');
      fs.addFile('/var/log/auth.log', 'May 08 10:02:01 blog-server sudo: guest : command not allowed ; TTY=pts/0 ; PWD=/home/guest ; USER=root ; COMMAND=/bin/cat /etc/shadow\n', { owner: 'root', group: 'adm', perm: '-rw-r-----' });
      fs.addFile('/root/secret.txt', options.rootSecret || 'replace this private demo content\n', { owner: 'root', group: 'root', perm: '-rw-------' });
    };
  }
  
  __exports["blogSandboxPreset"] = blogSandboxPreset;
  
};
__modules["src/adapters/dom.mjs"] = (__exports, __import) => {
  const { DEFAULT_TERMINAL_CSS } = __import("src/adapters/dom-styles.mjs");
  const { createInputController } = __import("src/toolbox/input-controller.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  const __reexport1 = __import("src/adapters/dom-styles.mjs");
  __exports["DEFAULT_TERMINAL_CSS"] = __reexport1["DEFAULT_TERMINAL_CSS"];
  function createDomTerminalAdapter(options = {}) {
    const doc = options.document || globalThis.document;
    const mount = typeof options.mount === 'string' ? doc.querySelector(options.mount) : options.mount;
    if (!mount) throw new Error('createDomTerminalAdapter requires a mount element or selector');
    const className = options.className || 'blog-terminal';
    const theme = options.theme || 'linux';
    const welcome = options.welcome ?? '';
    const maxLines = Math.max(50, Number(options.maxLines || 1000));
    const inputController = options.inputController || createInputController();
    const state = {
      session: null,
      unsubscribe: null,
      activeInput: null,
      activeRow: null,
      prompt: '',
      destroyed: false,
      lastDispatch: Promise.resolve(),
    };
  
    const adapter = {
      capabilities: {
        inputModes: ['line', 'password', 'raw'],
        transcript: true,
        streaming: true,
        structuredData: true,
      },
      get activeInput() {
        return state.activeInput;
      },
      get lastDispatch() {
        return state.lastDispatch;
      },
      mount(session) {
        state.session = session;
        state.destroyed = false;
        mount.classList.add(className);
        applyTheme(mount, theme, options.themeClass);
        if (!mount.hasAttribute('tabindex')) mount.tabIndex = 0;
        output = doc.createElement('div');
        output.className = `${className}__output`;
        output.setAttribute('role', 'log');
        output.setAttribute('aria-live', 'polite');
        mount.appendChild(output);
        if (welcome) appendLine(welcome, 'muted');
        const clickHandler = () => focus();
        mount.addEventListener('click', clickHandler);
        disposers.push(() => mount.removeEventListener('click', clickHandler));
        state.unsubscribe = session.subscribe(handleEvent);
        newInput(session.getState().prompt);
        return adapter;
      },
      destroy() {
        state.destroyed = true;
        state.unsubscribe?.();
        state.unsubscribe = null;
        disposers.splice(0).forEach(dispose => dispose());
        inputController.dispose?.();
        state.activeInput = null;
        state.activeRow = null;
      },
      focus,
    };
  
    let output = null;
    const disposers = [];
  
    function handleEvent(event) {
      if (state.destroyed) return;
      if (event.type === 'command.submitted') freezeInput(event.prompt, event.command);
      else if (event.type === 'input.changed') syncInput(event);
      else if (event.type === 'output.chunk') appendBlock(event.text, event.stream === 'stderr' ? 'error' : '');
      else if (event.type === 'screen.cleared') clearOutput();
      else if (event.type === 'session.reset') {
        clearOutput();
        newInput(event.state?.prompt || state.prompt);
      } else if (event.type === 'prompt.changed') {
        newInput(event.prompt);
      } else if (event.type === 'error') {
        appendLine(`${event.code}: ${event.message}`, 'error');
      }
    }
  
    function newInput(promptText = '') {
      if (!output || state.destroyed) return;
      const row = doc.createElement('div');
      row.className = `${className}__input-row`;
      const prompt = doc.createElement('span');
      prompt.className = `${className}__prompt`;
      prompt.textContent = promptText;
      const input = doc.createElement('input');
      input.className = `${className}__input`;
      input.autocomplete = 'off';
      input.autocapitalize = 'off';
      input.autocorrect = 'off';
      input.spellcheck = false;
      input.setAttribute('aria-label', options.ariaLabel || 'Terminal command');
      row.append(prompt, input);
      output.appendChild(row);
      state.activeRow = row;
      state.activeInput = input;
      state.prompt = promptText;
  
      const keyHandler = event => handleKey(event, input);
      const inputHandler = () => queue({ type: 'input.set', value: input.value, cursor: input.value.length });
      input.addEventListener('keydown', keyHandler);
      input.addEventListener('input', inputHandler);
      disposers.push(() => input.removeEventListener('keydown', keyHandler));
      disposers.push(() => input.removeEventListener('input', inputHandler));
      trimOutput();
      focus();
    }
  
    function handleKey(event, input) {
      let actions = [];
      if (event.key === 'Enter') {
        actions = [
          { type: 'input.set', value: input.value, cursor: input.value.length },
          { type: 'input.submit' },
        ];
      } else {
        actions = inputController.handleKey({
          key: event.key,
          text: event.key?.length === 1 ? event.key : '',
          ctrlKey: event.ctrlKey,
          metaKey: event.metaKey,
          altKey: event.altKey,
          isComposing: event.isComposing,
        });
      }
      if (!actions.length) return;
      state.lastDispatch = actions.reduce(
        (promise, action) => promise.then(() => state.session.dispatch(action)),
        state.lastDispatch,
      );
      event.preventDefault?.();
    }
  
    function queue(action) {
      state.lastDispatch = state.lastDispatch.then(() => state.session.dispatch(action));
      return state.lastDispatch;
    }
  
    function syncInput(event) {
      if (!state.activeInput || state.activeInput.value === event.value) return;
      state.activeInput.value = event.value;
    }
  
    function freezeInput(promptText, command) {
      const row = state.activeRow;
      if (!row) return;
      row.textContent = '';
      const prompt = doc.createElement('span');
      prompt.className = `${className}__prompt`;
      prompt.textContent = promptText || state.prompt;
      const text = doc.createElement('span');
      text.className = `${className}__command`;
      text.textContent = ` ${command || ''}`;
      row.append(prompt, text);
      state.activeInput = null;
      state.activeRow = null;
    }
  
    function appendBlock(text, cls = '') {
      String(text || '').replace(/\n$/, '').split('\n').forEach(line => appendLine(line, cls));
    }
  
    function appendLine(text, cls = '') {
      if (!output) return null;
      const line = doc.createElement('div');
      line.className = `${className}__line ${sanitizeClass(cls)}`.trim();
      line.textContent = String(text ?? '');
      output.appendChild(line);
      trimOutput();
      output.scrollTop = output.scrollHeight;
      return line;
    }
  
    function clearOutput() {
      if (output) output.textContent = '';
    }
  
    function trimOutput() {
      if (!output) return;
      while (output.childNodes.length > maxLines) output.removeChild(output.firstChild);
    }
  
    function focus(focusOptions = {}) {
      if (!state.activeInput || state.activeInput.disabled) return;
      try {
        state.activeInput.focus({ preventScroll: focusOptions.preventScroll !== false });
      } catch (error) {
        reportDiagnostic(error, { source: 'adapter.dom.focus' });
        state.activeInput.focus();
      }
    }
  
    return adapter;
  }
  
  function injectDefaultStyles(doc = globalThis.document) {
    if (doc.getElementById('blog-terminal-default-style')) return;
    const style = doc.createElement('style');
    style.id = 'blog-terminal-default-style';
    style.textContent = DEFAULT_TERMINAL_CSS;
    doc.head.appendChild(style);
  }
  
  function applyTheme(mount, theme, themeClass = '') {
    [theme ? `termlet-theme-${theme}` : '', themeClass]
      .join(' ')
      .split(/\s+/)
      .map(sanitizeClass)
      .filter(Boolean)
      .forEach(name => mount.classList.add(name));
  }
  
  function sanitizeClass(value) {
    return String(value || '').split(/\s+/).filter(name => /^[-_A-Za-z0-9]{1,80}$/.test(name)).join(' ');
  }
  
  __exports["createDomTerminalAdapter"] = createDomTerminalAdapter;
  __exports["injectDefaultStyles"] = injectDefaultStyles;
  
};
__modules["src/adapters/dom-styles.mjs"] = (__exports, __import) => {
  const DEFAULT_TERMINAL_CSS = `
  .blog-terminal { --termlet-bg:#05080d; --termlet-fg:#c9fdd7; --termlet-border:#1f3b2d; --termlet-prompt:#2ea043; --termlet-error:#ff7b72; --termlet-muted:#7d8590; --termlet-focus:#58a6ff; background:var(--termlet-bg); color:var(--termlet-fg); font:13px/1.45 ui-monospace, SFMono-Regular, Consolas, monospace; padding:12px; border:1px solid var(--termlet-border); border-radius:8px; }
  .blog-terminal__output { min-height:320px; max-height:70vh; overflow:auto; white-space:pre-wrap; overflow-wrap:anywhere; tab-size:2; }
  .blog-terminal__line.error { color:var(--termlet-error); }
  .blog-terminal__line.muted { color:var(--termlet-muted); }
  .blog-terminal__input-row { display:flex; gap:8px; align-items:baseline; min-width:0; }
  .blog-terminal__prompt { color:var(--termlet-prompt); flex:none; }
  .blog-terminal__command { color:inherit; }
  .blog-terminal__input { flex:1; min-width:0; background:transparent; border:0; color:inherit; font:inherit; outline:0; }
  .blog-terminal__input:focus-visible { box-shadow:0 1px 0 var(--termlet-focus); }
  .blog-terminal.termlet-theme-linux { --termlet-bg:#05080d; --termlet-fg:#c9fdd7; --termlet-border:#1f3b2d; --termlet-prompt:#2ea043; --termlet-error:#ff7b72; --termlet-muted:#7d8590; --termlet-focus:#58a6ff; }
  .blog-terminal.termlet-theme-powershell { --termlet-bg:#012456; --termlet-fg:#f2f7ff; --termlet-border:#2563eb; --termlet-prompt:#f2f7ff; --termlet-error:#ffb4b4; --termlet-muted:#9ec5ff; --termlet-focus:#ffffff; }
  .blog-terminal.termlet-theme-cmd { --termlet-bg:#000000; --termlet-fg:#d8d8d8; --termlet-border:#333333; --termlet-prompt:#d8d8d8; --termlet-error:#ff5555; --termlet-muted:#a0a0a0; --termlet-focus:#ffffff; }
  .blog-terminal.termlet-theme-light { --termlet-bg:#f8fafc; --termlet-fg:#111827; --termlet-border:#cbd5e1; --termlet-prompt:#0f766e; --termlet-error:#b91c1c; --termlet-muted:#64748b; --termlet-focus:#2563eb; }
  .blog-terminal.termlet-theme-crt { --termlet-bg:#020403; --termlet-fg:#b8ffcf; --termlet-border:#194d2b; --termlet-prompt:#39ff88; --termlet-error:#ff6b6b; --termlet-muted:#5fae7a; --termlet-focus:#c6ffdd; text-shadow:0 0 6px rgba(57,255,136,.35); }
  .blog-terminal.termlet-size-compact { font-size:12px; padding:10px; }
  .termlet-sr-only { position:absolute; width:1px; height:1px; margin:-1px; overflow:hidden; clip:rect(0 0 0 0); white-space:nowrap; border:0; padding:0; }
  `;
  
  __exports["DEFAULT_TERMINAL_CSS"] = DEFAULT_TERMINAL_CSS;
  
};
__modules["src/toolbox/input-controller.mjs"] = (__exports, __import) => {
  function createInputController() {
    let disposed = false;
  
    return {
      handleKey(eventLike = {}) {
        if (disposed || eventLike.isComposing) return [];
        const key = String(eventLike.key || '');
        const text = typeof eventLike.text === 'string' ? eventLike.text : '';
        const ctrl = Boolean(eventLike.ctrlKey || eventLike.metaKey);
        if (ctrl && key.toLowerCase() === 'c') return [{ type: 'interrupt' }];
        if (ctrl && key.toLowerCase() === 'l') return [{ type: 'screen.clear' }];
        if (key === 'Enter') return [{ type: 'input.submit' }];
        if (key === 'Backspace') return [{ type: 'input.deleteBackward' }];
        if (key === 'ArrowUp') return [{ type: 'history.prev' }];
        if (key === 'ArrowDown') return [{ type: 'history.next' }];
        if (key === 'ArrowLeft' || key === 'ArrowRight' || key === 'Tab') {
          return [{ type: 'input.raw', key, text: '' }];
        }
        if (text) return [{ type: 'input.insert', text }];
        if (key.length === 1 && !ctrl && !eventLike.altKey) return [{ type: 'input.insert', text: key }];
        return [];
      },
      insertText(text) {
        if (disposed || !text) return [];
        return [{ type: 'input.insert', text: String(text) }];
      },
      dispose() {
        disposed = true;
      },
    };
  }
  
  __exports["createInputController"] = createInputController;
  
};
__modules["src/adapters/starter.mjs"] = (__exports, __import) => {
  const { createTerminal } = __import("src/factory.mjs");
  const { blogSandboxPreset } = __import("src/presets/blog-sandbox.mjs");
  const { createTerminalSession } = __import("src/protocol/session.mjs");
  const { createDomTerminalAdapter, injectDefaultStyles } = __import("src/adapters/dom.mjs");
  const { createSessionStorageAdapter } = __import("src/adapters/persistence.mjs");
  async function mountStarterTerminal(options = {}) {
    const {
      mount = '#terminal',
      document = globalThis.document,
      injectStyles = true,
      theme = 'linux',
      themeClass = '',
      storageKey = 'termlet.starter',
      persist = true,
      hostname = 'blog',
      user = 'guest',
      welcome = 'Try: help, about-site, ls, tree ~/blog, session reset\n',
      siteName = 'My Blog',
      intro = 'This terminal runs entirely in the browser.',
      files = {},
      commands = {},
      terminalOptions = {},
      adapterOptions = {},
      plugins = [],
    } = options;
  
    if (!mount) throw new Error('mountStarterTerminal requires a mount element or selector');
    if (injectStyles) injectDefaultStyles(document);
  
    const starterPlugin = terminal => {
      terminal.register('about-site', () => ({
        status: 0,
        stdout: `${siteName}\n${intro}\n`,
        stderr: '',
        events: [],
        data: null,
      }));
      Object.entries(commands).forEach(([name, handler]) => {
        if (typeof handler === 'function') terminal.register(name, handler);
        else terminal.register(name, () => ({
          status: 0,
          stdout: String(handler ?? '').replace(/\n?$/, '\n'),
          stderr: '',
          events: [],
          data: null,
        }));
      });
      Object.entries(files).forEach(([path, content]) => {
        terminal.fs.addFile(path, String(content ?? ''), { owner: terminal.user, group: terminal.user });
      });
    };
  
    const terminalPersistence = persist ? createSessionStorageAdapter({ key: `${storageKey}.core` }) : null;
    const sessionPersistence = persist ? createSessionStorageAdapter({ key: `${storageKey}.session` }) : null;
  
    const terminal = createTerminal({
      hostname,
      user,
      persistence: terminalPersistence,
      persistVfs: persist,
      ...terminalOptions,
      plugins: [
        blogSandboxPreset({
          hostname,
          ...(terminalOptions.blogSandbox || {}),
        }),
        ...(terminalOptions.plugins || []),
        starterPlugin,
        ...plugins,
      ],
    });
  
    const session = createTerminalSession(terminal, {
      persistence: sessionPersistence,
      adapterCapabilities: {
        inputModes: ['line', 'password', 'raw'],
      },
    });
    const adapter = createDomTerminalAdapter({
      mount,
      document,
      welcome,
      theme,
      themeClass,
      ...adapterOptions,
    }).mount(session);
  
    return { terminal, session, adapter };
  }
  
  __exports["mountStarterTerminal"] = mountStarterTerminal;
  
};
__modules["src/factory.mjs"] = (__exports, __import) => {
  const { TerminalCore } = __import("src/shell.mjs");
  const { createLinuxLikeFs } = __import("src/vfs.mjs");
  const { formatRecords, mergeProfileOptions } = __import("src/extension.mjs");
  const { basicCommandsPlugin } = __import("src/plugins/basic-commands.mjs");
  const { systemCommandsPlugin } = __import("src/plugins/system-commands.mjs");
  const { windowsCommandsPlugin } = __import("src/plugins/windows-commands.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  function createTerminal(options = {}) {
    const prepared = mergeProfileOptions(options);
    const { commandPacks = [], plugins = [], ...coreOptions } = prepared;
    const shouldRestore = prepared.restore !== false;
    const terminal = new TerminalCore({
      ...coreOptions,
      fs: prepared.fs || createLinuxLikeFs(prepared.fsOptions),
      restore: false,
    });
  
    if (prepared.basicCommands !== false) {
      terminal.use(basicCommandsPlugin, prepared.basicCommands || {});
    }
    if (prepared.systemCommands !== false) {
      terminal.use(systemCommandsPlugin, prepared.systemCommands || {});
    }
    [...commandPacks, ...plugins].forEach(plugin => {
      if (Array.isArray(plugin)) terminal.use(plugin[0], plugin[1]);
      else terminal.use(plugin);
    });
    terminal.captureInitialVfsSnapshot();
    if (shouldRestore && terminal.persistence?.load) {
      try {
        terminal.restore(terminal.persistence.load());
      } catch (error) {
        reportDiagnostic(error, { source: 'factory.restore.load' });
      }
    }
    return terminal;
  }
  
  function createWindowsTerminal(options = {}) {
    const shell = options.shell || 'powershell';
    const {
      basicCommands = shell === 'cmd',
      env = {},
      hostname,
      plugins = [],
      systemCommands = shell === 'cmd',
      windowsCommands = {},
      formatPipelineData = formatRecords,
      ...coreOptions
    } = options;
    return createTerminal({
      hostname: hostname || (shell === 'cmd' ? 'DESKTOP' : 'termlet-win'),
      caseInsensitiveCommands: true,
      backslashEscapes: false,
      expandGlobs: shell === 'cmd',
      env: {
        USERPROFILE: 'C:\\Users\\guest',
        ...env,
      },
      basicCommands,
      systemCommands,
      formatPipelineData,
      ...coreOptions,
      plugins: [
        [windowsCommandsPlugin, { shell, ...windowsCommands }],
        ...plugins,
      ],
    });
  }
  
  __exports["createTerminal"] = createTerminal;
  __exports["createWindowsTerminal"] = createWindowsTerminal;
  
};
__modules["src/protocol/session.mjs"] = (__exports, __import) => {
  const { ERR_COMMAND_RUNNING, ERR_INTERNAL, ERR_INVALID_ACTION, ERR_INVALID_STATE, ERR_MODE_UNSUPPORTED, ERR_SNAPSHOT_INVALID, errorEvent } = __import("src/protocol/errors.mjs");
  const { INPUT_MODES, TERMLET_PROTOCOL, isPlainObject, isSerializable, isTerminalAction } = __import("src/protocol/schema.mjs");
  const { createTranscriptStore } = __import("src/state/transcript.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  const DEFAULT_INPUT_MODES = ['line', 'raw', 'password', 'editor', 'select', 'paused'];
  
  function createTerminalSession(terminal, options = {}) {
    if (!terminal || typeof terminal.execute !== 'function') {
      throw new TypeError('createTerminalSession expects a terminal core');
    }
  
    const listeners = new Set();
    const promptFactory = typeof options.prompt === 'function'
      ? options.prompt
      : state => `[${state.user}@${state.hostname} ${formatPromptPath(state.cwd, state.home)}]$`;
    const transcript = createTranscriptStore({
      maxEntries: options.maxTranscriptEntries,
      maxBytes: options.maxTranscriptBytes,
    });
    const persistence = options.persistence || null;
    const state = {
      protocol: TERMLET_PROTOCOL,
      status: 'ready',
      mode: 'line',
      input: '',
      cursor: 0,
      historyIndex: terminal.history?.length || 0,
      runId: null,
    };
    const adapterCapabilities = options.adapterCapabilities || {};
    const supportedInputModes = new Set(
      Array.isArray(adapterCapabilities.inputModes) ? adapterCapabilities.inputModes : DEFAULT_INPUT_MODES,
    );
    const allowConcurrent = Boolean(options.allowConcurrent);
    let runCounter = 0;
    let activeAbort = null;
    let destroyed = false;
  
    const session = {
      dispatch(action) {
        return dispatch(action);
      },
      subscribe(listener) {
        if (typeof listener !== 'function') return () => {};
        listeners.add(listener);
        return () => listeners.delete(listener);
      },
      getState() {
        return publicState();
      },
      snapshot() {
        return snapshot();
      },
      restore(nextSnapshot) {
        return restore(nextSnapshot);
      },
      destroy() {
        destroyed = true;
        state.status = 'destroyed';
        activeAbort?.abort();
        activeAbort = null;
      },
    };
  
    if (options.restore !== false && persistence?.load) {
      try {
        const saved = persistence.load();
        if (isValidSnapshot(saved)) restore(saved);
      } catch (error) {
        reportDiagnostic(error, { source: 'protocol.session.load' });
      }
    }
  
    return session;
  
    async function dispatch(action) {
      if (destroyed) {
        emit(errorEvent(ERR_INVALID_STATE, 'session is destroyed'));
        return false;
      }
      if (!isTerminalAction(action)) {
        emit(errorEvent(ERR_INVALID_ACTION, 'invalid terminal action'));
        return false;
      }
  
      switch (action.type) {
        case 'input.set':
          setInput(String(action.value ?? ''), Number(action.cursor));
          return true;
        case 'input.insert':
          insertInput(String(action.text ?? ''));
          return true;
        case 'input.deleteBackward':
          deleteBackward();
          return true;
        case 'input.clear':
          setInput('', 0);
          return true;
        case 'input.cursor.set':
          setInput(state.input, Number(action.index));
          return true;
        case 'input.raw':
          return handleRawInput(action);
        case 'input.submit':
          return submitInput();
        case 'history.prev':
          return moveHistory(-1);
        case 'history.next':
          return moveHistory(1);
        case 'interrupt':
          return interrupt();
        case 'screen.clear':
          emit({ type: 'screen.cleared' });
          return true;
        case 'mode.set':
          return setMode(action.mode, action.reason || 'action');
        case 'session.reset':
          return resetSession();
        case 'session.restore':
          return restore(action.snapshot);
        default:
          emit(errorEvent(ERR_INVALID_ACTION, 'invalid terminal action'));
          return false;
      }
    }
  
    function setInput(value, cursor = value.length) {
      state.input = String(value ?? '');
      state.cursor = clampCursor(Number.isFinite(cursor) ? cursor : state.input.length, state.input);
      if (state.status === 'ready') state.status = 'editing';
      emit({ type: 'input.changed', value: state.input, cursor: state.cursor, mode: state.mode });
      persist();
    }
  
    function insertInput(text) {
      const left = state.input.slice(0, state.cursor);
      const right = state.input.slice(state.cursor);
      setInput(left + text + right, state.cursor + text.length);
    }
  
    function deleteBackward() {
      if (state.cursor <= 0) return setInput(state.input, state.cursor);
      setInput(state.input.slice(0, state.cursor - 1) + state.input.slice(state.cursor), state.cursor - 1);
    }
  
    function handleRawInput(action) {
      const key = String(action.key || '');
      if (key === 'ArrowLeft') setInput(state.input, state.cursor - 1);
      else if (key === 'ArrowRight') setInput(state.input, state.cursor + 1);
      else if (typeof action.text === 'string' && action.text) insertInput(action.text);
      return true;
    }
  
    async function submitInput() {
      if (state.status === 'running' && !allowConcurrent) {
        emit(errorEvent(ERR_COMMAND_RUNNING, 'command already running'));
        return false;
      }
      const command = state.input;
      const trimmed = command.trim();
      if (!trimmed) {
        setInput('', 0);
        return true;
      }
      const runId = `run-${++runCounter}`;
      const prompt = promptFactory(publicState());
      state.status = 'running';
      state.runId = runId;
      state.input = '';
      state.cursor = 0;
      state.historyIndex = terminal.history?.length || state.historyIndex;
      activeAbort = typeof AbortController === 'function' ? new AbortController() : null;
  
      emit({ type: 'command.submitted', command, runId, prompt });
      emit({ type: 'command.started', command, runId });
  
      let result;
      try {
        result = await terminal.execute(command, { signal: activeAbort?.signal || null });
      } catch (error) {
        emit(errorEvent(ERR_INTERNAL, error?.message || String(error), { runId }));
        result = { status: 1, stdout: '', stderr: `${error?.message || String(error)}\n`, events: [] };
      } finally {
        activeAbort = null;
      }
  
      if (result.stdout) emit({ type: 'output.chunk', command, runId, stream: 'stdout', text: result.stdout });
      if (result.stderr) emit({ type: 'output.chunk', command, runId, stream: 'stderr', text: result.stderr });
      const commandEvents = Array.isArray(result.events) ? safeJson(result.events) : [];
      commandEvents.forEach(event => emitCommandEvent(event, runId, command));
      if (result.status === 130) emit({ type: 'command.interrupted', command, runId });
      emit({
        type: 'command.result',
        command,
        runId,
        status: result.status,
        stdout: result.stdout || '',
        stderr: result.stderr || '',
        data: Array.isArray(result.data) ? result.data : null,
        events: commandEvents,
      });
      const entry = transcript.append({
        type: 'command',
        prompt,
        command,
        status: result.status,
        stdout: result.stdout || '',
        stderr: result.stderr || '',
      });
      if (entry) emit({ type: 'transcript.appended', entry });
      state.status = 'editing';
      state.runId = null;
      state.historyIndex = terminal.history?.length || state.historyIndex;
      emit({ type: 'prompt.changed', prompt: promptFactory(publicState()), ...promptState() });
      persist();
      return true;
    }
  
    function emitCommandEvent(event, runId, command) {
      if (!event || typeof event !== 'object') return;
      if (event.type === 'clear') {
        emit({ type: 'screen.cleared', runId, command });
        return;
      }
      emit({
        type: 'custom',
        namespace: 'termlet.command-event',
        name: sanitizeCustomName(event.type),
        runId,
        command,
        payload: event,
      });
    }
  
    function moveHistory(delta) {
      const history = Array.isArray(terminal.history) ? terminal.history : [];
      if (!history.length) return true;
      state.historyIndex = clamp(state.historyIndex + delta, 0, history.length);
      const value = state.historyIndex >= history.length ? '' : history[state.historyIndex];
      setInput(value, value.length);
      emit({ type: 'history.changed', history: history.slice(), index: state.historyIndex });
      return true;
    }
  
    function interrupt() {
      if (state.status !== 'running' || !activeAbort) return false;
      emit({ type: 'interrupt.received', runId: state.runId });
      activeAbort.abort();
      return true;
    }
  
    function setMode(mode, reason = 'action') {
      const nextMode = String(mode || '');
      if (!INPUT_MODES.has(nextMode) || !supportedInputModes.has(nextMode)) {
        emit(errorEvent(ERR_MODE_UNSUPPORTED, `unsupported input mode: ${nextMode}`));
        return false;
      }
      state.mode = nextMode;
      state.status = nextMode === 'paused' ? 'paused' : (state.status === 'ready' ? 'editing' : state.status);
      emit({ type: 'mode.changed', mode: state.mode, reason: String(reason) });
      persist();
      return true;
    }
  
    function resetSession() {
      terminal.resetSessionState?.();
      transcript.clear();
      state.status = 'ready';
      state.mode = 'line';
      state.input = '';
      state.cursor = 0;
      state.historyIndex = terminal.history?.length || 0;
      state.runId = null;
      persistence?.reset?.();
      emit({ type: 'session.reset', state: publicState() });
      return true;
    }
  
    function snapshot() {
      return {
        protocol: TERMLET_PROTOCOL,
        status: state.status,
        mode: state.mode,
        input: state.input,
        cursor: state.cursor,
        historyIndex: state.historyIndex,
        terminal: typeof terminal.snapshot === 'function' ? terminal.snapshot() : {},
        transcript: transcript.snapshot(),
      };
    }
  
    function restore(nextSnapshot) {
      if (!isValidSnapshot(nextSnapshot)) {
        emit(errorEvent(ERR_SNAPSHOT_INVALID, 'invalid session snapshot'));
        return false;
      }
      const previous = snapshot();
      state.status = 'restoring';
      try {
        if (nextSnapshot.terminal && typeof terminal.restore === 'function') terminal.restore(nextSnapshot.terminal);
        if (nextSnapshot.transcript) transcript.restore(nextSnapshot.transcript);
        state.status = ['ready', 'editing', 'paused'].includes(nextSnapshot.status) ? nextSnapshot.status : 'editing';
        state.mode = INPUT_MODES.has(nextSnapshot.mode) ? nextSnapshot.mode : 'line';
        state.input = String(nextSnapshot.input || '').slice(0, 8000);
        state.cursor = clampCursor(Number(nextSnapshot.cursor || 0), state.input);
        state.historyIndex = Number.isInteger(nextSnapshot.historyIndex) ? nextSnapshot.historyIndex : terminal.history?.length || 0;
        emit({ type: 'session.ready', state: publicState() });
        emit({ type: 'input.changed', value: state.input, cursor: state.cursor, mode: state.mode });
        persist();
        return true;
      } catch (error) {
        reportDiagnostic(error, { source: 'protocol.session.restore' });
        restore(previous);
        emit(errorEvent(ERR_SNAPSHOT_INVALID, 'invalid session snapshot'));
        return false;
      }
    }
  
    function publicState() {
      return {
        protocol: TERMLET_PROTOCOL,
        status: state.status,
        mode: state.mode,
        input: state.input,
        cursor: state.cursor,
        historyIndex: state.historyIndex,
        runId: state.runId,
        ...promptState(),
        prompt: promptFactory({ ...promptState(), input: state.input, mode: state.mode }),
      };
    }
  
    function promptState() {
      return {
        cwd: terminal.cwd,
        home: terminal.home,
        user: terminal.user,
        hostname: terminal.hostname,
      };
    }
  
    function emit(event) {
      const safeEvent = safeJson(event);
      listeners.forEach(listener => {
        try {
          listener(safeEvent);
        } catch (error) {
          reportDiagnostic(error, { source: 'protocol.session.listener' });
        }
      });
    }
  
    function persist() {
      if (!persistence?.save) return;
      try {
        persistence.save(snapshot());
      } catch (error) {
        reportDiagnostic(error, { source: 'protocol.session.persist' });
      }
    }
  }
  
  function isValidSnapshot(snapshot) {
    if (!isPlainObject(snapshot) || !isSerializable(snapshot)) return false;
    if ('version' in snapshot) return false;
    if (typeof snapshot.input !== 'string') return false;
    if (snapshot.mode != null && !INPUT_MODES.has(snapshot.mode)) return false;
    if (snapshot.terminal != null && !isPlainObject(snapshot.terminal)) return false;
    if (snapshot.transcript != null && !isPlainObject(snapshot.transcript)) return false;
    return true;
  }
  
  function clampCursor(index, value) {
    return clamp(Math.trunc(index || 0), 0, String(value || '').length);
  }
  
  function clamp(value, min, max) {
    return Math.min(max, Math.max(min, value));
  }
  
  function safeJson(value) {
    return JSON.parse(JSON.stringify(value));
  }
  
  function sanitizeCustomName(value) {
    return String(value || 'event').replace(/[^-_A-Za-z0-9.]/g, '-').slice(0, 80) || 'event';
  }
  
  function formatPromptPath(path, home) {
    if (path === home) return '~';
    if (path && home && path.startsWith(`${home}/`)) return `~${path.slice(home.length)}`;
    return path || '~';
  }
  
  __exports["createTerminalSession"] = createTerminalSession;
  
};
__modules["src/protocol/errors.mjs"] = (__exports, __import) => {
  const ERR_INVALID_ACTION = 'ERR_INVALID_ACTION';
  const ERR_INVALID_STATE = 'ERR_INVALID_STATE';
  const ERR_COMMAND_NOT_FOUND = 'ERR_COMMAND_NOT_FOUND';
  const ERR_COMMAND_RUNNING = 'ERR_COMMAND_RUNNING';
  const ERR_COMMAND_TIMEOUT = 'ERR_COMMAND_TIMEOUT';
  const ERR_COMMAND_INTERRUPTED = 'ERR_COMMAND_INTERRUPTED';
  const ERR_PERMISSION_DENIED = 'ERR_PERMISSION_DENIED';
  const ERR_PATH_NOT_FOUND = 'ERR_PATH_NOT_FOUND';
  const ERR_SNAPSHOT_INVALID = 'ERR_SNAPSHOT_INVALID';
  const ERR_SNAPSHOT_UNSUPPORTED = 'ERR_SNAPSHOT_UNSUPPORTED';
  const ERR_MODE_UNSUPPORTED = 'ERR_MODE_UNSUPPORTED';
  const ERR_CAPABILITY_DENIED = 'ERR_CAPABILITY_DENIED';
  const ERR_OUTPUT_LIMIT = 'ERR_OUTPUT_LIMIT';
  const ERR_INTERNAL = 'ERR_INTERNAL';
  
  function errorEvent(code, message, extra = {}) {
    return {
      type: 'error',
      code,
      message: String(message || code),
      ...sanitizeExtra(extra),
    };
  }
  
  function sanitizeExtra(extra) {
    if (!extra || typeof extra !== 'object' || Array.isArray(extra)) return {};
    return Object.fromEntries(
      Object.entries(extra)
        .filter(([, value]) => value == null || ['string', 'number', 'boolean'].includes(typeof value))
        .map(([key, value]) => [String(key), value]),
    );
  }
  
  __exports["errorEvent"] = errorEvent;
  __exports["ERR_INVALID_ACTION"] = ERR_INVALID_ACTION;
  __exports["ERR_INVALID_STATE"] = ERR_INVALID_STATE;
  __exports["ERR_COMMAND_NOT_FOUND"] = ERR_COMMAND_NOT_FOUND;
  __exports["ERR_COMMAND_RUNNING"] = ERR_COMMAND_RUNNING;
  __exports["ERR_COMMAND_TIMEOUT"] = ERR_COMMAND_TIMEOUT;
  __exports["ERR_COMMAND_INTERRUPTED"] = ERR_COMMAND_INTERRUPTED;
  __exports["ERR_PERMISSION_DENIED"] = ERR_PERMISSION_DENIED;
  __exports["ERR_PATH_NOT_FOUND"] = ERR_PATH_NOT_FOUND;
  __exports["ERR_SNAPSHOT_INVALID"] = ERR_SNAPSHOT_INVALID;
  __exports["ERR_SNAPSHOT_UNSUPPORTED"] = ERR_SNAPSHOT_UNSUPPORTED;
  __exports["ERR_MODE_UNSUPPORTED"] = ERR_MODE_UNSUPPORTED;
  __exports["ERR_CAPABILITY_DENIED"] = ERR_CAPABILITY_DENIED;
  __exports["ERR_OUTPUT_LIMIT"] = ERR_OUTPUT_LIMIT;
  __exports["ERR_INTERNAL"] = ERR_INTERNAL;
  
};
__modules["src/protocol/schema.mjs"] = (__exports, __import) => {
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  const TERMLET_PROTOCOL = 'termlet.session';
  
  const ACTION_TYPES = new Set([
    'input.set',
    'input.insert',
    'input.deleteBackward',
    'input.clear',
    'input.cursor.set',
    'input.raw',
    'input.submit',
    'history.prev',
    'history.next',
    'interrupt',
    'screen.clear',
    'mode.set',
    'session.reset',
    'session.restore',
  ]);
  
  const EVENT_TYPES = new Set([
    'session.ready',
    'input.changed',
    'command.submitted',
    'command.started',
    'output.chunk',
    'command.progress',
    'command.result',
    'command.interrupted',
    'interrupt.received',
    'mode.changed',
    'prompt.changed',
    'history.changed',
    'transcript.appended',
    'screen.cleared',
    'session.reset',
    'diagnostic',
    'custom',
    'error',
  ]);
  
  const INPUT_MODES = new Set(['line', 'raw', 'password', 'editor', 'select', 'paused']);
  const SESSION_STATUSES = new Set(['ready', 'editing', 'running', 'paused', 'restoring', 'destroyed']);
  
  function isTerminalAction(value) {
    return isPlainObject(value) && ACTION_TYPES.has(value.type) && isSerializable(value);
  }
  
  function isTerminalEvent(value) {
    return isPlainObject(value) && EVENT_TYPES.has(value.type) && isSerializable(value);
  }
  
  function isPlainObject(value) {
    return Boolean(value) && typeof value === 'object' && !Array.isArray(value)
      && (Object.getPrototypeOf(value) === Object.prototype || Object.getPrototypeOf(value) === null);
  }
  
  function isSerializable(value) {
    try {
      JSON.stringify(value);
      return true;
    } catch (error) {
      reportDiagnostic(error, { source: 'protocol.schema.isSerializable' });
      return false;
    }
  }
  
  function getActionSchema() {
    return {
      protocol: TERMLET_PROTOCOL,
      types: [...ACTION_TYPES].sort(),
      requirements: ['plain-object', 'json-serializable', 'no-dom-event', 'no-functions'],
    };
  }
  
  function getEventSchema() {
    return {
      protocol: TERMLET_PROTOCOL,
      types: [...EVENT_TYPES].sort(),
      requirements: ['plain-object', 'json-serializable', 'no-dom-node', 'no-functions', 'no-error-instance'],
    };
  }
  
  function getSnapshotSchema() {
    return {
      protocol: TERMLET_PROTOCOL,
      fields: ['status', 'mode', 'input', 'cursor', 'historyIndex', 'terminal', 'transcript'],
      requirements: ['plain-object', 'json-serializable', 'no-version-field', 'no-functions'],
    };
  }
  
  __exports["isTerminalAction"] = isTerminalAction;
  __exports["isTerminalEvent"] = isTerminalEvent;
  __exports["isPlainObject"] = isPlainObject;
  __exports["isSerializable"] = isSerializable;
  __exports["getActionSchema"] = getActionSchema;
  __exports["getEventSchema"] = getEventSchema;
  __exports["getSnapshotSchema"] = getSnapshotSchema;
  __exports["TERMLET_PROTOCOL"] = TERMLET_PROTOCOL;
  __exports["ACTION_TYPES"] = ACTION_TYPES;
  __exports["EVENT_TYPES"] = EVENT_TYPES;
  __exports["INPUT_MODES"] = INPUT_MODES;
  __exports["SESSION_STATUSES"] = SESSION_STATUSES;
  
};
__modules["src/state/transcript.mjs"] = (__exports, __import) => {
  function createTranscriptStore(options = {}) {
    const maxEntries = Math.max(1, Number(options.maxEntries || 1000));
    const maxBytes = Math.max(1024, Number(options.maxBytes || 256 * 1024));
    let entries = sanitizeEntries(options.entries || [], maxEntries, maxBytes);
  
    return {
      append(entry) {
        const normalized = normalizeTranscriptEntry(entry);
        if (!normalized) return null;
        entries.push(normalized);
        entries = sanitizeEntries(entries, maxEntries, maxBytes);
        return normalized;
      },
      clear() {
        entries = [];
      },
      entries() {
        return entries.map(entry => ({ ...entry }));
      },
      snapshot() {
        return { entries: entries.map(entry => ({ ...entry })) };
      },
      restore(snapshot) {
        const source = Array.isArray(snapshot) ? snapshot : snapshot?.entries;
        if (!Array.isArray(source)) return false;
        entries = sanitizeEntries(source, maxEntries, maxBytes);
        return true;
      },
    };
  }
  
  function normalizeTranscriptEntry(entry) {
    if (!entry || typeof entry !== 'object') return null;
    if (entry.type === 'command') {
      return {
        type: 'command',
        prompt: safeString(entry.prompt, 400),
        command: safeString(entry.command, 4000),
        status: Number.isInteger(entry.status) ? entry.status : 0,
        stdout: safeString(entry.stdout, 20000),
        stderr: safeString(entry.stderr, 20000),
      };
    }
    if (entry.type === 'input') {
      return {
        type: 'input',
        prompt: safeString(entry.prompt, 400),
        command: safeString(entry.command, 4000),
      };
    }
    if (entry.type === 'stdout' || entry.type === 'stderr') {
      return {
        type: entry.type,
        text: safeString(entry.text, 20000),
      };
    }
    if (entry.type === 'event') {
      return {
        type: 'event',
        event: safeJson(entry.event),
      };
    }
    return null;
  }
  
  function sanitizeEntries(source, maxEntries, maxBytes) {
    const entries = source.map(normalizeTranscriptEntry).filter(Boolean).slice(-maxEntries);
    while (entries.length && JSON.stringify(entries).length > maxBytes) entries.shift();
    return entries;
  }
  
  function safeString(value, maxLength) {
    return String(value ?? '').slice(0, maxLength);
  }
  
  function safeJson(value) {
    if (!value || typeof value !== 'object') return {};
    return JSON.parse(JSON.stringify(value));
  }
  
  __exports["createTranscriptStore"] = createTranscriptStore;
  __exports["normalizeTranscriptEntry"] = normalizeTranscriptEntry;
  __exports["sanitizeEntries"] = sanitizeEntries;
  
};
__modules["src/adapters/persistence.mjs"] = (__exports, __import) => {
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  function memoryPersistenceAdapter(initialState = {}) {
    let state = structuredCloneSafe(initialState);
    return {
      load() {
        return structuredCloneSafe(state);
      },
      save(nextState) {
        state = structuredCloneSafe(nextState || {});
      },
      reset() {
        state = {};
      },
    };
  }
  
  function createStorageAdapter(options = {}) {
    const {
      storage = globalThis.localStorage,
      key = 'termlet.state',
    } = options;
  
    if (!storage) return memoryPersistenceAdapter();
  
    return {
      load() {
        try {
          const raw = storage.getItem(key);
          if (!raw) return {};
          const parsed = JSON.parse(raw);
          return parsed && typeof parsed === 'object' ? parsed : {};
        } catch (error) {
          reportDiagnostic(error, { source: 'adapter.persistence.load' });
          return {};
        }
      },
      save(state) {
        try {
          storage.setItem(key, JSON.stringify(state || {}));
        } catch (error) {
          reportDiagnostic(error, { source: 'adapter.persistence.save' });
        }
      },
      reset() {
        try {
          storage.removeItem(key);
        } catch (error) {
          reportDiagnostic(error, { source: 'adapter.persistence.reset' });
        }
      },
    };
  }
  
  function createSessionStorageAdapter(options = {}) {
    return createStorageAdapter({
      key: 'termlet.session',
      ...options,
      storage: options.storage || globalThis.sessionStorage,
    });
  }
  
  function structuredCloneSafe(value) {
    if (typeof structuredClone === 'function') return structuredClone(value);
    return JSON.parse(JSON.stringify(value || {}));
  }
  
  __exports["memoryPersistenceAdapter"] = memoryPersistenceAdapter;
  __exports["createStorageAdapter"] = createStorageAdapter;
  __exports["createSessionStorageAdapter"] = createSessionStorageAdapter;
  
};
__modules["src/adapters/static-site.mjs"] = (__exports, __import) => {
  const { createTerminal } = __import("src/factory.mjs");
  const { createTerminalSession } = __import("src/protocol/session.mjs");
  const { createDomTerminalAdapter, injectDefaultStyles } = __import("src/adapters/dom.mjs");
  async function mountStaticTerminal(options = {}) {
    const {
      mount,
      adapterFactory = createDomTerminalAdapter,
      injectStyles = true,
      terminalOptions = {},
      sessionOptions = {},
      adapterOptions = {},
      plugins = [],
    } = options;
  
    if (!mount) throw new Error('mountStaticTerminal requires a mount element or selector');
    if (injectStyles) injectDefaultStyles(options.document || globalThis.document);
  
    const terminal = createTerminal({
      ...terminalOptions,
      plugins: [
        ...(terminalOptions.plugins || []),
        ...plugins,
      ],
    });
  
    const session = createTerminalSession(terminal, sessionOptions);
    const adapter = adapterFactory({
      mount,
      document: options.document,
      ...adapterOptions,
    }).mount(session);
  
    return { terminal, session, adapter };
  }
  
  __exports["mountStaticTerminal"] = mountStaticTerminal;
  
};
__modules["src/adapters/feed.mjs"] = (__exports, __import) => {
  const { createTerminal } = __import("src/factory.mjs");
  const { createTerminalSession } = __import("src/protocol/session.mjs");
  const { createDomTerminalAdapter, injectDefaultStyles } = __import("src/adapters/dom.mjs");
  const { feedPostsPlugin, fetchDiscoveredFeedPosts, fetchFeedPosts } = __import("src/plugins/feed-posts.mjs");
  const { blogSandboxPreset } = __import("src/presets/blog-sandbox.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  async function createFeedTerminal(options = {}) {
    const {
      feedUrl = '',
      fetchImpl = globalThis.fetch,
      includeBlogPreset = true,
      posts,
      plugins = [],
      terminalOptions = {},
    } = options;
  
    const loadedPosts = posts || await loadPosts({
      feedUrl,
      fetchImpl,
      document: options.document,
      baseUrl: options.baseUrl,
    }).catch(error => {
      reportDiagnostic(error, { source: 'adapter.feed.loadPosts' });
      return [];
    });
  
    return createTerminal({
      hostname: 'blog-server',
      ...terminalOptions,
      plugins: [
        ...(includeBlogPreset ? [blogSandboxPreset(options.blogPreset || {})] : []),
        feedPostsPlugin(loadedPosts, options.feedPosts || {}),
        ...(terminalOptions.plugins || []),
        ...plugins,
      ],
    });
  }
  
  async function mountFeedTerminal(options = {}) {
    const {
      mount,
      adapterFactory = createDomTerminalAdapter,
      injectStyles = true,
      sessionOptions = {},
      adapterOptions = {},
    } = options;
  
    if (!mount) throw new Error('mountFeedTerminal requires a mount element or selector');
    if (injectStyles) injectDefaultStyles(options.document || globalThis.document);
  
    const terminal = await createFeedTerminal(options);
    const session = createTerminalSession(terminal, sessionOptions);
    const adapter = adapterFactory({
      mount,
      document: options.document,
      ...adapterOptions,
    }).mount(session);
  
    return { terminal, session, adapter };
  }
  
  function loadPosts(options) {
    if (options.feedUrl) return fetchFeedPosts(options.feedUrl, options.fetchImpl);
    return fetchDiscoveredFeedPosts(options);
  }
  
  __exports["createFeedTerminal"] = createFeedTerminal;
  __exports["mountFeedTerminal"] = mountFeedTerminal;
  
};
__modules["src/adapters/hugo.mjs"] = (__exports, __import) => {
  const { createTerminal } = __import("src/factory.mjs");
  const { createTerminalSession } = __import("src/protocol/session.mjs");
  const { createDomTerminalAdapter, injectDefaultStyles } = __import("src/adapters/dom.mjs");
  const { fetchHugoPosts, hugoPostsPlugin } = __import("src/plugins/hugo-adapter.mjs");
  const { blogSandboxPreset } = __import("src/presets/blog-sandbox.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  async function createHugoTerminal(options = {}) {
    const {
      feedUrl = '/index.xml',
      fetchImpl = globalThis.fetch,
      includeBlogPreset = true,
      posts,
      plugins = [],
      terminalOptions = {},
    } = options;
  
    const loadedPosts = posts || await fetchHugoPosts(feedUrl, fetchImpl).catch(error => {
      reportDiagnostic(error, { source: 'adapter.hugo.fetchPosts' });
      return [];
    });
    return createTerminal({
      hostname: 'blog-server',
      ...terminalOptions,
      plugins: [
        ...(includeBlogPreset ? [blogSandboxPreset(options.blogPreset || {})] : []),
        hugoPostsPlugin(loadedPosts, options.hugoPosts || {}),
        ...(terminalOptions.plugins || []),
        ...plugins,
      ],
    });
  }
  
  async function mountHugoTerminal(options = {}) {
    const {
      mount,
      adapterFactory = createDomTerminalAdapter,
      injectStyles = true,
      sessionOptions = {},
      adapterOptions = {},
    } = options;
  
    if (!mount) throw new Error('mountHugoTerminal requires a mount element or selector');
    if (injectStyles) injectDefaultStyles(options.document || globalThis.document);
  
    const terminal = await createHugoTerminal(options);
    const session = createTerminalSession(terminal, sessionOptions);
    const adapter = adapterFactory({
      mount,
      document: options.document,
      ...adapterOptions,
    }).mount(session);
  
    return { terminal, session, adapter };
  }
  
  __exports["createHugoTerminal"] = createHugoTerminal;
  __exports["mountHugoTerminal"] = mountHugoTerminal;
  
};
__modules["src/toolbox/completion.mjs"] = (__exports, __import) => {
  function createCompletionEngine() {
    let providers = [];
    let disposed = false;
    let requestId = 0;
  
    return {
      registerProvider(provider) {
        if (disposed) return () => {};
        const normalized = normalizeProvider(provider);
        providers.push(normalized);
        providers = sortProviders(providers);
        return () => {
          providers = providers.filter(item => item !== normalized);
        };
      },
      async complete(context = {}) {
        if (disposed) return { requestId: ++requestId, items: [], diagnostics: [] };
        const currentRequest = ++requestId;
        const items = [];
        const diagnostics = [];
        for (const provider of providers) {
          try {
            const provided = await provider.provide({ ...context, requestId: currentRequest });
            normalizeItems(provided).forEach(item => items.push(item));
          } catch (error) {
            diagnostics.push({
              type: 'completion.providerFailed',
              name: provider.name,
              message: String(error?.message || error || 'provider failed'),
            });
          }
        }
        return { requestId: currentRequest, items, diagnostics };
      },
      providers() {
        return providers.map(provider => ({ name: provider.name, priority: provider.priority }));
      },
      dispose() {
        disposed = true;
        providers = [];
      },
    };
  }
  
  function normalizeProvider(provider = {}) {
    if (typeof provider.provide !== 'function') {
      throw new TypeError('completion provider requires provide(context)');
    }
    return {
      name: sanitizeName(provider.name || 'completion-provider'),
      priority: clampPriority(provider.priority),
      provide: provider.provide,
    };
  }
  
  function normalizeItems(items) {
    if (!Array.isArray(items)) return [];
    return items
      .filter(item => item && typeof item === 'object')
      .map(item => ({
        label: String(item.label || '').slice(0, 200),
        kind: String(item.kind || 'value').slice(0, 40),
        detail: item.detail == null ? undefined : String(item.detail).slice(0, 500),
        insertText: item.insertText == null ? undefined : String(item.insertText).slice(0, 500),
      }))
      .filter(item => item.label);
  }
  
  function sortProviders(values) {
    return [...values].sort((left, right) => (
      right.priority - left.priority || left.name.localeCompare(right.name)
    ));
  }
  
  function clampPriority(value) {
    const number = Number(value || 0);
    if (!Number.isFinite(number)) return 0;
    return Math.max(-1000, Math.min(1000, Math.trunc(number)));
  }
  
  function sanitizeName(value) {
    return String(value).replace(/[^A-Za-z0-9_.-]/g, '-').slice(0, 80) || 'completion-provider';
  }
  
  __exports["createCompletionEngine"] = createCompletionEngine;
  
};
__modules["src/toolbox/output-stream.mjs"] = (__exports, __import) => {
  function createOutputStreamController(options = {}) {
    const maxBytes = Math.max(1, Number(options.maxBytes || 256 * 1024));
    const runs = new Map();
  
    return {
      push(chunk = {}) {
        const runId = String(chunk.runId || 'default');
        const stream = chunk.stream === 'stderr' ? 'stderr' : 'stdout';
        const run = runs.get(runId) || { stdout: '', stderr: '', truncated: false };
        const current = run[stream];
        const next = current + String(chunk.text ?? '');
        const limited = limitText(next, maxBytes);
        run[stream] = limited.text;
        run.truncated = run.truncated || limited.truncated;
        runs.set(runId, run);
        return { runId, stream, text: String(chunk.text ?? ''), truncated: run.truncated };
      },
      result(runId = 'default') {
        const run = runs.get(String(runId));
        if (!run) return { stdout: '', stderr: '', truncated: false };
        return { ...run };
      },
      cancel(runId = 'default') {
        runs.delete(String(runId));
      },
      clear() {
        runs.clear();
      },
    };
  }
  
  const textEncoder = new TextEncoder();
  
  function limitText(value, maxBytes) {
    const text = String(value ?? '');
    if (byteLength(text) <= maxBytes) return { text, truncated: false };
    let out = '';
    for (const char of text) {
      if (byteLength(out + char) > maxBytes) break;
      out += char;
    }
    return { text: `${out.replace(/\s+$/, '')}\n[output truncated]`, truncated: true };
  }
  
  function byteLength(value) {
    return textEncoder.encode(String(value ?? '')).length;
  }
  
  __exports["createOutputStreamController"] = createOutputStreamController;
  
};
__modules["src/toolbox/mode-machine.mjs"] = (__exports, __import) => {
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  const DEFAULT_MODES = ['line', 'raw', 'password', 'editor', 'select', 'paused'];
  
  function createInteractionModeMachine(options = {}) {
    const supportedModes = new Set(options.supportedModes || DEFAULT_MODES);
    const listeners = new Set();
    let mode = supportedModes.has(options.initialMode) ? options.initialMode : 'line';
  
    return {
      current() {
        return mode;
      },
      setMode(nextMode, reason = 'action') {
        const normalized = String(nextMode || '');
        if (!supportedModes.has(normalized)) {
          emit({ type: 'mode.rejected', mode: normalized, reason: String(reason) });
          return false;
        }
        mode = normalized;
        emit({ type: 'mode.changed', mode, reason: String(reason) });
        return true;
      },
      reset(reason = 'reset') {
        mode = 'line';
        emit({ type: 'mode.changed', mode, reason });
      },
      subscribe(listener) {
        if (typeof listener !== 'function') return () => {};
        listeners.add(listener);
        return () => listeners.delete(listener);
      },
      dispose() {
        listeners.clear();
        mode = 'line';
      },
    };
  
    function emit(event) {
      const safeEvent = JSON.parse(JSON.stringify(event));
      listeners.forEach(listener => {
        try {
          listener(safeEvent);
        } catch (error) {
          reportDiagnostic(error, { source: 'toolbox.mode.listener' });
        }
      });
    }
  }
  
  __exports["createInteractionModeMachine"] = createInteractionModeMachine;
  
};
__modules["src/toolbox/format.mjs"] = (__exports, __import) => {
  const __reexport1 = __import("src/extension.mjs");
  __exports["formatRecords"] = __reexport1["formatRecords"];
  function formatJson(value, options = {}) {
    const spaces = Math.max(0, Math.min(8, Number(options.spaces ?? 2)));
    return `${JSON.stringify(value ?? null, null, spaces)}\n`;
  }
  
  function formatTree(tree, options = {}) {
    const rootLabel = options.rootLabel || '';
    const lines = [];
    if (rootLabel) lines.push(String(rootLabel));
    renderNode(tree, '', lines);
    return lines.join('\n') + (lines.length ? '\n' : '');
  }
  
  function renderNode(value, prefix, lines) {
    if (!value || typeof value !== 'object') return;
    const entries = Object.entries(value);
    entries.forEach(([key, child], index) => {
      const last = index === entries.length - 1;
      const branch = last ? '`-- ' : '|-- ';
      lines.push(`${prefix}${branch}${key}`);
      if (child && typeof child === 'object') {
        renderNode(child, `${prefix}${last ? '    ' : '|   '}`, lines);
      }
    });
  }
  
  __exports["formatJson"] = formatJson;
  __exports["formatTree"] = formatTree;
  
};
__modules["src/toolbox/capability.mjs"] = (__exports, __import) => {
  const { ERR_CAPABILITY_DENIED } = __import("src/protocol/errors.mjs");
  function createCapabilityBroker(initial = {}) {
    const capabilities = new Map();
    const auditEntries = [];
    Object.entries(initial || {}).forEach(([name, value]) => {
      if (value) capabilities.set(sanitizeName(name), value);
    });
  
    return {
      register(name, capability) {
        const key = sanitizeName(name);
        if (!key) throw new TypeError('capability name is required');
        capabilities.set(key, capability === true ? () => true : capability);
        return () => capabilities.delete(key);
      },
      has(name) {
        return capabilities.has(sanitizeName(name));
      },
      request(name, context = {}) {
        const key = sanitizeName(name);
        const capability = capabilities.get(key);
        if (!capability) {
          const denied = { ok: false, code: ERR_CAPABILITY_DENIED, name: key };
          auditEntries.push({ name: key, ok: false, code: denied.code });
          return denied;
        }
        try {
          const value = typeof capability === 'function' ? capability(context) : capability;
          auditEntries.push({ name: key, ok: true });
          return { ok: true, name: key, value };
        } catch (error) {
          auditEntries.push({ name: key, ok: false, code: ERR_CAPABILITY_DENIED });
          return { ok: false, code: ERR_CAPABILITY_DENIED, name: key, message: String(error?.message || error) };
        }
      },
      audit() {
        return auditEntries.map(entry => ({ ...entry }));
      },
      dispose() {
        capabilities.clear();
        auditEntries.splice(0);
      },
    };
  }
  
  function sanitizeName(value) {
    return String(value || '').replace(/[^A-Za-z0-9_.-]/g, '').slice(0, 80);
  }
  
  __exports["createCapabilityBroker"] = createCapabilityBroker;
  
};
__modules["src/toolbox/visual.mjs"] = (__exports, __import) => {
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  const SAFE_CLASS = /^[-_A-Za-z0-9 ]{0,160}$/;
  
  function createLayer(mount, options = {}) {
    const doc = options.document || mount?.ownerDocument || globalThis.document;
    if (!mount || !doc) throw new Error('createLayer requires a mount element');
    const root = doc.createElement(options.tagName || 'div');
    const name = sanitizeName(options.name || 'layer');
    root.className = joinClasses('termlet-layer', `termlet-layer--${name}`, options.className);
    root.setAttribute?.('data-termlet-layer', name);
    if (options.ariaHidden !== false) root.setAttribute?.('aria-hidden', 'true');
    if (options.append !== false) mount.appendChild(root);
    const maxNodes = Math.max(1, Number(options.maxNodes || 512));
    let destroyed = false;
  
    const layer = {
      root,
      append(value, nodeOptions = {}) {
        if (destroyed) return null;
        const node = normalizeNode(doc, value, nodeOptions);
        root.appendChild(node);
        trim(root, maxNodes);
        return node;
      },
      text(value, nodeOptions = {}) {
        return layer.append(String(value ?? ''), nodeOptions);
      },
      clear() {
        root.textContent = '';
      },
      destroy() {
        destroyed = true;
        root.remove?.();
        if (root.parentNode) root.parentNode.removeChild(root);
      },
    };
    return layer;
  }
  
  function createVisualHost(mount, options = {}) {
    if (!mount) throw new Error('createVisualHost requires a mount element');
    const doc = options.document || mount.ownerDocument || globalThis.document;
    const layers = new Map();
    const timelines = new Set();
    const disposers = new Set();
    const className = options.className || 'termlet-visual-host';
    let destroyed = false;
  
    mount.classList?.add?.(className);
  
    const host = {
      mount,
      layers,
      layer(name = 'fx', layerOptions = {}) {
        const key = sanitizeName(name);
        if (!layers.has(key)) {
          layers.set(key, createLayer(mount, {
            document: doc,
            name: key,
            className: layerOptions.className || `${className}__layer ${className}__layer--${key}`,
            ...layerOptions,
          }));
        }
        return layers.get(key);
      },
      timeline(timelineOptions = {}) {
        const timeline = createTimeline(timelineOptions);
        timelines.add(timeline);
        return timeline;
      },
      emitText(layerName, text, emitOptions = {}) {
        const layer = host.layer(layerName, emitOptions.layer || {});
        const tokens = tokenizeText(text, emitOptions);
        const nodes = [];
        tokens.forEach((token, index) => {
          const node = layer.text(token.text, {
            kind: emitOptions.kind || token.kind,
            className: emitOptions.className,
            tagName: emitOptions.tagName,
          });
          if (!node) return;
          emitOptions.decorate?.(node, token, index, tokens, host);
          nodes.push(node);
        });
        return nodes;
      },
      emitPathText(layerName, text, path, emitOptions = {}) {
        const layer = host.layer(layerName, emitOptions.layer || {});
        const layout = layoutTextPath(text, path, emitOptions);
        const nodes = [];
        layout.forEach((entry, index) => {
          const node = layer.text(entry.text, {
            kind: emitOptions.kind || entry.kind || 'token',
            className: emitOptions.className,
            tagName: emitOptions.tagName,
          });
          if (!node) return;
          applyPathLayout(node, entry, index);
          emitOptions.decorate?.(node, entry, index, layout, host);
          nodes.push(node);
        });
        return nodes;
      },
      bind(session, handlers = {}) {
        if (!session || typeof session.subscribe !== 'function') return () => {};
        const unsubscribe = session.subscribe(event => {
          if (destroyed) return;
          const handler = handlers[event.type] || handlers['*'];
          handler?.(event, host);
        });
        const dispose = () => {
          unsubscribe();
          disposers.delete(dispose);
        };
        disposers.add(dispose);
        return dispose;
      },
      cleanup(disposer) {
        if (typeof disposer !== 'function') return () => {};
        disposers.add(disposer);
        return () => {
          disposer();
          disposers.delete(disposer);
        };
      },
      destroy() {
        destroyed = true;
        disposers.forEach(dispose => {
          try {
            dispose();
          } catch (error) {
            reportDiagnostic(error, { source: 'toolbox.visual.destroy' });
          }
        });
        disposers.clear();
        timelines.forEach(timeline => timeline.destroy?.());
        timelines.clear();
        layers.forEach(layer => layer.destroy?.());
        layers.clear();
        mount.classList?.remove?.(className);
      },
    };
  
    return host;
  }
  
  function layoutTextPath(text, path, options = {}) {
    const source = String(text ?? '');
    const maxTokens = Math.max(0, Number(options.maxTokens || 256));
    const advance = Math.max(1, Number(options.advance || 13));
    const spaceAdvance = Math.max(1, Number(options.spaceAdvance || advance * 1.65));
    const start = Number(options.start || 0);
    const sample = resolvePathSampler(path);
    const chars = [...source].slice(0, Math.max(maxTokens * 2, maxTokens));
    const entries = [];
    let distance = start;
    let inWord = false;
    let wordIndex = -1;
    let wordStartDistance = start;
    let wordCharIndex = 0;
    let currentWord = [];
    const finishWord = () => {
      if (!currentWord.length) return;
      const wordWidth = currentWord[currentWord.length - 1].charOffset;
      const wordCenterOffset = wordWidth / 2;
      const wordCenterDistance = currentWord[0].wordStartDistance + wordCenterOffset;
      currentWord.forEach(entry => {
        entry.wordWidth = wordWidth;
        entry.wordCenterOffset = wordCenterOffset;
        entry.wordCenterDistance = wordCenterDistance;
      });
      currentWord = [];
    };
    for (const [sourceIndex, char] of chars.entries()) {
      if (entries.length >= maxTokens) break;
      if (/\s/.test(char)) {
        if (inWord) {
          finishWord();
          inWord = false;
        }
        distance += spaceAdvance;
        continue;
      }
      if (!inWord) {
        inWord = true;
        wordIndex += 1;
        wordStartDistance = distance;
        wordCharIndex = 0;
      }
      const token = { text: char, index: entries.length, sourceIndex, distance };
      const point = normalizePoint(sample(distance, token, {
        text: source,
        options,
        index: entries.length,
        maxTokens,
      }));
      const entry = {
        ...token,
        ...point,
        wordIndex,
        wordCharIndex,
        wordStartDistance,
        wordCenterDistance: wordStartDistance,
        charOffset: distance - wordStartDistance,
        wordWidth: 0,
        wordCenterOffset: 0,
        kind: options.kind || 'token',
      };
      entries.push(entry);
      currentWord.push(entry);
      wordCharIndex += 1;
      distance += advance;
    }
    finishWord();
    return entries;
  }
  
  function createTimeline(options = {}) {
    const controllers = new Set();
    const duration = Math.max(0, Number(options.duration ?? 240));
    const reducedMotion = Boolean(options.reducedMotion);
    let destroyed = false;
  
    return {
      animate(node, frames = [], timing = {}) {
        if (destroyed || !node) return settledAnimation();
        const lastFrame = Array.isArray(frames) && frames.length ? frames[frames.length - 1] : {};
        const controller = typeof node.animate === 'function' && !reducedMotion
          ? node.animate(frames, { duration, fill: 'forwards', ...timing })
          : fallbackAnimation(node, lastFrame);
        controllers.add(controller);
        Promise.resolve(controller.finished).finally(() => controllers.delete(controller));
        return controller;
      },
      destroy() {
        destroyed = true;
        controllers.forEach(controller => controller.cancel?.());
        controllers.clear();
      },
    };
  }
  
  function tokenizeText(text, options = {}) {
    const source = String(text ?? '');
    const maxTokens = Math.max(0, Number(options.maxTokens || 256));
    const mode = options.mode || options.split || 'words';
    const raw = mode === 'chars'
      ? [...source]
      : source.match(/\S+|\s+/g) || [];
    return raw
      .filter(token => mode === 'chars' || /\S/.test(token))
      .slice(0, maxTokens)
      .map((text, index) => ({
        text,
        index,
        kind: /\s/.test(text) ? 'space' : 'token',
      }));
  }
  
  function createPath(options = {}) {
    const type = options.type || 'line';
    if (type === 'orbit') {
      const cx = Number(options.cx ?? 0);
      const cy = Number(options.cy ?? 0);
      const rx = Math.max(1, Number(options.rx ?? options.radius ?? 120));
      const ry = Math.max(1, Number(options.ry ?? options.radius ?? 80));
      const step = Number(options.step ?? 0.055);
      const phase = Number(options.phase ?? 0);
      return distance => {
        const angle = phase + distance * step;
        return {
          x: cx + Math.cos(angle) * rx,
          y: cy + Math.sin(angle) * ry,
          angle: angle * 180 / Math.PI + 90,
        };
      };
    }
    if (type === 'sine') {
      const x = Number(options.x ?? 0);
      const y = Number(options.y ?? 0);
      const amplitude = Number(options.amplitude ?? 32);
      const frequency = Number(options.frequency ?? 0.028);
      const slope = Number(options.slope ?? 0);
      return distance => {
        const wave = Math.sin(distance * frequency);
        const nextWave = Math.sin((distance + 1) * frequency);
        return {
          x: x + distance,
          y: y + wave * amplitude + distance * slope,
          angle: Math.atan2((nextWave - wave) * amplitude + slope, 1) * 180 / Math.PI,
        };
      };
    }
    if (type === 'spiral') {
      const cx = Number(options.cx ?? 0);
      const cy = Number(options.cy ?? 0);
      const startRadius = Math.max(0, Number(options.startRadius ?? 20));
      const growth = Number(options.growth ?? 0.18);
      const step = Number(options.step ?? 0.045);
      const phase = Number(options.phase ?? 0);
      return distance => {
        const angle = phase + distance * step;
        const radius = startRadius + distance * growth;
        return {
          x: cx + Math.cos(angle) * radius,
          y: cy + Math.sin(angle) * radius,
          angle: angle * 180 / Math.PI + 90,
        };
      };
    }
    const x = Number(options.x ?? 0);
    const y = Number(options.y ?? 0);
    const angle = Number(options.angle ?? 0);
    const radians = angle * Math.PI / 180;
    return distance => ({
      x: x + Math.cos(radians) * distance,
      y: y + Math.sin(radians) * distance,
      angle,
    });
  }
  
  function getBounds(element) {
    const rect = element?.getBoundingClientRect?.();
    if (!rect) return { x: 0, y: 0, width: 0, height: 0 };
    return {
      x: Number(rect.x || rect.left || 0),
      y: Number(rect.y || rect.top || 0),
      width: Number(rect.width || 0),
      height: Number(rect.height || 0),
    };
  }
  
  function normalizeNode(doc, value, options) {
    if (value && typeof value === 'object' && 'nodeType' in value) return value;
    const tagName = options.tagName || 'span';
    const node = doc.createElement(tagName);
    node.className = joinClasses('termlet-layer-node', options.kind ? `termlet-layer-node--${sanitizeName(options.kind)}` : '', options.className);
    node.textContent = String(value ?? '');
    return node;
  }
  
  function fallbackAnimation(node, frame) {
    applyFrame(node, frame);
    return settledAnimation();
  }
  
  function settledAnimation() {
    return {
      finished: Promise.resolve(),
      cancel() {},
    };
  }
  
  function applyFrame(node, frame = {}) {
    if (!node.style || !frame || typeof frame !== 'object') return;
    Object.entries(frame).forEach(([key, value]) => {
      if (/^[A-Za-z][A-Za-z0-9-]{0,80}$/.test(key)) node.style[key] = String(value);
    });
  }
  
  function applyPathLayout(node, entry, index) {
    node.style?.setProperty?.('--termlet-path-x', `${round(entry.x)}px`);
    node.style?.setProperty?.('--termlet-path-y', `${round(entry.y)}px`);
    node.style?.setProperty?.('--termlet-path-angle', `${round(entry.angle || 0)}deg`);
    node.style?.setProperty?.('--termlet-path-scale', String(round(entry.scale ?? 1)));
    node.style?.setProperty?.('--termlet-path-opacity', String(round(entry.opacity ?? 1)));
    node.style?.setProperty?.('--termlet-path-index', String(index));
    node.style?.setProperty?.('--i', String(index));
    if (!node.style) return;
    node.style.transform = `translate3d(var(--termlet-path-x), var(--termlet-path-y), 0) rotate(var(--termlet-path-angle)) scale(var(--termlet-path-scale))`;
    node.style.opacity = 'var(--termlet-path-opacity)';
  }
  
  function resolvePathSampler(path) {
    if (typeof path === 'function') return path;
    if (path && typeof path.sample === 'function') return path.sample.bind(path);
    return createPath(path || {});
  }
  
  function normalizePoint(value) {
    const point = value && typeof value === 'object' ? value : {};
    return {
      x: Number(point.x || 0),
      y: Number(point.y || 0),
      angle: Number(point.angle || 0),
      scale: Number(point.scale ?? 1),
      opacity: Number(point.opacity ?? 1),
    };
  }
  
  function round(value) {
    return Math.round(Number(value || 0) * 1000) / 1000;
  }
  
  function trim(root, maxNodes) {
    while (root.childNodes.length > maxNodes) root.removeChild(root.firstChild);
  }
  
  function joinClasses(...items) {
    return items
      .flatMap(item => String(item || '').split(/\s+/))
      .map(item => SAFE_CLASS.test(item) ? item : '')
      .filter(Boolean)
      .join(' ');
  }
  
  function sanitizeName(value) {
    return String(value || 'layer').replace(/[^-_A-Za-z0-9]/g, '-').slice(0, 80) || 'layer';
  }
  
  __exports["createLayer"] = createLayer;
  __exports["createVisualHost"] = createVisualHost;
  __exports["layoutTextPath"] = layoutTextPath;
  __exports["createTimeline"] = createTimeline;
  __exports["tokenizeText"] = tokenizeText;
  __exports["createPath"] = createPath;
  __exports["getBounds"] = getBounds;
  
};
__modules["src/testkit/contract.mjs"] = (__exports, __import) => {
  const { composeExtensions } = __import("src/extension.mjs");
  const { reportDiagnostic } = __import("src/diagnostics.mjs");
  function createSessionContractTests(options = {}) {
    return {
      async run() {
        const diagnostics = [];
        const session = options.createSession?.();
        if (!session || typeof session.dispatch !== 'function' || typeof session.subscribe !== 'function') {
          return failReport('session.missing', diagnostics);
        }
        const events = [];
        const unsubscribe = session.subscribe(event => events.push(event));
        await session.dispatch({ type: 'input.set', value: 'echo contract' });
        await session.dispatch({ type: 'input.submit' });
        unsubscribe();
        if (!events.some(event => event.type === 'command.result')) diagnostics.push({ type: 'session.noResult' });
        if (!events.every(isSerializable)) diagnostics.push({ type: 'session.nonSerializableEvent' });
        const snapshot = session.snapshot();
        if (!isSerializable(snapshot)) diagnostics.push({ type: 'session.nonSerializableSnapshot' });
        if (JSON.stringify(snapshot).includes('version')) diagnostics.push({ type: 'session.versionField' });
        return { ok: diagnostics.length === 0, diagnostics };
      },
    };
  }
  
  function createAdapterContractTests(options = {}) {
    return {
      async run() {
        const diagnostics = [];
        const session = options.createSession?.();
        const mountContext = options.createMount?.() || {};
        const adapter = options.createAdapter?.(mountContext);
        if (!adapter || typeof adapter.mount !== 'function' || typeof adapter.destroy !== 'function') {
          return failReport('adapter.missing', diagnostics);
        }
        adapter.mount(session);
        await session.dispatch({ type: 'input.set', value: 'echo adapter' });
        await session.dispatch({ type: 'input.submit' });
        const beforeDestroy = mountContext.mount?.textContent || '';
        adapter.destroy();
        await session.dispatch({ type: 'input.set', value: 'echo after' });
        const afterDestroy = mountContext.mount?.textContent || '';
        if (!beforeDestroy.includes('adapter')) diagnostics.push({ type: 'adapter.noOutput' });
        if (afterDestroy !== beforeDestroy) diagnostics.push({ type: 'adapter.leakedAfterDestroy' });
        return { ok: diagnostics.length === 0, diagnostics };
      },
    };
  }
  
  function createExtensionContractTests(options = {}) {
    return {
      run() {
        const graph = composeExtensions(options.extensions || [], {
          capabilities: options.capabilities || {},
        });
        const diagnostics = [];
        if (!Array.isArray(graph.activeExtensions)) diagnostics.push({ type: 'extension.noActiveList' });
        if (!graph.contributions || typeof graph.contributions !== 'object') diagnostics.push({ type: 'extension.noContributions' });
        if (!Array.isArray(graph.diagnostics)) diagnostics.push({ type: 'extension.noDiagnostics' });
        return { ok: diagnostics.length === 0, diagnostics: diagnostics.concat(graph.diagnostics || []) };
      },
    };
  }
  
  function failReport(type, diagnostics) {
    return { ok: false, diagnostics: diagnostics.concat({ type }) };
  }
  
  function isSerializable(value) {
    try {
      JSON.stringify(value);
      return true;
    } catch (error) {
      reportDiagnostic(error, { source: 'testkit.contract.isSerializable' });
      return false;
    }
  }
  
  __exports["createSessionContractTests"] = createSessionContractTests;
  __exports["createAdapterContractTests"] = createAdapterContractTests;
  __exports["createExtensionContractTests"] = createExtensionContractTests;
  
};
function __import(id) {
  if (__cache[id]) return __cache[id];
  const factory = __modules[id];
  if (!factory) throw new Error(`Termlet bundle missing module: ${id}`);
  const exports = Object.create(null);
  __cache[id] = exports;
  factory(exports, __import);
  return exports;
}
const __entry = __import("src/index.mjs");
globalThis.Termlet = __entry;
export const TerminalCore = __entry["TerminalCore"];
export const composeExtensions = __entry["composeExtensions"];
export const defineCommandPack = __entry["defineCommandPack"];
export const defineExtension = __entry["defineExtension"];
export const defineProfile = __entry["defineProfile"];
export const filterRecords = __entry["filterRecords"];
export const formatRecords = __entry["formatRecords"];
export const getExtensionDiagnostics = __entry["getExtensionDiagnostics"];
export const getRecordValue = __entry["getRecordValue"];
export const mergeProfileOptions = __entry["mergeProfileOptions"];
export const normalizeProperties = __entry["normalizeProperties"];
export const projectRecords = __entry["projectRecords"];
export const sortRecords = __entry["sortRecords"];
export const validateExtension = __entry["validateExtension"];
export const MemoryFileSystem = __entry["MemoryFileSystem"];
export const VfsError = __entry["VfsError"];
export const createLinuxLikeFs = __entry["createLinuxLikeFs"];
export const onDiagnostic = __entry["onDiagnostic"];
export const reportDiagnostic = __entry["reportDiagnostic"];
export const withDiagnostic = __entry["withDiagnostic"];
export const basicCommandsPlugin = __entry["basicCommandsPlugin"];
export const systemCommandsPlugin = __entry["systemCommandsPlugin"];
export const effectEventsPlugin = __entry["effectEventsPlugin"];
export const windowsCommandsPlugin = __entry["windowsCommandsPlugin"];
export const toWindowsPath = __entry["toWindowsPath"];
export const fromWindowsPath = __entry["fromWindowsPath"];
export const feedPostsPlugin = __entry["feedPostsPlugin"];
export const fetchFeedPosts = __entry["fetchFeedPosts"];
export const fetchDiscoveredFeedPosts = __entry["fetchDiscoveredFeedPosts"];
export const parseFeedPosts = __entry["parseFeedPosts"];
export const discoverFeedUrl = __entry["discoverFeedUrl"];
export const hugoPostsPlugin = __entry["hugoPostsPlugin"];
export const fetchHugoPosts = __entry["fetchHugoPosts"];
export const blogSandboxPreset = __entry["blogSandboxPreset"];
export const DEFAULT_TERMINAL_CSS = __entry["DEFAULT_TERMINAL_CSS"];
export const createDomTerminalAdapter = __entry["createDomTerminalAdapter"];
export const injectDefaultStyles = __entry["injectDefaultStyles"];
export const mountStarterTerminal = __entry["mountStarterTerminal"];
export const mountStaticTerminal = __entry["mountStaticTerminal"];
export const createFeedTerminal = __entry["createFeedTerminal"];
export const mountFeedTerminal = __entry["mountFeedTerminal"];
export const createHugoTerminal = __entry["createHugoTerminal"];
export const mountHugoTerminal = __entry["mountHugoTerminal"];
export const createSessionStorageAdapter = __entry["createSessionStorageAdapter"];
export const createStorageAdapter = __entry["createStorageAdapter"];
export const memoryPersistenceAdapter = __entry["memoryPersistenceAdapter"];
export const ok = __entry["ok"];
export const fail = __entry["fail"];
export const normalizeResult = __entry["normalizeResult"];
export const createTerminal = __entry["createTerminal"];
export const createWindowsTerminal = __entry["createWindowsTerminal"];
export const TERMLET_PROTOCOL = __entry["TERMLET_PROTOCOL"];
export const getActionSchema = __entry["getActionSchema"];
export const getEventSchema = __entry["getEventSchema"];
export const getSnapshotSchema = __entry["getSnapshotSchema"];
export const isTerminalAction = __entry["isTerminalAction"];
export const isTerminalEvent = __entry["isTerminalEvent"];
export const ERR_CAPABILITY_DENIED = __entry["ERR_CAPABILITY_DENIED"];
export const ERR_COMMAND_INTERRUPTED = __entry["ERR_COMMAND_INTERRUPTED"];
export const ERR_COMMAND_NOT_FOUND = __entry["ERR_COMMAND_NOT_FOUND"];
export const ERR_COMMAND_RUNNING = __entry["ERR_COMMAND_RUNNING"];
export const ERR_COMMAND_TIMEOUT = __entry["ERR_COMMAND_TIMEOUT"];
export const ERR_INTERNAL = __entry["ERR_INTERNAL"];
export const ERR_INVALID_ACTION = __entry["ERR_INVALID_ACTION"];
export const ERR_INVALID_STATE = __entry["ERR_INVALID_STATE"];
export const ERR_MODE_UNSUPPORTED = __entry["ERR_MODE_UNSUPPORTED"];
export const ERR_OUTPUT_LIMIT = __entry["ERR_OUTPUT_LIMIT"];
export const ERR_PATH_NOT_FOUND = __entry["ERR_PATH_NOT_FOUND"];
export const ERR_PERMISSION_DENIED = __entry["ERR_PERMISSION_DENIED"];
export const ERR_SNAPSHOT_INVALID = __entry["ERR_SNAPSHOT_INVALID"];
export const ERR_SNAPSHOT_UNSUPPORTED = __entry["ERR_SNAPSHOT_UNSUPPORTED"];
export const createTerminalSession = __entry["createTerminalSession"];
export const createTranscriptStore = __entry["createTranscriptStore"];
export const createInputController = __entry["createInputController"];
export const createCompletionEngine = __entry["createCompletionEngine"];
export const createOutputStreamController = __entry["createOutputStreamController"];
export const createInteractionModeMachine = __entry["createInteractionModeMachine"];
export const formatJson = __entry["formatJson"];
export const formatTree = __entry["formatTree"];
export const createCapabilityBroker = __entry["createCapabilityBroker"];
export const createLayer = __entry["createLayer"];
export const createPath = __entry["createPath"];
export const createTimeline = __entry["createTimeline"];
export const createVisualHost = __entry["createVisualHost"];
export const getBounds = __entry["getBounds"];
export const layoutTextPath = __entry["layoutTextPath"];
export const tokenizeText = __entry["tokenizeText"];
export const createAdapterContractTests = __entry["createAdapterContractTests"];
export const createExtensionContractTests = __entry["createExtensionContractTests"];
export const createSessionContractTests = __entry["createSessionContractTests"];
export default __entry;
